---
covers:
  - packages/demo-ui/src/markdown/crepe-config.ts
  - packages/demo-ui/src/markdown/remote-image-paste.ts
  - packages/demo-ui/src/markdown/remote-image-localizer.ts
  - packages/demo-ui/src/markdown/remote-image-paste.test.ts
  - packages/demo-ui/src/markdown/remote-image-localizer.test.ts
  - packages/demo-ui/src/PageRequirements.tsx
  - packages/demo-ui/src/NoteDialog.tsx
  - packages/demo-ui/src/MarkdownImageLightbox.tsx
  - packages/demo-ui/src/FieldRenderer.tsx
  - packages/demo-ui/src/markdown/render.ts
  - packages/shared/src/markdown-reference/*
  - packages/demo-ui/src/markdown/ProjectReferencePicker.tsx
  - packages/demo-ui/src/markdown/project-reference-presentation.ts
---

# Markdown 编辑器扩展与渲染适配

## 一、图片、附件与视频

Crepe ImageBlock 通过 `uploadHandler` 请求宿主处理本地文件和图片地址。编辑器不决定图片最终存储位置：知识库和创作端宿主可将外链图片交给当前 Session 的图床本地化能力；未提供处理器时才沿用浏览器临时对象地址。

网页富文本粘贴应先保留正文结构，再基于编辑器当前 Markdown 状态替换图片，避免异步处理覆盖用户刚刚粘贴的内容。图床下载、格式/大小限制、跨站展示和失败降级由[全局图床设计](../../../创作端/06-基础设施/技术/06_全局图床设计.md)维护。

BlockEdit 可以由宿主追加“上传视频/附件”等菜单项。扩展项必须通过宿主回调完成资源登记，不得让编辑器直接写入 Workspace 或绕过权限。

## 二、业务引用适配

`crepe-config.ts` 可按宿主传入的候选项追加“引用配置项”或“插入项目引用”入口。配置项引用（`@` + 名称 + 配置 key）与项目引用 `wb://` 分开识别，不能因为展示形式相似而共用解析规则。

项目引用的 canonical URI、实体目录、权限投影、backlinks 和发布索引由[项目页面文档引用与双向链接](../../../创作端/09-知识库/技术/03_项目页面文档引用与双向链接.md)维护。编辑器扩展只负责候选触发、插入、删除和点击回调，不直接解析项目文件或发起自定义协议导航。

`ProjectReferencePicker` 替换平铺候选列表，按目录元数据的稳定祖先 ID 组装页面树、按文档类别分组，不按名称合并节点，也不截断空查询的完整目录。页面或配置父节点的名称和展开箭头分别处理，目录加载、失败重试、空态与取消互相独立。选择通过一次 ProseMirror 事务插入 canonical 链接并清除后续输入的链接 mark；菜单操作不创建临时段落，晚到的已取消请求不能重新打开菜单。

`MarkdownReferenceProvider` 通过可选 `listProjects(signal)` 提供项目列表，通过请求的 `projectId` 指定目标项目；`context.source` 始终表示正在编辑的来源文档。宿主启用跨项目策略并提供项目列表后，选择器显示项目搜索与切换入口；仅支持本项目的宿主继续隐藏该入口。重新打开显式菜单默认当前项目，切换时保留页面/文档 Tab、重置树展开状态并取消旧请求。

整项目候选独立于页面/文档树：项目列表名称只改变范围，行尾快捷入口通过 `onProjectInsert` 请求并验证目标项目候选，再沿用单事务插入；选定范围下的「引用整个项目」使用该范围的 project candidate。入口受宿主 allowedTargetKinds 约束，异步验证需遵循菜单关闭、来源变化与实例清理边界。整项目展示和浮层继续复用统一 presentation。

编辑器按目标项目分别缓存成功目录和已解析状态，仅刷新正文中实际引用的项目；某个项目的加载失败不会使其他项目引用失效。401/403/404 清空对应目录并标记不可用，其他请求失败保留已知状态；同项目请求使用版本标记防止晚到响应覆盖新结果。项目或 session 对应的 provider 变化时清空缓存。来源展示及点击校验都按包含项目 ID 的完整 URI 匹配。

`projectReferencePresentation` 使用 ProseMirror decoration 显示类型图标、当前名称和来源浮层，不改写文档节点、保存快照或撤销历史。成功获取的目录才用于判断目标不可用；未知或请求失败不等价于已删除。编辑器在已有引用就绪和窗口重新获得焦点时刷新目录，名称读取失败时保留原显示。引用末尾的折叠光标由该 feature 归一化为不带 `wb://` 链接 mark 的后续输入状态；只移除引用链接 mark，不影响引用内部编辑、其他文本 mark 或普通外链。点击由宿主负责新页签导航，禁止浏览器直接请求 `wb://`。

悬停命中统一归一到引用 mark 包装层，图标、名称及内部格式节点共享同一提示；包装层增加水平 3px、垂直 4px 留白并以负外边距抵消排版影响，换行使用克隆盒装饰。鼠标进入延迟 200ms、离开延迟 150ms，移入气泡保持显示；键盘聚焦立即显示，Escape 关闭。气泡使用 Floating UI 固定定位、8px 间距和 12px 边界保护，自动翻转及平移，展示类型图标、类型与当前名称、来源路径及打开提示。

项目引用同时取消普通链接预览的待执行悬停任务，并在原生预览 `show` 入口按引用协议拦截，避免坐标命中绕过 DOM 事件过滤；普通外链仍委托原有预览。引用 markView 仅忽略自身展示属性及引用节点的 `aria-describedby` 属性变更，不忽略正文和子节点修改，防止提示的无障碍属性被误解析为正文编辑。回归测试覆盖异步悬停后文档不变、包装层命中、气泡停留与滚动、Escape 关闭、引用末尾继续输入、普通外链 mark 保持和原生坐标预览隔离。

## 三、只读渲染适配

编辑态使用 Milkdown，知识文档和页面要求的只读展示可以使用共享 Markdown renderer 或薄 adapter。Canvas、Note、Streamdown、Markdown-it 和 Viewer 各自保留宿主外壳，但都应复用同一套 Markdown 语义和安全引用转换，不把 `wb://` 交给浏览器原生导航。

只读 renderer 不加载编辑器实例、不创建保存队列，也不暴露编辑菜单。发布端只读取不可变快照，不能通过渲染器访问 live Workspace。所有只读 Markdown 容器复用图片预览层：点击已渲染图片只读取其现有 URL 与替代文本，以自适应弹层查看；关闭不影响正文或资源引用，且不改变既有的图片 URL 清洗策略。

## 四、扩展原则

- 扩展通过 typed props 和宿主回调注入。
- 没有稳定 source context 时不启用项目实体候选。
- 扩展失败时保留可恢复正文，不静默写入未经处理的受管资源地址。
- 新扩展先定义 canonical Markdown 表达，再实现编辑态与只读态 adapter。
