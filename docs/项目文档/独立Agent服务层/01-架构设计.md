# 独立 Agent 服务层 - 架构设计文档

> 版本：v2.1
> 创建日期：2026-04-05
> 更新日期：2026-06-25
> 状态：已实现

---

## 一、背景与目标

### 1.1 问题背景

当前项目中，Agent 相关逻辑直接嵌入在 Next.js 的 API Routes 中，存在以下问题：

| 问题         | 影响                                       |
| :----------- | :----------------------------------------- |
| **耦合度高** | Agent 逻辑与 Web 框架紧耦合，难以独立演进  |
| **后端形态变化** | 早期多后端方案已迁移为 Pi Agent 单后端，需要文档与代码保持一致 |
| **复用性低** | Agent 能力与前端紧耦合，难以独立测试和维护 |
| **部署受限** | 必须与前端一起部署，无法独立扩缩容         |

### 1.2 设计目标

1. **解耦**：将 Agent 服务层从 Next.js 应用中剥离，作为独立服务运行
2. **单后端聚焦**：当前只支持 Pi Agent，避免恢复已移除的多后端架构
3. **高性能**：支持流式响应、WebSocket 实时推送
4. **易维护**：AI 编程友好，代码结构清晰

---

## 二、整体架构

### 2.1 架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              消费层 (Consumers)                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                           Web 前端 (Next.js)                           │  │
│  └─────────────────────────────────────────┬─────────────────────────────┘  │
└────────────────────────────────────────────┼────────────────────────────────┘
                                             │
                                             │     HTTP REST / WebSocket
                                             │
┌────────────────────────────────────────────┴────────────────────────────────┐
│                           Agent 服务层 (独立服务)                            │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                          API Gateway                                 │   │
│  │   • REST API: POST /api/agent/:sessionId/message                     │   │
│  │   • WebSocket: /api/agent/:sessionId/stream                          │   │
│  │   • Health: GET /health                                              │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│  ┌─────────────────────────────────┴────────────────────────────────────┐   │
│  │                        Agent Manager                                  │   │
│  │   • Agent 生命周期管理（创建、缓存、销毁）                             │   │
│  │   • Session 状态管理                                                  │   │
│  │   • 事件分发（stream、error、finish）                                 │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│  ┌─────────────────────────────────┴────────────────────────────────────┐   │
│  │                         Pi Agent Backend                              │   │
│  │   • 通过 @earendil-works/pi-agent-core 进程内嵌入                     │   │
│  │   • 统一处理模型配置、工具调用、事件转换和文件变更捕获                 │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 分层职责

| 层级                | 职责                         | 关键模块                       |
| :------------------ | :--------------------------- | :----------------------------- |
| **消费层**          | 用户交互、UI 渲染            | Web 前端 (Next.js)             |
| **API Gateway**     | 请求路由、认证、限流         | Fastify 路由、中间件           |
| **Agent Manager**   | Agent 生命周期、Session 管理 | AgentManager、SessionStore     |
| **Backend Agent**   | 包装 Pi Agent 后端并接入服务生命周期 | BackendAgent、PiAgentBackend   |
| **Pi Tools**        | 文件、截图、控制台、页面、子 Agent 等工具 | `backends/pi-tools/`           |

---

## 三、核心设计原则

### 3.1 单后端适配

**实现文件**：[agent-factory.ts](../packages/agent-service/src/core/agent-factory.ts)

**核心思路**：

- `AgentFactory` 当前只创建 Pi Agent 后端实例。
- 保留工厂入口是为了隔离创建过程和配置装配，不表示要恢复多后端动态注册。
- 新增模型或供应商能力应走 Pi Agent 配置，不新增新的后端适配器。

### 3.2 观察者模式

Agent 与消费层通过事件解耦，Agent 通过回调函数向外部发送流式内容、错误和完成事件。

**实现文件**：[agent.ts](../packages/agent-service/src/core/agent.ts)

**核心思路**：

- 继承 Node.js EventEmitter 实现事件发射
- 定义统一的事件类型：`stream`、`error`、`finish`、`status`
- 消费者通过 `on()`/`off()` 订阅事件

### 3.3 单例模式

AgentManager 作为全局单例，管理所有 Agent 实例的生命周期。

**实现文件**：[agent-manager.ts](../packages/agent-service/src/core/agent-manager.ts)

**核心思路**：

- 使用 Map 缓存 Agent 实例，避免重复创建
- `getOrCreate()` 方法实现懒加载和缓存复用
- 提供 `destroy()` 和 `destroyAll()` 方法清理资源

### 3.4 后端适配接口

**接口定义**：[backends/base.ts](../packages/agent-service/src/backends/base.ts)

**核心思路**：

- 定义 `IBackendAdapter` 接口约束服务层与 Pi Agent 后端的交互。
- Pi Agent 后端负责把底层事件转换为服务层统一事件。
- 接口存在是为了稳定服务边界，不代表多后端是当前目标。

---

## 四、目录结构

```
packages/
├── agent-service/                    # 独立 Agent 服务
│   ├── src/
│   │   ├── core/                     # 核心模块
│   │   │   ├── types.ts              # 类型定义
│   │   │   ├── agent.ts              # Agent 基类
│   │   │   ├── agent-manager.ts      # Agent 管理器
│   │   │   ├── agent-factory.ts      # Agent 工厂
│   │   │   └── backend-agent.ts      # 后端 Agent 适配器
│   │   │
│   │   ├── backends/                 # Pi Agent 后端适配器
│   │   │   ├── base.ts               # 后端适配接口
│   │   │   ├── pi-agent.ts           # Pi Agent 后端（ESM 动态导入）
│   │   │   ├── pi-tools/             # Pi Agent 工具集
│   │   │   └── index.ts              # 导出
│   │   │
│   │   ├── session/                  # Session 管理
│   │   │   ├── session-store.ts      # Session 存储
│   │   │   ├── session-guard.ts      # 文件访问校验
│   │   │   └── snapshot-service.ts   # 快照服务
│   │   │
│   │   ├── workspace/                # 工作空间管理
│   │   │   ├── workspace-manager.ts  # 工作空间管理器
│   │   │   ├── project-workspace-manager.ts  # 项目工作空间管理器
│   │   │   ├── utils.ts              # 工具函数
│   │   │   └── index.ts              # 导出
│   │   │
│   │   ├── events/                   # 事件系统
│   │   │   └── event-bus.ts          # 事件总线
│   │   │
│   │   ├── routes/                   # API 路由
│   │   │   ├── agent.ts              # Agent 相关路由
│   │   │   ├── websocket.ts          # WebSocket 路由
│   │   │   ├── ws-event-router.ts    # WS 事件路由
│   │   │   ├── models.ts             # 模型管理路由
│   │   │   ├── projects.ts           # 项目管理路由
│   │   │   ├── validate.ts           # 校验路由
│   │   │   └── index.ts              # 路由注册
│   │   │
│   │   ├── utils/                    # 工具函数
│   │   │   ├── logger.ts             # 日志
│   │   │   └── config.ts             # 配置
│   │   │
│   │   └── server.ts                 # 服务入口
│   │
│   ├── package.json
│   ├── tsconfig.json
│   └── vitest.config.ts
```

---

## 五、技术选型

| 组件            | 选型           | 理由                                 |
| :-------------- | :------------- | :----------------------------------- |
| **运行时**      | Node.js 18+    | Monorepo 要求，与 Pi Agent 依赖兼容  |
| **语言**        | TypeScript 5.x | 类型安全，代码提示完善               |
| **框架**        | Fastify        | 高性能，原生支持 WebSocket           |
| **HTTP 客户端** | undici         | 比 node-fetch 更快，原生支持流式响应 |
| **日志**        | pino           | Fastify 默认日志库，高性能           |
| **测试**        | vitest         | agent-service 专用测试框架           |

---

## 六、通信协议

### 6.1 REST API

| 端点                                  | 方法                | 说明               |
| :------------------------------------ | :------------------ | :----------------- |
| `/api/agent/:sessionId/message`       | POST                | 发送消息           |
| `/api/agent/:sessionId`               | GET                 | 获取 Agent 状态    |
| `/api/agent/:sessionId`               | DELETE              | 销毁 Agent         |
| `/api/agent/:sessionId/files`         | GET                 | 获取变更文件       |
| `/api/agent/:sessionId/rollback`      | POST                | 回滚文件变更       |
| `/api/agent/:sessionId/workspace`     | GET/PUT             | 获取/切换工作空间  |
| `/api/agent/:sessionId/files/stage`   | POST                | 暂存文件           |
| `/api/agent/:sessionId/files/discard` | POST                | 丢弃变更           |
| `/api/sessions`                       | GET                 | 列出所有 Session   |
| `/api/models/*`                       | GET/POST            | 模型管理（新增）   |
| `/api/projects/*`                     | GET/POST/PUT/DELETE | 项目管理（新增）   |
| `/api/validate/*`                     | POST                | 校验服务（新增）   |
| `/health`                             | GET                 | 健康检查           |
| `/models`                             | GET                 | 获取 Pi Agent 可用模型信息 |

**路由实现**：[routes/agent.ts](../packages/agent-service/src/routes/agent.ts)

### 6.2 WebSocket

```
ws://host/api/agent/:sessionId/stream

客户端发送：
{ "type": "message", "content": "..." }

服务端推送：
{ "type": "stream", "content": "..." }
{ "type": "error", "message": "..." }
{ "type": "finish", "files": [...] }
```

**WebSocket 实现**：[routes/websocket.ts](../packages/agent-service/src/routes/websocket.ts)

---

## 七、安全设计

### 7.1 三层防护隔离机制

Agent 服务采用**三层防护**实现安全隔离：

| 层级   | 机制                                   | 强度 | 说明                   |
| :----- | :------------------------------------- | :--- | :--------------------- |
| **L1** | Pi Agent 工具权限与路径白名单          | 强   | 工具调用级别的访问控制 |
| **L2** | 行为约束（AGENTS.md）                  | 软   | AI 行为规范和约束      |
| **L3** | 后端校验（session-guard 文件变更检测） | 强   | 运行时校验和自动回滚   |

**L3 后端校验实现**：[session/session-guard.ts](../packages/agent-service/src/session/session-guard.ts)

**核心思路**：

- 定义允许修改的文件白名单
- 每次文件变更前校验路径是否在白名单内
- 违规时自动回滚并记录日志
- 防止路径遍历攻击和符号链接攻击

### 7.2 认证（可选）

支持 API Key 和 Bearer Token 两种认证方式，通过 Fastify 中间件实现。

### 7.3 速率限制

使用 `@fastify/rate-limit` 插件实现请求限流，防止滥用。

---

## 八、性能考量

### 8.1 连接池

使用 undici 的连接池管理 HTTP 连接，提升并发性能。

**实现位置**：[backends/pi-agent.ts](../packages/agent-service/src/backends/pi-agent.ts)

### 8.2 Agent 缓存

AgentManager 内部使用 Map 缓存 Agent 实例，避免重复创建和初始化。

**实现位置**：[core/agent-manager.ts](../packages/agent-service/src/core/agent-manager.ts)

### 8.3 流式响应优化

使用 Transfer-Encoding: chunked 实现流式响应，直接写入底层 socket，减少内存占用。

---

## 九、扩展性设计

### 9.1 扩展 Pi Agent 工具

新增 Agent 能力时优先扩展 `backends/pi-tools/`，并同时补齐路径权限、文件变更捕获、事件流和测试。不要通过新增后端适配器来绕过当前 Pi Agent 单后端边界。

### 9.2 插件系统（未来）

预留插件接口，支持在消息处理前后插入自定义逻辑。

---

## 十、与现有系统集成

### 10.1 迁移策略

```
阶段 1：并行运行
├── Agent 服务独立部署
├── Web 前端同时支持新旧 API
└── 逐步切换流量

阶段 2：完全迁移
├── 删除 Web 前端中的 Agent 逻辑
├── 所有请求走 Agent 服务
└── 清理遗留代码
```

### 10.2 共享类型

类型定义统一放在 `packages/shared/src/` 目录，供 Web 前端和 Agent 服务共同使用。

---

## 十一、监控与运维

### 11.1 健康检查

`/health` 端点返回服务状态、运行时间、Agent 数量、内存使用等信息。

### 11.2 指标收集（未来）

预留 Prometheus 指标收集接口，支持监控系统集成。

---

## 十二、总结

本架构设计遵循以下核心原则：

1. **解耦**：Agent 服务独立部署，消费层轻量化
2. **单后端聚焦**：统一服务边界，当前只接入 Pi Agent
3. **高性能**：流式响应、连接池、缓存
4. **可扩展**：通过 Pi Agent 工具、配置和未来插件扩展能力
5. **易维护**：清晰的分层结构、完善的文档

下一步：参考 [02-接口规范.md](./02-接口规范.md) 了解详细的 API 设计。
