---
covers:
  - packages/demo-ui/src/ConfigForm.tsx
  - packages/demo-ui/src/FieldRenderer.tsx
  - packages/demo-ui/src/OptionGroup.tsx
  - packages/demo-ui/src/ConfigItemEditorDialog.tsx
  - packages/demo-ui/src/comment/ConfigCommentPopover.tsx
  - packages/demo-ui/src/note-upload.ts
  - packages/demo-ui/src/PageConfigPanel.tsx
  - packages/shared/src/demo/config-schema-definition.ts
  - packages/demo-ui/src/ConfigForm.test.tsx
  - packages/demo-ui/src/widgets.tsx
  - packages/demo-ui/src/widgets.test.tsx
  - packages/author-site/src/app/api/sessions/[sessionId]/assets/upload/asset-validation.ts
  - packages/author-site/src/app/api/sessions/[sessionId]/assets/upload/route.ts
  - packages/author-site/src/app/api/sessions/[sessionId]/workspace/[...path]/route.ts
  - packages/demo-ui/src/ConfigDefinitionManagerDialog.test.ts
  - packages/shared/src/comment.ts
---

# 配置系统 - 表单生成器

## 一、职责边界

`ConfigForm` 根据页面或项目 `config.schema.json` 生成运行时配置表单，`PageConfigPanel` 负责共享配置与页面配置的分层展示。表单只维护当前会话中的临时值，配置持久化和预览投影仍由宿主负责。

## 二、控件映射

字段映射按 `ui:widget`、`format`、基础类型三层解析。`enum` 是值语义和校验约束；没有控件覆盖时使用下拉选择器。单值枚举可通过 `ui:widget` 选择 `radio` 或 `segmented`，两者只改变展示，不改变枚举值、默认值和校验行为。

选项展示优先使用 `enumNames`，否则显示枚举值。表单把枚举项映射为稳定的内部选项索引，提交时再还原为 Schema 中的原始值，因此字符串、数字枚举都能使用同一套交互。

## 三、共享选项组

`OptionGroup` 是 `demo-ui` 内的共享展示组件，使用原生 `input[type=radio]` 作为交互基础：

- `radio` 以纵向选项组呈现，适合需要同时比较的少量或较长文案；
- `segmented` 以填满可用宽度的横向分段呈现，固定 30px 高度并使用均匀留白和更强的选中对比，适合桌面端 2–4 个短标签和频繁切换；触控优先场景应使用 `radio`；
- 两种变体都提供 `radiogroup` 名称、可见焦点、禁用态和浏览器原生方向键行为；
- 标签过长、选项较多或容器狭窄时，产品层应选择 `radio` 或下拉，而不是强行压缩分段控件。

创作端和使用端都通过 `demo-ui` 的 `FieldRenderer` 消费同一实现。创作端视觉属性面板已有的专用分段交互不改变配置表单契约；后续若需要统一视觉，可在共享组件层复用样式。

## 四、配置定义编辑

配置定义编辑器仍以 `enum` 作为唯一的枚举字段类型，并额外保存 `enumWidget` 展示偏好。写入 Schema 时，`radio` / `segmented` 转换为 `ui:widget`；`select` 删除这两个覆盖值，回到默认下拉。读取旧 Schema 时没有覆盖值的枚举字段回填为 `select`。

默认值编辑器会复制同一控件覆盖，避免编辑默认值时出现与日常配置面板不同的交互。控件偏好属于字段元数据，不参与运行值清理计划，也不会触发字段类型变更。

## 五、其它控件

普通文本、数字、开关、颜色、图片、视频、MP3 音频、富文本、数组和定位字段继续沿用现有 `FieldRenderer` 分支。图片与文件由 `FileUploadWidget`、`ImageListWidget` 负责上传和尺寸规则；`format: "file"` 字段的 `accept` 包含 `audio/mpeg` 或 `.mp3` 时自动切换为音频模式，默认大小上限为 1 MiB，上传后展示音频状态卡片，不自动播放且不渲染图片。颜色输入保持紧凑高度，容器使用 `w-full min-w-[132px] max-w-[180px]` 自适应宽度，并对 Hex 文本进行溢出处理。排序、定位和复杂对象数组仍由专用控件承载，不通过枚举选项组表达。

## 六、配置项能力与标题批注气泡

`PageConfigPanel` 按当前实际页面计算项目级、页面级的 `ConfigItemCapabilities`，再由 `ConfigForm` 传给每个 `FieldRenderer`。能力拆成四个维度：定义是否可编辑、值是否可编辑、是否可添加批注和不可编辑原因。这样“不能改值”与“不能改定义”不会再被单一 `readonly` 混为一谈。

字段标题是可聚焦的操作触发器：鼠标悬浮或键盘聚焦时打开受控 Popover，标题与菜单之间移动时保持打开，离开后延迟关闭，支持 Enter/Space、Escape 和点击外部关闭。可编辑字段显示“编辑配置项”和“添加批注”，不可编辑字段只显示“添加批注”；有未解决批注时标题旁显示小圆点徽标。设计规范按钮保持独立并阻止事件冒泡，不触发配置操作菜单。

`ConfigCommentPopover` 以 `scope + pageId + fieldKey` 精确筛选线程，沿用规范气泡的 portal、定位、箭头、最大高度和移动端全宽回退；正文按最新在前顺序在同一个可滚动气泡内展示。创作端主批注可在当前卡片内使用共享 `RichTextEditor` 原位编辑，新增、回复、解决/恢复和删除继续复用现有接口；浏览端只传入线程数据并隐藏全部写入控件。配置项评论不回写 Schema，也不复用 `$demo.note`。嵌套对象数组将能力、批注回调和字段路径继续向子 `FieldRenderer` 传递。

### 6.1 值编辑能力的控件覆盖

`canEditValue=false` 时，文本、长文本、数字、整数、滑块、开关、颜色、枚举选择、单选/分段、图片、文件、音频、视频、富文本、数组、级联、多选、定位和白板入口均不得产生配置变更；数组的新增、删除、排序操作也一并隐藏。该规则只影响配置栏交互，评论和设计规范入口仍按各自能力工作。

## 七、验证与测试

- Schema 定义 mutation 覆盖 `select`、`radio`、`segmented` 的读写回环；
- `ConfigForm` 覆盖默认值回显、`enumNames` 标签和选项切换后的原始枚举值回调；
- 共享组件使用语义化 `radiogroup` / `radio`，并检查键盘可达性所需的原生表单结构；
- `FileUploadWidget` 覆盖 MP3 accept 自动识别、1 MiB 客户端拦截、音频状态卡片和替换/删除操作；颜色输入覆盖自适应宽度与完整 Hex 值展示；
- 配置项能力矩阵覆盖普通页、引用页、模板页普通编辑者和模板页管理员；字段标题覆盖悬浮/聚焦打开、键盘操作、Escape、外部点击、菜单延迟关闭及规范按钮隔离；配置项 Markdown 批注覆盖多线程顺序、创作端原位编辑和浏览端只读。
- `pnpm --filter @workbench/demo-ui typecheck` 与 `pnpm --filter @workbench/demo-ui test` 作为改动后的基础门禁。
