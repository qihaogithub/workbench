---
covers:
  - packages/author-site/src/app/admin/model-config/page.tsx
  - packages/author-site/src/components/admin/model-selector.tsx
  - packages/author-site/src/lib/ai-models.ts
  - packages/agent-service/src/routes/models.ts
---

# 模型选择交互设计

## 页面布局

模型配置页面分为三个区域:

```
┌─────────────────────────────────────────────┐
│ 顶部操作栏                                    │
│ [拉取模型] 按钮  |  [保存配置] 按钮          │
├─────────────────────────────────────────────┤
│ 已启用模型区 (Enabled Models)                │
│ ┌───────────────────────────────────────┐   │
│ │ 🗑️ 拖拽柄 | 序号 | 模型名称 | 分组    │   │
│ │ ☑️ 启用开关  🖼️ 多模态开关            │   │
│ ├───────────────────────────────────────┤   │
│ │ 🗑️ 拖拽柄 | 2 | gpt-4o | xjjj/      │   │
│ │ ☑️ 启用开关  ☑️ 多模态开关            │   │
│ └───────────────────────────────────────┘   │
├─────────────────────────────────────────────┤
│ 自动启用规则区 (Auto Enable Rules)           │
│ ┌───────────────────────────────────────┐   │
│ │ 规则类型: 分组前缀                     │   │
│ │ 规则值: xjjj/                          │   │
│ │ 匹配: 5 个模型  [批量启用] [删除]      │   │
│ ├───────────────────────────────────────┤   │
│ │ 规则类型: 名称关键词                   │   │
│ │ 规则值: Free                           │   │
│ │ 匹配: 2 个模型  [批量启用] [删除]      │   │
│ └───────────────────────────────────────┘   │
│ [+ 添加规则] 按钮                            │
├─────────────────────────────────────────────┤
│ 未启用模型区 (Disabled Models)               │
│ [搜索框: 输入模型 ID 或名称过滤]             │
│ ┌───────────────────────────────────────┐   │
│ │ claude-3-opus | anthropic/            │   │
│ │ [启用] 按钮                            │   │
│ ├───────────────────────────────────────┤   │
│ │ gemini-pro | google/  🏷️ 自动启用     │   │
│ │ [启用] 按钮                            │   │
│ └───────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

### 已启用模型区

**展示内容**:

- 拖拽柄: 用于调整顺序
- 序号: 自动编号,表示优先级(1 为最高)
- 模型 ID: 完整标识(如 `xjjj/deepseek-v4-flash`)
- 分组标签: 从模型 ID 提取的前缀(如 `xjjj/`)
- 默认标记: 序号为 1 的模型自动显示「默认」徽章
- 多模态开关: 控制该模型是否支持图片输入
- 启用开关: 关闭后模型移至未启用区

**交互行为**:

- 拖拽调整顺序 → 实时更新 `enabledModels` 数组
- 关闭启用开关 → 从 `enabledModels` 移除,加入未启用区
- 开启多模态开关 → 模型 ID 加入 `multimodalModels` 数组

### 自动启用规则区

**规则类型**:

1. **分组前缀** (`prefix`):
   - 匹配模型 ID 的前缀(如 `xjjj/`)
   - 示例: 规则值 `xjjj/` 匹配 `xjjj/deepseek-v4-flash`、`xjjj/gpt-4o` 等

2. **名称关键词** (`nameFilter`):
   - 格式: `分组:关键词`(如 `opencode:Free`)
   - 匹配逻辑: 模型 ID 以该分组开头,且名称部分包含关键词
   - 示例: 规则值 `opencode:Free` 匹配 `opencode/free-model-1`

**交互行为**:

- 添加规则 → 输入规则类型和值 → 显示匹配计数
- 点击「批量启用」→ 所有匹配规则的模型加入已启用列表
- 删除规则 → 从 `autoEnableRules` 移除(已启用的模型不受影响)

### 未启用模型区

**展示内容**:

- 模型 ID 和分组标签
- 匹配自动启用规则的模型显示「自动启用」徽章
- 「启用」按钮: 点击后模型加入已启用列表末尾

**搜索过滤**:

- 顶部搜索框支持按模型 ID 或名称过滤
- 实时过滤,输入即搜索

**交互行为**:

- 点击「启用」→ 模型追加到 `enabledModels` 末尾 → 移至已启用区
- 如果模型匹配自动启用规则,显示徽章提示

## 技术实现

### 拖拽排序

使用 `@dnd-kit/core` + `@dnd-kit/sortable` 实现:

```typescript
import { DndContext, closestCenter } from "@dnd-kit/core";
import {
  SortableContext,
  verticalListSortingStrategy,
  useSortable,
} from "@dnd-kit/sortable";

// 拖拽结束事件处理
function handleDragEnd(event: DragEndEvent) {
  const { active, over } = event;
  if (!over || active.id === over.id) return;

  const oldIndex = enabledModels.indexOf(active.id as string);
  const newIndex = enabledModels.indexOf(over.id as string);

  // 数组元素移动
  const newOrder = arrayMove(enabledModels, oldIndex, newIndex);
  setEnabledModels(newOrder);
}
```

**关键点**:

- `verticalListSortingStrategy`: 垂直列表排序策略
- `arrayMove()`: 工具函数,安全移动数组元素
- 拖拽结束后立即更新状态,实时反映到 UI

### 自动启用规则匹配

匹配逻辑在 `ai-models.ts` 的 `applyModelConfigsWithFullData()` 中实现:

```typescript
function matchesAutoEnableRules(
  modelId: string,
  rules: AutoEnableRule[],
): boolean {
  return rules.some((rule) => {
    if (rule.type === "prefix") {
      return modelId.startsWith(rule.value);
    }
    if (rule.type === "nameFilter") {
      const [group, keyword] = rule.value.split(":");
      return modelId.startsWith(group + "/") && modelId.includes(keyword);
    }
    return false;
  });
}
```

**过滤策略**:

1. 首先保留 `enabledModels` 列表中的所有模型
2. 扫描所有后端可用模型,匹配自动启用规则的加入列表
3. 最终结果 = `enabledModels`(按原顺序) + 新匹配模型(追加末尾)

### 数据流

```
页面加载
  ↓
调用 GET /api/admin/available-models
  ↓
显示后端可用模型列表
  ↓
调用 GET /api/admin/model-config
  ↓
加载当前配置(enabledModels + autoEnableRules)
  ↓
渲染三个区域
  ↓
用户操作(开关/拖拽/添加规则)
  ↓
更新本地状态
  ↓
点击「保存配置」
  ↓
PUT /api/admin/model-config
  ↓
写入 SQLite,清除缓存
  ↓
前端收到成功响应,重新拉取配置
```

## 配置数据结构

```typescript
{
  frontend: {
    enabledModels: [                     // 启用模型列表(有序)
      "xjjj/deepseek-v4-flash",
      "xjjj/gpt-4o",
      "anthropic/claude-3-opus"
    ],
    autoEnableRules: [                   // 自动启用规则
      { type: "prefix", value: "xjjj/" },
      { type: "nameFilter", value: "opencode:Free" }
    ]
  },
  multimodalModels: [                    // 多模态模型
    "xjjj/gpt-4o",
    "anthropic/claude-3-opus"
  ]
}
```

## 后端模型查询

### Agent-service 端点

新增 `GET /models` 端点([routes/models.ts](../../../../packages/agent-service/src/routes/models.ts)):

```typescript
fastify.get("/models", async (request, reply) => {
  const backend = factory.getCurrentBackend();
  const models = await backend.getModelInfo();
  return { success: true, data: { models } };
});
```

**实现细节**:

- 复用 `OpenCodeHttpBackend.getModelInfo()` 方法
- 不依赖 WebSocket session,直接 HTTP 查询
- 返回模型列表: `[{ id: "xjjj/deepseek-v4-flash", label: "DeepSeek V4 Flash" }]`

### Author-site 代理 API

新增 `GET /api/admin/available-models`([route.ts](../../../../packages/author-site/src/app/api/admin/available-models/route.ts)):

```typescript
export async function GET(request: Request) {
  if (!(await verifyAdminRequest(request))) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  // 代理请求 agent-service
  const res = await fetch(`${AGENT_SERVICE_URL}/models`, {
    signal: AbortSignal.timeout(5000), // 5s 超时
  });

  const data = await res.json();
  return NextResponse.json(data);
}
```

**设计理由**:

- 管理后台前端不直接访问 agent-service(跨域问题)
- 通过 author-site 代理,统一鉴权(admin 权限)
- 5s 超时防止后端响应慢阻塞页面

## 优势

1. **零配置上手**: 无需知道模型 ID,打开页面即可看到所有可用模型
2. **所见即所得**: 开关状态直观反映启用情况
3. **拖拽排序**: 优先级调整更符合直觉
4. **智能规则**: 自动启用规则减少重复操作,后端新增模型时自动匹配
5. **实时反馈**: 匹配计数、自动启用徽章等视觉提示

## 相关文件

- 模型配置页面: [model-config/page.tsx](../../../../packages/author-site/src/app/admin/model-config/page.tsx)
- 模型过滤逻辑: [ai-models.ts](../../../../packages/author-site/src/lib/ai-models.ts)
- Agent-service 模型端点: [routes/models.ts](../../../../packages/agent-service/src/routes/models.ts)
- 代理 API: [available-models/route.ts](../../../../packages/author-site/src/app/api/admin/available-models/route.ts)
