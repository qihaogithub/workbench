# 项目工作空间与版本管理 - 实现方案

> 状态：待实现
> 关联文档：02_生命周期设计_v2.md、03_草稿工作区_v2.md

---

## 一、现状分析

### 1.1 已实现（v1.0）

当前代码运行的是 Demo 模型：

```typescript
// packages/shared/src/types.ts
interface SessionMeta {
  sessionId: string
  demoId: string           // 实际存的是 projectId
  createdAt: number
  expiresAt: number
}
```

**已有功能**：
- 项目创建（`createProject`）
- Session 创建/读取/合并/删除
- 临时工作空间隔离（`sessions/{userId}/{projectId}/{sessionId}/`）
- 项目元数据 `project.json`（含空 `versions: []`）
- Snapshots 目录已定义但未使用

### 1.2 未实现（v2.0）

v2.0 设计文档中描述但未落地的功能：

| 功能 | 状态 | 说明 |
|:-----|:-----|:-----|
| 版本快照（Snapshot） | ❌ 未实现 | 保存时未创建历史备份 |
| 版本号生成 | ❌ 未实现 | 无 `v1`, `v2` 自动编号逻辑 |
| 版本历史查询 | ❌ 未实现 | `project.json` 中 `versions` 始终为空 |
| 版本恢复 | ❌ 未实现 | 无回滚到历史版本的能力 |
| 版本清理 | ❌ 未实现 | `MAX_VERSIONS_KEEP = 50` 未生效 |
| Session 状态追踪 | ❌ 未实现 | `status: 'editing' \| 'saved' \| 'discarded'` |
| basedOnVersion 记录 | ❌ 未实现 | 编辑基于哪个版本开始 |
| 用户级 Session 隔离 | ⚠️ 部分实现 | `createEditSession` 有 userId，但元数据中未存储 |

---

## 二、实现目标

将当前系统从 Demo 模型升级为**项目工作空间 + 版本管理**模型，实现以下能力：

1. **版本快照**：每次保存自动生成历史备份
2. **版本追溯**：查看项目完整版本历史
3. **版本恢复**：回滚到任意历史版本
4. **自动清理**：保留最近 50 个版本，自动删除旧版本
5. **编辑溯源**：记录每次编辑基于哪个版本

---

## 三、数据模型变更

### 3.1 类型升级

当前 `packages/shared/src/types.ts` 中的 `SessionMeta` 需要扩展：

```typescript
// packages/shared/src/types.ts —— 升级 SessionMeta
export interface SessionMeta {
  sessionId: string
  demoId: string              // 兼容字段，实际值为 projectId
  userId?: string             // 新增：编辑者标识
  createdAt: number
  expiresAt: number
  status?: 'editing' | 'saved' | 'discarded'  // 新增：会话状态
  basedOnVersion?: string     // 新增：基于哪个版本开始编辑
  opencodeSessionId?: string | null
}
```

`packages/shared/src/workspace.ts` 中的类型已定义完整，可直接使用：

```typescript
// 已定义，直接使用
interface VersionInfo {
  versionId: string           // "v1", "v2"...
  savedAt: number
  savedBy: string
  sessionId: string
  snapshotPath: string
  fileCount: number
  note?: string
}

interface Project {
  id: string
  name: string
  description?: string
  workspacePath: string
  versions: VersionInfo[]     // 当前为空数组，需填充
  createdAt: number
  updatedAt: number
}
```

### 3.2 project.json 结构演进

当前 `createProject` 生成的 `project.json`：

```json
{
  "id": "proj_123",
  "name": "项目名称",
  "workspacePath": ".../proj_123/workspace",
  "versions": [],           // ← 当前为空
  "createdAt": 1234567890,
  "updatedAt": 1234567890
}
```

升级后：

```json
{
  "id": "proj_123",
  "name": "项目名称",
  "workspacePath": ".../proj_123/workspace",
  "versions": [
    {
      "versionId": "v1",
      "savedAt": 1234567890,
      "savedBy": "user_001",
      "sessionId": "session-xxx",
      "snapshotPath": ".../snapshots/proj_123/v1",
      "fileCount": 5,
      "note": "初始版本"
    },
    {
      "versionId": "v2",
      "savedAt": 1234568000,
      "savedBy": "user_001",
      "sessionId": "session-yyy",
      "snapshotPath": ".../snapshots/proj_123/v2",
      "fileCount": 5,
      "note": "修复了按钮样式"
    }
  ],
  "createdAt": 1234567890,
  "updatedAt": 1234568000
}
```

---

## 四、核心功能实现

### 4.1 版本号生成

**位置**：`packages/author-site/src/lib/fs-utils.ts`

```typescript
function generateVersionId(project: Project): string {
  const count = project.versions.length
  return `v${count + 1}`
}
```

### 4.2 保存时创建快照（核心变更）

**位置**：`packages/author-site/src/lib/session-manager.ts` 中 `saveEditSession`

当前实现（无版本快照）：

```typescript
export function saveEditSession(sessionId: string): boolean {
  // 1. 直接覆盖 workspace
  fs.rmSync(workspacePath, { recursive: true, force: true })
  fs.cpSync(sessionPath, workspacePath, { recursive: true })
  // 2. 删除 session
  deleteSession(sessionId)
}
```

升级后（含版本快照）：

```typescript
export function saveEditSession(
  sessionId: string,
  userId?: string,
  note?: string
): SaveResult {
  const sessionMeta = getEditSession(sessionId)
  if (!sessionMeta) return { success: false, error: 'Session not found' }

  const projectId = sessionMeta.demoId
  const projectPath = getProjectPath(projectId)
  const workspacePath = path.join(projectPath, 'workspace')
  const projectJsonPath = path.join(projectPath, 'project.json')
  const sessionPath = getSessionPath(sessionId)

  // 1. 读取项目元数据
  const project: Project = JSON.parse(
    fs.readFileSync(projectJsonPath, 'utf-8')
  )

  // 2. 生成新版本号
  const versionId = generateVersionId(project)
  const snapshotPath = getSnapshotPath(projectId, versionId)

  // 3. 【关键】创建快照：备份当前 workspace 到 snapshots
  fs.mkdirSync(path.dirname(snapshotPath), { recursive: true })
  fs.cpSync(workspacePath, snapshotPath, { recursive: true })

  // 4. 临时空间覆盖正式空间
  fs.rmSync(workspacePath, { recursive: true, force: true })
  fs.cpSync(sessionPath, workspacePath, { recursive: true })
  fs.rmSync(path.join(workspacePath, '.session.json'), { force: true })

  // 5. 记录版本信息
  const versionInfo: VersionInfo = {
    versionId,
    savedAt: Date.now(),
    savedBy: userId || 'anonymous',
    sessionId,
    snapshotPath,
    fileCount: countFiles(workspacePath),
    note,
  }

  project.versions.push(versionInfo)
  project.updatedAt = Date.now()

  // 6. 清理旧版本（保留最近 MAX_VERSIONS_KEEP 个）
  cleanupOldVersions(project)

  // 7. 保存项目元数据
  fs.writeFileSync(projectJsonPath, JSON.stringify(project, null, 2), 'utf-8')

  // 8. 清理 session
  deleteSession(sessionId)

  return {
    success: true,
    version: versionId,
    savedAt: versionInfo.savedAt,
  }
}
```

### 4.3 版本清理

**位置**：`packages/author-site/src/lib/fs-utils.ts`

```typescript
import { MAX_VERSIONS_KEEP } from '@opencode-workbench/shared'

function cleanupOldVersions(project: Project): void {
  if (project.versions.length <= MAX_VERSIONS_KEEP) return

  // 删除超出保留数量的最旧版本
  const toDelete = project.versions.slice(0, project.versions.length - MAX_VERSIONS_KEEP)
  
  for (const version of toDelete) {
    if (fs.existsSync(version.snapshotPath)) {
      fs.rmSync(version.snapshotPath, { recursive: true, force: true })
    }
  }

  // 保留最近 MAX_VERSIONS_KEEP 个
  project.versions = project.versions.slice(-MAX_VERSIONS_KEEP)
}
```

### 4.4 版本历史查询

**位置**：`packages/author-site/src/lib/fs-utils.ts`（新增函数）

```typescript
export function getVersionHistory(projectId: string): VersionInfo[] {
  const projectPath = getProjectPath(projectId)
  const projectJsonPath = path.join(projectPath, 'project.json')

  if (!fs.existsSync(projectJsonPath)) {
    return []
  }

  const project: Project = JSON.parse(
    fs.readFileSync(projectJsonPath, 'utf-8')
  )

  // 倒序返回：最新的在前面
  return [...project.versions].reverse()
}

export function getLatestVersion(projectId: string): VersionInfo | null {
  const versions = getVersionHistory(projectId)
  return versions.length > 0 ? versions[0] : null
}
```

### 4.5 版本恢复

**位置**：`packages/author-site/src/lib/fs-utils.ts`（新增函数）

```typescript
export function restoreVersion(
  projectId: string,
  versionId: string,
  userId?: string
): { success: boolean; newVersionId?: string; error?: string } {
  const projectPath = getProjectPath(projectId)
  const projectJsonPath = path.join(projectPath, 'project.json')
  const project: Project = JSON.parse(
    fs.readFileSync(projectJsonPath, 'utf-8')
  )

  // 查找目标版本
  const targetVersion = project.versions.find(v => v.versionId === versionId)
  if (!targetVersion) {
    return { success: false, error: `版本 ${versionId} 不存在` }
  }

  // 检查快照是否存在
  if (!fs.existsSync(targetVersion.snapshotPath)) {
    return { success: false, error: `版本快照已丢失: ${versionId}` }
  }

  const workspacePath = path.join(projectPath, 'workspace')

  // 1. 先备份当前 workspace（生成一个新版本）
  const backupVersionId = generateVersionId(project)
  const backupSnapshotPath = getSnapshotPath(projectId, backupVersionId)
  fs.cpSync(workspacePath, backupSnapshotPath, { recursive: true })

  const backupVersion: VersionInfo = {
    versionId: backupVersionId,
    savedAt: Date.now(),
    savedBy: userId || 'system',
    sessionId: `restore-from-${versionId}`,
    snapshotPath: backupSnapshotPath,
    fileCount: countFiles(workspacePath),
    note: `恢复版本前的自动备份 (基于 ${versionId})`,
  }

  project.versions.push(backupVersion)

  // 2. 用目标版本快照覆盖 workspace
  fs.rmSync(workspacePath, { recursive: true, force: true })
  fs.cpSync(targetVersion.snapshotPath, workspacePath, { recursive: true })

  // 3. 记录恢复操作（可选：作为新版本记录）
  const restoreVersionId = generateVersionId(project)
  const restoreSnapshotPath = getSnapshotPath(projectId, restoreVersionId)
  fs.cpSync(workspacePath, restoreSnapshotPath, { recursive: true })

  const restoreVersionInfo: VersionInfo = {
    versionId: restoreVersionId,
    savedAt: Date.now(),
    savedBy: userId || 'system',
    sessionId: `restore-${versionId}`,
    snapshotPath: restoreSnapshotPath,
    fileCount: countFiles(workspacePath),
    note: `恢复到版本 ${versionId}`,
  }

  project.versions.push(restoreVersionInfo)
  project.updatedAt = Date.now()

  // 4. 清理旧版本
  cleanupOldVersions(project)

  // 5. 保存项目元数据
  fs.writeFileSync(projectJsonPath, JSON.stringify(project, null, 2), 'utf-8')

  return { success: true, newVersionId: restoreVersionId }
}
```

### 4.6 Session 创建时记录基于版本

**位置**：`packages/author-site/src/lib/session-manager.ts` 中 `createEditSession`

修改 `.session.json` 写入逻辑：

```typescript
const latestVersion = getLatestVersion(projectId)

const sessionMeta = {
  sessionId,
  userId,
  demoId: projectId,
  status: 'editing' as const,
  basedOnVersion: latestVersion?.versionId || 'v0',  // 新增
  opencodeSessionId: null,
  createdAt: Date.now(),
  expiresAt: Date.now() + SESSION_EXPIRY_MS,
}
```

---

## 五、API 接口变更

### 5.1 新增接口

```
GET  /api/projects/{projectId}/versions          # 查询版本历史
POST /api/projects/{projectId}/versions/restore  # 恢复到指定版本
```

### 5.2 现有接口修改

```
POST /api/sessions/{sessionId}/merge             # 支持可选 note 参数
```

### 5.3 API 路由实现示例

**版本历史查询**：`packages/author-site/src/app/api/projects/[projectId]/versions/route.ts`

```typescript
import { NextRequest } from 'next/server'
import { getVersionHistory } from '@/lib/fs-utils'

export async function GET(
  request: NextRequest,
  { params }: { params: { projectId: string } }
) {
  const versions = getVersionHistory(params.projectId)
  return Response.json({
    success: true,
    projectId: params.projectId,
    currentVersion: versions[0]?.versionId || null,
    versions,
    totalVersions: versions.length,
  })
}
```

---

## 六、前端 UI 变更

### 6.1 新增页面/组件

| 组件 | 功能 |
|:-----|:-----|
| `VersionHistoryPanel` | 版本历史侧边栏 |
| `VersionCard` | 单个版本卡片 |
| `RestoreConfirmDialog` | 恢复确认弹窗 |
| `SaveWithNoteDialog` | 保存时添加备注 |

### 6.2 编辑器页面集成

在编辑器页面（`projects/[id]/edit/page.tsx`）新增：
- 版本历史按钮/侧边栏
- 保存时可选填写备注
- 当前编辑基于的版本提示

---

## 七、数据迁移策略

### 7.1 现有项目处理

已有项目的 `project.json` 中 `versions` 为空数组，升级后：

1. **首次保存时**：当前 workspace 自动成为 `v1`
2. **无需手动迁移**：`saveEditSession` 自动处理空 versions 的情况

### 7.2 Session 元数据兼容

已有 Session 的 `.session.json` 缺少 `status` 和 `basedOnVersion`：

```typescript
// 读取时兼容处理
function parseSessionMeta(raw: any): SessionMeta {
  return {
    ...raw,
    status: raw.status || 'editing',
    basedOnVersion: raw.basedOnVersion || 'v0',
  }
}
```

---

## 八、实现优先级

按依赖关系排序：

| 优先级 | 任务 | 文件 | 依赖 |
|:-----|:-----|:-----|:-----|
| P0 | 扩展 `SessionMeta` 类型 | `packages/shared/src/types.ts` | 无 |
| P0 | 实现 `generateVersionId` | `packages/author-site/src/lib/fs-utils.ts` | 无 |
| P0 | 修改 `saveEditSession` 创建快照 | `packages/author-site/src/lib/session-manager.ts` | P0 |
| P1 | 实现 `cleanupOldVersions` | `packages/author-site/src/lib/fs-utils.ts` | P0 |
| P1 | 实现 `getVersionHistory` | `packages/author-site/src/lib/fs-utils.ts` | P0 |
| P1 | 修改 `createEditSession` 记录 basedOnVersion | `packages/author-site/src/lib/session-manager.ts` | P0 |
| P2 | 实现 `restoreVersion` | `packages/author-site/src/lib/fs-utils.ts` | P1 |
| P2 | 新增 API 路由（版本历史、恢复） | `packages/author-site/src/app/api/...` | P1 |
| P3 | 前端 UI（版本面板、保存备注） | 前端组件 | P2 |

---

## 九、测试要点

1. **保存流程**：保存后检查 snapshots 目录是否生成了对应版本
2. **版本号连续性**：多次保存后版本号是否为 v1, v2, v3...
3. **版本清理**：创建 55 个版本后，最旧的 5 个是否被删除
4. **版本恢复**：恢复 v3 后，workspace 内容是否与 v3 一致
5. **恢复备份**：恢复操作前是否自动创建了当前版本的备份
6. **并发编辑**：多用户同时编辑同一项目，各自 session 是否独立
7. **过期清理**：过期 session 是否被正确清理
8. **元数据一致**：`project.json` 中的 `versions` 与实际 snapshots 是否一致

---

## 十、风险与注意事项

1. **磁盘空间**：每个版本都是完整副本，需监控磁盘使用
2. **大项目**：文件多的项目版本快照会占用较多空间
3. **并发保存**：同时保存可能产生竞态条件（建议加文件锁或队列）
4. **快照丢失**：手动删除 snapshots 目录会导致版本不可恢复（需加校验）
5. **回滚不可撤销**：版本恢复后，当前未保存的编辑会丢失（需提示用户）
