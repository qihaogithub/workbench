# 配置系统图片上传功能方案

> 状态：开发中 / 测试阶段
> 测试项目：`proj_1777302741358`
> 最后更新：2026-04-27（修复预览区图片路径 + 美化上传组件样式）

---

## 一、已完成工作

### 1.1 后端 API

| 文件 | 功能 | 状态 |
|------|------|------|
| `packages/web/src/app/api/sessions/[sessionId]/assets/upload/route.ts` | POST 上传单张图片 | 完成 |
| `packages/web/src/app/api/sessions/[sessionId]/assets/[filename]/route.ts` | GET 获取图片 / DELETE 删除图片 | 完成 |
| `packages/web/src/lib/fs-utils.ts` | Assets 工具函数（路径、保存、删除、列出） | 完成 |
| `packages/shared/src/index.ts` | 新增错误码（INVALID_FILE_TYPE、FILE_TOO_LARGE、UPLOAD_FAILED） | 完成 |

**存储路径：**
```
data/sessions/{userId}/{projectId}/{sessionId}/assets/images/img_{timestamp}_{random}.{ext}
```

**上传限制：**
- 仅允许图片类型：jpeg、png、gif、webp、svg
- 单文件最大 5MB
- 文件名使用 `img_时间戳_随机数.扩展名` 格式，避免冲突

### 1.2 前端组件

| 文件 | 功能 | 状态 |
|------|------|------|
| `packages/web/components/demo/widgets.tsx` | FileUploadWidget：支持 URL 输入 + 本地上传 + 预览 + 删除 | 完成 |
| `packages/web/components/demo/ImageListWidget.tsx` | ImageListWidget：支持 URL 添加 + 本地上传 + 网格预览 + 单图删除 | 完成 |
| `packages/web/components/demo/ConfigForm.tsx` | 通过 `formContext` 传递 `sessionId` | 完成 |
| `packages/web/components/demo/ConfigFormNew.tsx` | 传递 `sessionId` 到 ImageListWidget | 完成 |
| `packages/web/components/demo/types.ts` | `ConfigFormProps` 新增 `sessionId` | 完成 |

### 1.3 Schema 配置方式

单图字段：
```json
{
  "type": "string",
  "title": "横幅图片",
  "ui:widget": "file",
  "ui:options": {
    "accept": "image/*",
    "maxSize": 5242880,
    "placeholder": "输入图片 URL 或上传"
  }
}
```

多图字段：
```json
{
  "type": "array",
  "title": "图片画廊",
  "ui:widget": "imageList",
  "ui:options": {
    "accept": "image/*",
    "maxSize": 2097152,
    "maxItems": 20
  }
}
```

### 1.4 测试项目

项目：`proj_1777302741358`

组件：`packages/web/data/projects/proj_1777302741358/workspace/index.tsx`
Schema：`packages/web/data/projects/proj_1777302741358/workspace/config.schema.json`

包含三个测试字段：
- **banner**（string + file widget）：横幅单图上传
- **avatar**（string + file widget）：头像单图上传
- **gallery**（array + imageList widget）：多图画廊上传

**访问地址：** `http://localhost:3200/demo/proj_1777302741358/edit`

---

## 二、已知问题（待修复）

### 2.1 Session 与 Workspace 不同步（高优先级）

**现象：** 修改 workspace 下的 `config.schema.json` 后，已有 Session 仍然使用旧版本 schema。

**原因：** `createSession()` 通过 `fs.cpSync(workspacePath, sessionPath)` 复制文件，Session 创建后不再与 workspace 同步。

**影响：** 用户刷新页面或重新进入编辑时，如果复用已有 Session，看不到新的 schema 配置（如 `ui:widget: "file"`）。

**临时解决：** 手动修改 Session 目录下的 `config.schema.json`，或丢弃旧 Session 重新创建。

**根本解决：**
- 方案 A：每次加载 Session 时，从 workspace 重新复制 schema 文件（如果 workspace 更新）
- 方案 B：编辑页面在加载时对比 workspace 和 session 的 schema，自动同步

### 2.2 ImageListWidget 删除未清理服务器文件（高优先级）

**现象：** 在 ImageListWidget 中点击删除按钮，仅从前端列表移除图片，服务器上的文件仍然保留。

**原因：** `handleDelete` 只调用了 `onChange(value.filter(...))`，没有调用删除 API。

**影响：** Session 过期后，assets 目录中残留未使用的图片文件。

**修复位置：** `packages/web/components/demo/ImageListWidget.tsx:handleDelete`

**修复方案：** 如果删除的图片 URL 是 `/api/sessions/...` 格式，先调用 DELETE API 删除服务器文件，再更新列表。

### 2.3 FileUploadWidget 替换上传未删除旧文件（中优先级）

**现象：** FileUploadWidget 中上传新图片替换旧图片时，旧图片文件残留在服务器。

**原因：** `onChange(data.data.url)` 直接替换 value，没有清理之前的上传文件。

**修复位置：** `packages/web/components/demo/widgets.tsx:handleFileSelect`

### 2.4 ConfigFormNew array 类型识别过于宽泛（中优先级）

**现象：** ConfigFormNew 中所有 `type: "array"` 字段都渲染为 ImageListWidget，即使不是图片数组。

**原因：** `FieldRenderer` 中判断 `field.type === "array"` 即使用 ImageListWidget，没有其他区分逻辑。

**修复位置：** `packages/web/components/demo/ConfigFormNew.tsx`

**修复方案：** 通过 `ui:widget` 或 `items.properties` 结构来判断是否为图片列表。

### 2.5 上传进度和批量上传（低优先级）

**现象：**
- 上传过程中只显示"上传中..."文字，没有进度百分比
- ImageListWidget 一次只能上传一张图片，不支持多选文件批量上传

**修复方案：**
- 使用 XMLHttpRequest 替代 fetch，通过 `onprogress` 事件获取上传进度
- ImageListWidget 的 `<input type="file">` 添加 `multiple` 属性，支持批量选择

### 2.6 拖拽上传视觉反馈（低优先级）

**现象：** 拖拽图片到上传区域时，没有明显的视觉反馈（如边框高亮、背景变化）。

**修复方案：** 添加 `onDragEnter` / `onDragLeave` 状态，切换上传区域的样式。

### 2.7 图片在 iframe 预览中的路径问题（已修复）

**现象：** 上传后的图片 URL 格式为 `/api/sessions/{sessionId}/assets/{filename}`，iframe 中的预览组件无法直接访问。

**原因：** iframe 使用 Blob URL 加载（`src="blob:..."`），其 origin 为 `null`，相对路径 `/api/sessions/...` 无法在 Blob URL 上下文中正确解析。

**修复位置：** `packages/web/components/demo/PreviewPanel.tsx`

**修复方案：** 在 `sendUpdateCode` 和 `sendUpdateConfig` 发送 configData 到 iframe 之前，通过 `resolveImageUrls()` 函数将相对路径转换为绝对路径（加上 `window.location.origin`）。该函数递归遍历 configData 对象/数组，将所有以 `/api/sessions/` 开头的字符串替换为完整 URL。

### 2.8 配置面板图片上传组件样式不协调（已修复）

**现象：** FileUploadWidget 和 ImageListWidget 中使用了 `border-gray-300` 和 `text-gray-700` 等硬编码颜色，在暗色主题下显得突兀、不协调。

**修复位置：**
- `packages/web/components/demo/widgets.tsx`（FileUploadWidget）
- `packages/web/components/demo/ImageListWidget.tsx`（ImageListWidget）

**修复方案：**
- 输入框：`border-gray-300` → `border-border`，`bg-input`
- 文字：`text-gray-700` → `text-foreground`
- 必填标记：`text-red-500` → `text-destructive`
- 上传区域：添加圆角背景图标容器，增强视觉层次
- 空状态：添加背景色和图标容器，美化整体外观
- 删除按钮：改为半透明背景 + hover 变色，更优雅

---

## 三、下一步工作计划

### 第一阶段：修复核心问题（优先）

1. **修复 Session-Workspace 同步问题**
   - 修改 `getSessionFiles` 或编辑页面加载逻辑，确保 schema 始终从 workspace 同步
   - 或修改 `createSession` 时记录基于的 workspace 版本，加载时检测是否需要更新

2. **修复 ImageListWidget 删除不清理服务器文件**
   - 在 `handleDelete` 中添加服务器文件删除逻辑

3. **修复 FileUploadWidget 替换不删除旧文件**
   - 在上传新文件前，如果当前 value 是上传路径，先调用 DELETE API

### 第二阶段：功能完善

4. **修复 ConfigFormNew array 类型识别**
   - 仅当 `ui:widget === "imageList"` 或字段名/结构匹配图片列表时才使用 ImageListWidget

5. **添加批量上传支持**
   - ImageListWidget 支持一次选择多张图片，逐个上传

6. **添加上传进度显示**
   - FileUploadWidget 和 ImageListWidget 显示上传进度百分比

### 第三阶段：体验优化

7. **优化拖拽上传视觉反馈**
   - 拖拽进入时高亮上传区域

8. **Session 清理时一并删除 assets**
   - `deleteSession` 函数中删除 `assets` 目录

9. **添加图片压缩（可选）**
   - 大图片上传前自动压缩，减少存储和传输

---

## 四、测试检查清单

使用项目 `proj_1777302741358` 验证以下场景：

### FileUploadWidget（banner / avatar）
- [ ] URL 输入方式正常工作
- [ ] 点击选择文件上传成功
- [ ] 拖拽文件上传成功
- [ ] 上传后显示图片预览
- [ ] 删除按钮清除已上传图片
- [ ] 上传超过 5MB 的文件显示错误提示
- [ ] 上传非图片文件显示错误提示
- [ ] 替换上传时旧文件被清理（待修复）

### ImageListWidget（gallery）
- [ ] URL 添加方式正常工作
- [ ] 本地上传单张图片成功
- [ ] 上传后网格显示缩略图
- [ ] 单张删除从列表移除
- [ ] 单张删除同时清理服务器文件（待修复）
- [ ] 达到 maxItems 后禁止添加

### 通用
- [ ] 刷新页面后 Session schema 与 workspace 同步（待修复）
- [ ] Session 过期/丢弃后 assets 目录一并删除（待修复）
- [x] 预览区能正确显示上传的图片（已修复：Blob URL 相对路径 → 绝对路径转换）

---

## 五、相关文件索引

### 后端
- `packages/web/src/app/api/sessions/[sessionId]/assets/upload/route.ts`
- `packages/web/src/app/api/sessions/[sessionId]/assets/[filename]/route.ts`
- `packages/web/src/lib/fs-utils.ts`（assets 工具函数）

### 前端组件
- `packages/web/components/demo/widgets.tsx`（FileUploadWidget）
- `packages/web/components/demo/ImageListWidget.tsx`（ImageListWidget）
- `packages/web/components/demo/ConfigFormNew.tsx`（配置表单）

### 测试项目
- `packages/web/data/projects/proj_1777302741358/workspace/index.tsx`
- `packages/web/data/projects/proj_1777302741358/workspace/config.schema.json`
