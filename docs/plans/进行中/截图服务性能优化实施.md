# 截图服务性能优化实施

## 背景

项目文档 [截图服务性能优化方案](../../项目文档/创作端/04-配置与预览/技术/09_截图服务性能优化方案.md) 已明确当前 screenshot-service 具备 Browser 复用、队列限流、同 hash 合并、编译缓存、hash 文件缓存和 renderBox 校验等基础能力。用户要求开始实施并持续推进全部优化计划。

## 目标

- 先落地高 ROI、低风险优化：性能观测、当前可见页优先、后端显式优先级调度、浏览器预热。
- 保持截图一致性规则：不展示旧图，不让快速截图覆盖 immutable 严格截图产物。
- 在每轮实现后同步测试和项目文档，确保后续 agent 能接着推进。

## 范围

- author-site 截图调度、批量请求、状态轮询和测试。
- demo-ui 画布可见性与截图优先级相关能力。
- screenshot-service 路由、队列、浏览器池、缓存指标和测试。
- `docs/项目文档/创作端/04-配置与预览/技术/09_截图服务性能优化方案.md` 以及必要索引。

## 非目标

- 本轮不优先实现 page 池。page 池属于高风险状态复用能力，需在指标证明 `newPage` 是主要瓶颈后再推进。
- 不把浏览器扩展作为主链路优化。
- 不整理当前工作区里与截图优化无关的既有 dirty changes。

## 方案

1. 先检查当前源码，确认已有改动是否已覆盖部分优化。
2. 实施 P0 指标：批量状态携带阶段耗时、缓存命中和优先级完成情况；日志补充缓存和 in-flight 合并信息。
3. 实施 P1 调度：请求字段增加 `priority`，服务端按显式优先级排序；前端在批量生成时按当前页面、可见页、近邻页、后台页排序。
4. 实施低风险浏览器预热：配置开关控制服务启动后预热 Browser，保持默认行为兼容。
5. 补测试覆盖优先级排序、指标字段、旧接口兼容。
6. 更新项目文档和计划进度。

## 任务清单

- [x] 建立实施计划文档。
- [x] 核对当前 screenshot-service、author-site、demo-ui 代码现状。
- [x] 实施后端批量优先级与性能指标扩展。
- [x] 实施前端批量请求排序与 priority 透传。
- [x] 实施浏览器预热配置和启动路径。
- [x] 补充测试。
- [x] 更新项目文档。
- [x] 运行验证命令并记录结果。
- [x] 实施缓存身份审计与 fast/strict 产物分层。
- [x] 实施 measuredHeight hint。
- [ ] 评估 iframe shell 缓存和 page/context 池是否具备继续实现收益。

## 进度记录

- 2026-06-29：创建实施文档。当前工作区存在大量与本任务无关的既有 dirty changes，本任务只触碰截图优化相关文件。
- 2026-06-29：完成首轮 P0/P1 实施。screenshot-service 支持批量 priority、批量 metrics、BrowserPool priority 队列和可选 Browser 预热；author-site/demo-ui 支持可见页回传、批量请求排序和 priority 透传。
- 2026-06-29：由于当前运行时 `pnpm` 版本为 11，仓库要求 pnpm 8，直接运行 `pnpm --filter @opencode-workbench/screenshot-service test` 触发 lockfile 不兼容和联网安装失败；改用包内已安装的 `tsc`、`vitest`、`jest` 可执行文件完成验证。
- 2026-06-29：完成 fast/strict 产物分层。strict 继续写 `pageId.hash.png`，fast 写 `pageId.hash.fast.png` 并通过 `variant=fast` URL 读取，不覆盖 strict immutable 文件。active/visible 页面请求 fast，其余页面请求 strict。
- 2026-06-29：完成缓存身份审计落地。截图 hash 纳入 session/resource scope、Preview Runtime 来源、runtime base URL 和 CDN base URL。截图请求支持 `measuredHeight`，fast 路径把它作为 fullPage 高度提示。

## 验证方式

- `pnpm check:screenshot`
- `pnpm check:author`
- 必要时补充目标测试文件的包级命令。

已执行：

- `packages/screenshot-service/node_modules/.bin/tsc --noEmit -p packages/screenshot-service/tsconfig.json`：通过。
- `node_modules/.bin/vitest run --config vitest.config.ts`（在 `packages/screenshot-service/` 下执行）：通过，2 个测试文件、12 个用例。
- `packages/author-site/node_modules/.bin/tsc --noEmit -p packages/author-site/tsconfig.json`：通过。
- `node_modules/.bin/jest src/components/demo/useScreenshotGeneration.test.tsx src/components/demo/preview-canvas-interaction-mode.test.tsx --runInBand`（在 `packages/author-site/` 下执行）：通过，2 个测试文件、39 个用例。

## 风险与待确认事项

- 相关源码文件已经有未提交改动，实施时必须基于当前内容增量修改，不能回滚他人改动。
- 如果现有测试基线已经失败，需要区分本任务引入的问题与既有失败。
- “全部优化计划”范围很大，page 池和 fast/strict variant 需要在前置指标与接口模型稳定后继续分阶段推进。
- 尚未实现 iframe shell 缓存和 page/context 池。二者需要基于 `metrics.totalRenderMs`、`totalQueueWaitMs`、冷启动耗时和错误率判断收益；若 `newPage` 或 HTML 拼装不是主要瓶颈，应保持当前低风险实现。
