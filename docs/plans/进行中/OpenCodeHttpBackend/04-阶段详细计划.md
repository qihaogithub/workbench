# OpenCodeHttpBackend — 阶段详细计划

> 本文档包含：三阶段详细内容、技术实现细节、验收标准
>
> 关联文档：[00-总览.md](./00-总览.md) | [01-方案基础.md](./01-方案基础.md) | [02-API设计.md](./02-API设计.md) | [03-部署与集成.md](./03-部署与集成.md) | [05-当前进度.md](./05-当前进度.md)

---

## 一、Phase 1：基础实现

**目标**：完成 HTTP 后端注册，确保与 ACP 后端并行工作
**周期**：1-2 人日
**当前状态**：部分完成

### 1.1 任务清单

| 任务 | 优先级 | 状态 | 代码位置 | 验收标准 |
|:---|:---|:---|:---|:---|
| 注册 opencode-http 后端 | P0 | ⏳ 待完成 | `server.ts:60` | 后端可通过 `backend: "opencode-http"` 正常创建 |
| 修复 model 硬编码 | P0 | ⏳ 待完成 | `agent.ts:98` | 消息发送时使用配置中的 model 而非硬编码值 |
| 同步 opencodeSessionId 到 SessionMeta | P0 | ⏳ 待完成 | `backend-agent.ts` 或 `agent.ts` | SessionMeta 中可正确读取 opencodeSessionId |
| 验证 HTTP 和 ACP 并行工作 | P0 | ⏳ 待完成 | — | 两种后端可独立创建会话并正常收发消息 |
| 完善 OpenCodeHttpBackend 基础功能 | P0 | ✅ 已完成 | `opencode-http.ts:1-150` | 同步消息、会话管理、健康检查 |
| 支持通过 backend 参数切换 | P0 | ✅ 已完成 | `agent-client.ts:54-76` | 前端传入 backend 字段生效 |

### 1.2 实施步骤

#### 步骤 1：注册 opencode-http 后端

```typescript
// server.ts 中添加 import
import { OpenCodeHttpBackend } from './backends/opencode-http';

// 在 factory.register 调用处添加
factory.register('opencode-http', (agentConfig) =>
  new BackendAgent(agentConfig, new OpenCodeHttpBackend(agentConfig)));

// 保留 ACP 后端（兼容）
factory.register('opencode', (agentConfig) =>
  new BackendAgent(agentConfig, new OpenCodeAcpBackend(agentConfig)));
```

#### 步骤 2：修复 model 硬编码

> **问题**：`agent.ts:93-99` 中 `model` 硬编码为 `sensenova/deepseek-v4-flash`

```typescript
// 修改前（agent.ts:98）
model: 'sensenova/deepseek-v4-flash',

// 修改后：从 config.model 读取（需确认 config.model 来源）
model: this.config.model || 'sensenova/deepseek-v4-flash',  // 提供默认值保证向后兼容
```

#### 步骤 3：同步 opencodeSessionId

> **问题**：`opencode-http.ts:65` 创建 OpenCode 会话后，`sessionId` 仅存在后端实例中，未同步到 `SessionMeta`

```typescript
// 在 backend-agent.ts 或 agent.ts 中，初始化完成后
if (this.backend.name === 'opencode-http') {
  const ocSessionId = this.backend.getCurrentSessionId();
  if (ocSessionId) {
    sessionStore.update(this.config.sessionId, { opencodeSessionId: ocSessionId });
  }
}
```

#### 步骤 4：验证双轨并行

1. 启动 OpenCode Server（`opencode serve --port 4096`）
2. 启动 agent-service
3. 使用 `backend: "opencode"` 创建会话并发送消息（验证 ACP 正常）
4. 使用 `backend: "opencode-http"` 创建会话并发送消息（验证 HTTP 正常）
5. 确认两种方式的消息收发互不影响

---

## 二、Phase 2：功能完善

**目标**：实现 SSE 流式响应、文件变更追踪、权限审批流程
**周期**：3-4 人日
**当前状态**：全部待实现

### 2.1 任务清单

| 任务 | 优先级 | 状态 | 依赖 | 验收标准 |
|:---|:---|:---|:---|:---|
| 实现 SSE 流式响应 | P1 | ⏳ 待完成 | OpenCode Server 运行中 | 消息通过 SSE 实时推送至前端 |
| 实现文件变更追踪 | P1 | ⏳ 待完成 | SSE 流式响应 | 文件操作后前端可感知变更 |
| 实现权限审批流程 | P1 | ⏳ 待完成 | SSE 流式响应 | 权限请求通过 WebSocket 推送至前端 |
| 前端模型选择 UI | P1 | ⏳ 待完成 | 4.1.3 API 实现 | 用户可选择系统配置的模型 |

### 2.2 实施步骤

#### 步骤 1：实现 SSE 流式响应

**核心逻辑**：

```typescript
// opencode-http.ts 中 connectSSE() 方法实现
private connectSSE(sessionId: string): void {
  const eventSource = new EventSource(`${OPENCODE_SERVER_URL}/event`);

  eventSource.onmessage = (event) => {
    const data = JSON.parse(event.data);

    switch (data.type) {
      case 'agent_message_chunk':
        this.eventCallback?.({
          type: 'stream',
          sessionId: this.config.sessionId,
          content: data.content?.text || '',
          done: false,
        });
        break;
      case 'tool_call':
        this.eventCallback?.({
          type: 'tool_call',
          sessionId: this.config.sessionId,
          toolCallId: data.toolCallId,
          title: data.title,
          kind: data.kind,
          status: 'pending',
        });
        break;
      case 'agent_message_done':
        this.eventCallback?.({
          type: 'stream',
          sessionId: this.config.sessionId,
          content: '',
          done: true,
        });
        eventSource.close();
        break;
    }
  };

  eventSource.onerror = () => {
    eventSource.close();
  };
}
```

**sendMessage 改造**：

```typescript
async sendMessage(content: string, options?: { stream?: boolean }): Promise<string> {
  if (options?.stream) {
    // 异步发送
    await fetch(`${OPENCODE_SERVER_URL}/session/${this.sessionId}/prompt_async`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ parts: [{ type: 'text', text: content }] }),
    });

    // 连接 SSE 接收流式响应
    this.connectSSE(this.sessionId);
    return '';  // 流式响应通过 eventCallback 推送
  } else {
    // 同步发送（已实现）
    // ...
  }
}
```

#### 步骤 2：实现文件变更追踪

参考 `opencode-acp.ts:138-187` 中的文件操作通知处理，从 SSE 事件中解析文件变更，调用 `snapshotService` 同步。

#### 步骤 3：实现权限审批流程

对接 `/session/:id/permissions/:permissionID` 端点，将权限请求通过 WebSocket 推送至前端。

#### 步骤 4：前端模型选择 UI

实现 `GET /api/llm/models` API，前端添加模型选择下拉框。

---

## 三、Phase 3：优化与稳定

**目标**：HTTP 设为默认、ACP 标记 deprecated、完善测试
**周期**：持续
**当前状态**：待开始

### 3.1 任务清单

| 任务 | 优先级 | 状态 | 验收标准 |
|:---|:---|:---|:---|
| HTTP 设为默认后端 | P2 | ⏳ 待完成 | 新会话默认使用 `opencode-http` |
| ACP 后端标记 deprecated | P2 | ⏳ 待完成 | 文档中明确标记 ACP 为兼容备选 |
| 完善测试和文档 | P2 | ⏳ 待完成 | 覆盖两种后端的测试用例 |

### 3.2 实施步骤

#### 步骤 1：切换默认后端

```typescript
// agent.ts:95
// 修改前
const backendName = body.backend || 'opencode';

// 修改后
const backendName = body.backend || 'opencode-http';
```

#### 步骤 2：文档标记

在 `00-总览.md` 和相关文档中明确标注 ACP 后端为 deprecated（仅兼容，不删除）。

---

## 四、技术实现细节

### 4.1 OpenCodeHttpBackend 核心逻辑

> **当前实现**：`opencode-http.ts:1-150`（已存在，需完善）

```typescript
// 当前实现（opencode-http.ts）
export class OpenCodeHttpBackend implements IBackendAdapter {
  readonly name = 'opencode-http';
  private config: AgentConfig;
  private status: BackendStatus = 'idle';
  private eventCallback?: (event: AgentEvent) => void;
  private sessionId: string | null = null;
  private fullContent = '';

  constructor(config: AgentConfig) {
    this.config = config;
  }

  async initialize(): Promise<void> {
    // ✅ 已实现（opencode-http.ts:19-36）
    // 包含防重复初始化、错误处理
    await this.createSession();
    this.status = 'ready';
  }

  private async createSession(): Promise<void> {
    // ✅ 已实现（opencode-http.ts:48-67）
    const response = await fetch(`${OPENCODE_SERVER_URL}/session`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        title: this.config.sessionId || `session-${Date.now()}`,
        model: this.config.model,  // 用户选择的模型
        workingDir: this.config.workingDir,
      }),
      signal: AbortSignal.timeout(10000),
    });

    const data = await response.json();
    this.sessionId = data.id;
  }

  async sendMessage(content: string, options?: { stream?: boolean }): Promise<string> {
    // ✅ 同步消息已实现（opencode-http.ts:69-118）
    // ⏳ 异步 + SSE 待实现（Phase 2）

    if (options?.stream) {
      // 待实现：异步发送 + SSE 接收
      await fetch(`${OPENCODE_SERVER_URL}/session/${this.sessionId}/prompt_async`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ parts: [{ type: 'text", text: content }] }),
      });

      // 连接 SSE 接收流式响应
      this.connectSSE();
      return '';  // 流式响应通过 eventCallback 推送
    } else {
      // 同步发送（已实现）
      const response = await fetch(`${OPENCODE_SERVER_URL}/session/${this.sessionId}/message`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ parts: [{ type: 'text", text: content }] }),
        signal: AbortSignal.timeout(this.config.timeout || 120000),
      });

      const data = await response.json();
      const textParts = data.parts?.filter((p: any) => p.type === 'text') || [];
      this.fullContent = textParts.map((p: any) => p.text).join('');

      // 模拟流式事件（当前实现，opencode-http.ts:102-109）
      if (this.eventCallback && this.fullContent) {
        this.eventCallback({
          type: 'stream',
          sessionId: this.config.sessionId,
          content: this.fullContent,
          done: true,
        });
      }

      return this.fullContent;
    }
  }

  private connectSSE(): void {
    // ⏳ 待实现（Phase 2）
    // 参见 Phase 2 实施步骤
  }

  // ✅ 已实现方法：
  // - start()（opencode-http.ts:38-46）
  // - onStream()（opencode-http.ts:120-122）
  // - getStatus()（opencode-http.ts:124-126）
  // - destroy()（opencode-http.ts:128-133）
  // - checkHealth()（opencode-http.ts:135-145）
  // - getCurrentSessionId()（opencode-http.ts:147-149）
}
```

> **待实现方法**：
> - `setModel()` - 模型切换
> - `getModelInfo()` - 获取模型信息
> - `getFiles()` - 获取文件变更列表
> - `setPromptTimeout()` - 设置超时
> - `cancelPrompt()` - 取消提示
> - `getWorkingDir()` - 获取工作目录

### 4.2 SessionStore 扩展

> **当前实现**：`session-store.ts:4-18`

```typescript
// 当前实现（session-store.ts:4-18）
export interface SessionMeta {
  sessionId: string;
  demoId?: string;
  backend: string;              // "opencode" | "opencode-http" | ...
  workingDir: string;
  customWorkspace: boolean;
  workspaceType: 'user' | 'temp';
  snapshotMode: 'git-repo' | 'snapshot' | null;
  snapshotBranch: string | null;
  status: AgentStatus;
  createdAt: number;
  updatedAt: number;
  messageCount: number;
  opencodeSessionId?: string;   // ✅ 已存在（HTTP 后端使用）
}
```

> **待补充**：
> 1. `opencodeSessionId` 同步逻辑：在 `backend-agent.ts` 或 `agent.ts` 中，当 `OpenCodeHttpBackend` 创建会话后，将 `sessionId` 同步到 `SessionMeta`
> 2. 可选：添加 `modelName?: string` 字段记录用户选择的模型
