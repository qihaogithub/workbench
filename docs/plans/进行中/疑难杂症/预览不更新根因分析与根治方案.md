# 预览不更新根治方案 — 实施指南

> AI 编辑代码后，预览区**闪烁后回退到旧内容**。

## 根因（3 个，缺一不可）

### A. 数据优先级颠倒（use-chat-stream.ts:333-342）

`finalFiles` 优先使用 `result.files`（后端缓存，可能过时），`realtimeFilesRef`（实时流，最权威）只是后备。当 `result.files` 包含旧版本代码时，路径 B 用旧值覆盖路径 A 已写入的新值。

### B. onFinish 中 processRealtimeFiles 冗余调用（use-chat-stream.ts:326-330）

路径 A 在 onFinish 中再次触发，与路径 B（extractCodeAndSchemaUpdates）产生数据竞争。

### C. applyDemoSnapshot 闭包陷阱（page.tsx:227,238-240,252）

`setEditorContent` 中 `newCode ?? code` / `newSchema ?? schema` 的 `code`/`schema` 是闭包捕获的旧值；`mergeConfigWithUserValues` 的第三个参数 `schema` 也是闭包值。在同批次连续调用中 fallback 到过期状态。

---

## 方案 R-A：实时流优先 + 去重写入

**文件：** `packages/author-site/src/components/ai-elements/chat/hooks/use-chat-stream.ts`

### 改动 1：新增 import

```typescript
import {
  processFileChanges,
  extractCodeAndSchemaUpdates,
  isCodeFile,       // ← 新增
  isSchemaFile,     // ← 新增
  normalizePath,     // ← 新增
  type FileChangeEntry,
} from "../utils/chat-file-utils";
```

> 需确认 `chat-file-utils.ts` 中 `isCodeFile`、`isSchemaFile`、`normalizePath` 已 export。若无，需添加 export。

### 改动 2：重写 onFinish 回调（替换第 295-395 行）

```typescript
onFinish: async (result) => {
    streamService.stopKeepalive();
    stopSilenceTracking();
    const currentMsg = currentMessageRef.current;
    const assistantMessage: ChatMessage = {
        id: `assistant-${Date.now()}`,
        role: "assistant",
        content:
            accumulatedContent ||
            result.content ||
            "抱歉，我没有收到有效的回复。",
        parts: currentMsg.parts,
    };

    const updatedMessages = [...messagesRef.current, assistantMessage];
    setMessages(updatedMessages);
    setCurrentMessage({
        role: "assistant",
        content: "",
        parts: [],
    });
    setStreamContent("");

    await persistMessages(sessionId, updatedMessages);
    await updateSessionTitle(
        sessionId,
        userMessage,
        messagesRef.current.length === 0,
    );

    // ── 1. 清除 pending timer（不调用 processRealtimeFiles，避免路径 A 重复触发） ──
    if (fileUpdateTimer) {
        clearTimeout(fileUpdateTimer);
        fileUpdateTimer = null;
    }

    // ── 2. 检测实时流已更新的数据类型 ──
    let realtimeUpdatedCode = false;
    let realtimeUpdatedSchema = false;
    for (const [path, info] of realtimeFilesRef.entries()) {
        const normalizedPath = normalizePath(path);
        if (isCodeFile(normalizedPath) && info.content) realtimeUpdatedCode = true;
        if (isSchemaFile(normalizedPath) && info.content) realtimeUpdatedSchema = true;
    }

    // ── 3. Flush 实时流文件到 UI（保持流式阶段的预览体验） ──
    const pendingRealtimeFiles = Array.from(realtimeFilesRef.entries()).map(
        ([path, info]) => ({
            path,
            action: info.action as "created" | "modified" | "deleted",
            content: info.content,
        }),
    );
    if (pendingRealtimeFiles.length > 0) {
        processFileChanges(pendingRealtimeFiles, {
            onCodeUpdate,
            onSchemaUpdate,
            onFilesChange,
        });
    }

    // ── 4. 构建最终文件列表：realtimeFilesRef 优先，result.files 仅补充缺失文件 ──
    const realtimeFileMap = new Map<string, FileChangeEntry>();
    for (const [path, info] of realtimeFilesRef.entries()) {
        realtimeFileMap.set(path, {
            path,
            action: info.action as "created" | "modified" | "deleted",
            content: info.content,
        });
    }
    if (result.files && result.files.length > 0) {
        for (const f of result.files) {
            if (!realtimeFileMap.has(f.path)) {
                realtimeFileMap.set(f.path, f);
            }
        }
    }
    const finalFiles = Array.from(realtimeFileMap.values());

    if (finalFiles.length > 0) {
        for (const f of finalFiles) {
            if (f.path && f.path.endsWith(".md")) {
                memoryFilePathsRef.current.add(f.path);
            }
        }
        onFilesChange?.(finalFiles);
    }

    setIsStreaming(false);

    // ── 5. 从 finalFiles 提取更新，但跳过实时流已更新的数据类型（避免旧值覆盖新值） ──
    const { codeUpdated, schemaUpdated } =
        finalFiles.length > 0
            ? extractCodeAndSchemaUpdates(finalFiles, {
                onCodeUpdate: realtimeUpdatedCode
                    ? undefined
                    : (code) => onCodeUpdate?.(code, "ai-finish"),
                onSchemaUpdate: realtimeUpdatedSchema
                    ? undefined
                    : (schema) => onSchemaUpdate?.(schema, "ai-finish"),
            })
            : { codeUpdated: false, schemaUpdated: false };

    // ── 6. HTTP 兜底：仅在完全没有数据更新时触发 ──
    const effectiveCodeUpdated = realtimeUpdatedCode || codeUpdated;
    const effectiveSchemaUpdated = realtimeUpdatedSchema || schemaUpdated;

    if (!effectiveCodeUpdated && !effectiveSchemaUpdated) {
        const filesData = await fetchSessionFiles(sessionId, demoId);
        if (filesData) {
            const { code, schema } = filesData;
            if (code) onCodeUpdate?.(code, "ai-finish");
            if (schema) onSchemaUpdate?.(schema, "ai-finish");

            const fetchedFiles: FileChangeEntry[] = [];
            if (code)
                fetchedFiles.push({
                    path: "index.tsx",
                    action: "modified",
                    content: code,
                });
            if (schema)
                fetchedFiles.push({
                    path: "config.schema.json",
                    action: "modified",
                    content: schema,
                });
            if (fetchedFiles.length > 0) onFilesChange?.(fetchedFiles);
        }
    }

    onSnapshotReady?.();
    realtimeFilesRef.clear();
},
```

### 改动核心逻辑说明

| 原代码 | 改后 | 原因 |
|--------|------|------|
| `finalFiles` 优先用 `result.files` | `realtimeFilesRef` 优先，`result.files` 仅补充 | 实时流数据最权威，避免后端缓存旧值覆盖 |
| `processRealtimeFiles()` 在 onFinish 中调用 | 改用 `processFileChanges()` 直接处理（谨慎调用 onCodeUpdate/onSchemaUpdate） | 避免路径 A 二次触发与路径 B 竞争 |
| `extractCodeAndSchemaUpdates` 无条件调用 onCodeUpdate | 实时流已更新的数据类型跳过 onCodeUpdate | 避免旧值覆盖新值 |
| HTTP 兜底条件 `!codeUpdated && !schemaUpdated` | 增加 `effectiveCodeUpdated` / `effectiveSchemaUpdated` | 将实时流更新纳入判断 |

---

## 方案 R-B：修复 applyDemoSnapshot 闭包陷阱

**文件：** `packages/author-site/src/app/demo/[id]/edit/page.tsx`

### 改动 1：添加 codeRef 和 schemaRef（约第 100 行 state 声明区域之后）

```typescript
const codeRef = useRef(code);
codeRef.current = code;
const schemaRef = useRef(schema);
schemaRef.current = schema;
```

### 改动 2：修改 applyDemoSnapshot（第 201-253 行）

```typescript
const applyDemoSnapshot = useCallback(
    (params: {
        code?: string;
        schema?: string;
        source: "ai-realtime" | "ai-finish" | "manual-load" | "page-switch";
    }) => {
        const { code: newCode, schema: newSchema, source } = params;

        if (newCode !== undefined) {
            setCode((prev) => (prev === newCode ? prev : newCode));
            codeRef.current = newCode;
            if (sessionId && activeDemoId) {
                invalidateCompileCache(sessionId, activeDemoId);
            }
        }

        if (newSchema !== undefined) {
            setSchema(newSchema);
            schemaRef.current = newSchema;
            setPreviewSize(getPreviewSize(newSchema));

            try {
                setConfigDataMap((prev) => {
                    const current = prev[activeDemoIdRef.current] ?? {};
                    const merged = mergeConfigWithUserValues(
                        current,
                        newSchema,
                        schemaRef.current,
                    );
                    return { ...prev, [activeDemoIdRef.current]: merged };
                });
            } catch (e) {
                console.warn("[DemoEditPage] Failed to merge schema defaults:", e);
            }
        }

        setEditorContent((prev) => {
            const currentCode = newCode ?? extractCodeFromFigma(prev) ?? codeRef.current;
            const currentSchema = newSchema ?? extractSchemaFromFigma(prev) ?? schemaRef.current;
            return buildFigmaText(currentCode, currentSchema);
        });

        if (source === "ai-realtime" || source === "ai-finish") {
            if (schemaRegenerateTimerRef.current) {
                clearTimeout(schemaRegenerateTimerRef.current);
                schemaRegenerateTimerRef.current = null;
            }
        }
    },
    [sessionId, activeDemoId],
);
```

**关键改动点：**
1. `setEditorContent` 中 `code` → `codeRef.current`，`schema` → `schemaRef.current`
2. `mergeConfigWithUserValues` 第三个参数 `schema` → `schemaRef.current`
3. `newCode`/`newSchema` 更新时同步更新 ref（`codeRef.current = newCode`，`schemaRef.current = newSchema`）
4. `useCallback` deps 从 `[code, schema, sessionId, activeDemoId]` 改为 `[sessionId, activeDemoId]`

---

## 方案 R-C：移除冗余 invalidateCompileCache

**文件：** `packages/author-site/src/app/demo/[id]/edit/page.tsx`（约第 976 行）

```typescript
// 改前：
onSnapshotReady={() => {
    invalidateCompileCache(sessionId, activeDemoId);
}}

// 改后：
onSnapshotReady={() => {
    // 已在 applyDemoSnapshot 中调用 invalidateCompileCache，此处无需重复
}}
```

> 如 `onSnapshotReady` 无其他用途，可直接移除 prop。

---

## 实施顺序

R-A 和 R-B **可并行实施**，无依赖关系。R-C 可在任何时候实施。

```
R-A（use-chat-stream.ts onFinish 重写）  ─┐
R-B（page.tsx 闭包修复）                   ─┼─  可并行
R-C（page.tsx 冗余清理）                   ─┘
```

---

## 涉及文件

| 文件 | 方案 | 改动 |
|------|------|------|
| `packages/author-site/src/components/ai-elements/chat/hooks/use-chat-stream.ts` | R-A | 重写 onFinish（~100 行） |
| `packages/author-site/src/app/demo/[id]/edit/page.tsx` | R-B + R-C | 新增 2 ref，修改 applyDemoSnapshot，移除 onSnapshotReady 中的 invalidate |
| `packages/author-site/src/components/ai-elements/chat/utils/chat-file-utils.ts` | R-A | 确认 `isCodeFile`/`isSchemaFile`/`normalizePath` 已 export |

---

## 验证清单

| # | 场景 | 预期结果 | 验证方法 |
|---|------|----------|----------|
| 1 | AI 修改代码 | 预览立即显示新内容，不闪烁不回退 | 观察 AI 完成后预览区 |
| 2 | AI 修改 schema | 预览更新样式，ConfigForm 保持用户填值 | 修改配置后让 AI 改 schema |
| 3 | AI 同时修改 code+schema | 两者同步更新 | 观察网络面板，只触发一次编译 |
| 4 | AI 完成后 `result.files` 含旧代码 | 实时流版本优先，不回退 | 后端调试：让 result.files 返回旧版本 |
| 5 | 代码Tab 编辑保存 | 预览立即更新 | 手动编辑 index.tsx 保存 |
| 6 | 页面切换 | 新页面正确显示 | AI 运行时切换 demo 页面 |
| 7 | editorContent 一致性 | Figma 编辑器面板内容与预览一致 | AI 完成后检查编辑器代码 |
| 8 | ConfigForm 合并 | AI 改 schema 后用户已填值不丢失 | 填好配置 → 让 AI 改 schema → 检查配置值 |
| 9 | HTTP 兜底 | 无实时流也无 result.files 时正确获取 | 模拟空 result.files 场景 |