# 底部弹窗配置化还原

## 背景

用户希望 `proj_1779608460372` 项目呈现为参考截图中的底部弹窗效果。参考来源是 `E:\重要文件\Programming\1_Work\uiweb-vue` 项目的 `bottom-pop-up` 页面。

## 目标

- 还原手机与平板两个页面的底部弹窗视觉效果。
- 将弹窗配图和按钮文案作为项目配置项。
- 保持改动局限在目标项目数据与必要文档范围内。

## 范围

- 目标项目：`data/projects/proj_1779608460372/`
- 参考页面：`uiweb-vue/src/components/bottom-pop-up/`
- 配置入口：目标项目工作区内的配置 schema 与 demo 页面配置读取逻辑。

## 方案

1. 对比参考 Vue 页面与目标项目两个 demo 页面现状。
2. 更新目标项目配置 schema，暴露弹窗配图与按钮文案。
3. 更新手机、平板 demo 页面，使其读取配置并呈现底部弹窗。
4. 补充或更新长期项目文档，说明配置化弹窗项目的约束。
5. 运行与数据项目改动匹配的静态检查或结构校验。

## 任务清单

- [x] 读取参考页与目标项目文件，确认实现边界。
- [x] 更新配置 schema 和 demo 页面。
- [x] 更新相关项目文档。
- [x] 执行验证并记录结果。

## 进度记录

- 2026-06-28：开始调查目标项目 `proj_1779608460372` 和参考 `bottom-pop-up` 页面。
- 2026-06-28：确认参考页默认隐藏标题区，目标项目已有配图配置；将两个按钮文案迁移到项目级共享配置，页面级 schema 仅保留预览尺寸，避免项目级与页面级字段冲突。
- 2026-06-28：更新手机和平板 demo 页面，按参考图隐藏标题区，并从项目级配置读取弹窗配图、主按钮文案和次按钮文案。
- 2026-06-28：完成轻量验证：目标项目三个 JSON schema 可解析且无项目级/页面级字段冲突，两个 demo TSX 可转译，文档文件存在且非空。`corepack pnpm check:author` 受既有共享 demo 文件缺失影响失败，具体为 `packages/author-site/src/components/demo/*.test.*` 引用 `../../../../shared/src/demo/...` 无法解析；本地 viewer/API 路由在 5 秒内未返回，未完成浏览器截图验收。
- 2026-06-29：修复预览区白屏。根因是两个 demo 根容器依赖 `h-full`，但 iframe 内 `#root` 只有 `min-height: 100vh`，没有可继承的显式 `height`，导致根容器和绝对定位遮罩高度为 0。已改为根容器内联 `height: '100vh'`。
- 2026-06-29：完成浏览器验证：手机页 iframe 组件尺寸为 375×812，平板页 iframe 组件尺寸为 1024×768，弹窗图片加载完成，按钮文案可见，未发现 runtime error。

## 最终状态

已完成本次目标项目改动和白屏修复。手机和平板页面都按参考图隐藏标题文字区，保留遮罩、弹窗配图、黄色主按钮和次按钮；弹窗配图、主按钮文案、次按钮文案统一放在项目级共享配置中。

## 验证方式

- 检查 JSON 配置文件可解析。
- 检查两个 demo 页面可读取配置项并有默认值。
- 如改动触及平台代码，运行对应 `check:*` 命令；若仅改项目数据，执行针对目标文件的轻量校验。

## 风险与待确认事项

- 参考项目资源较大，目标项目需决定是否复制资源到项目工作区，或引用现有公开资源路径。
- 截图中的背景首页元素可能属于视觉衬底，核心验收以弹窗本体、遮罩、配图和文案配置化为准。
