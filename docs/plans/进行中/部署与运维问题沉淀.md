# 部署与运维问题沉淀

> 按问题条目追加/更新，只保留当前仍有价值的结论、待验证项和后续动作。事实类信息以 `docs/用户指南/正式环境部署指南.md` 为准，本文件只沉淀过程结论与风险。

## 正式环境迁移到 macOS 主机 + 密码登录 + 本机 compose 部署（2026-07-17）

- **现象/背景**：正式环境从 `root@10.130.33.131`（SSH 私钥）迁移到 `jojo@10.131.75.39`（密码 `123456`）。`10.131.75.39` 是一台 **Mac**，代码已在其本机 `/Users/jojo/Documents/workbench`。最终采用的部署方式是：SSH 登录该机后**在本机用 `docker compose` 构建并启动**（方式 A），不再走「开发机 rsync 推送」（方式 B，保留但非主路径）。
- **改动范围**：
  - 认证：`scripts/deploy.sh`、`deploy-author-with-data.sh`、`sync-production-data-to-local.sh` 默认服务器改为 `jojo@10.131.75.39`，新增 `SSH_PASSWORD`（默认 `123456`）；三脚本统一 `SSH_CMD`/`RSYNC_RSH` 封装 ssh/rsync，设了密码走 `sshpass`、置空回退 `-i ${SSH_KEY}`。相关变量均可环境变量覆盖。
  - 路径适配 Mac：`.env.docker` 的 `APP_DATA_DIR` → `/Users/jojo/Documents/workbench/data`（Docker Desktop/OrbStack 默认只共享 `/Users`，不共享 `/opt`）；三脚本 `REMOTE_DIR` → `/Users/jojo/Documents/workbench`，`deploy-author-with-data.sh` 的备份/暂存目录 → `/Users/jojo/workbench-data-{backups,staging}`。
  - 截图服务默认开启：`docker-compose.yml` 移除 `screenshot-service` 的 `profiles: [screenshot]`，并移除强制 `platform: linux/amd64`（改为跟随宿主机原生平台，Apple 芯片下 arm64，避免 amd64 模拟拖慢/崩溃；Dockerfile 用 `apt-get install chromium` + `FROM --platform=$TARGETPLATFORM`，原生可装）。`deploy.sh` 默认 `DEPLOY_SERVICES` 含 `screenshot-service`、`INCLUDE_SCREENSHOT_SERVICE=true`。
  - 浏览器 URL：`.env.docker` 的 `NEXT_PUBLIC_*`、`CORS_ORIGINS`、`FIGMA_OAUTH_REDIRECT_URI` → `10.131.75.39`（NEXT_PUBLIC_* 为构建期变量，必须同步）。
  - 文档：`docs/用户指南/正式环境部署指南.md` 增加「方式 A 本机 compose 部署（推荐）」，同步数据目录/截图默认/访问地址。`deploy-fast.sh` 未改（透传继承新默认）。
- **本机部署命令（方式 A）**：`cd /Users/jojo/Documents/workbench && git pull && mkdir -p /Users/jojo/Documents/workbench/data && docker compose --env-file .env.docker up -d --build`（`up -d --build` 不带服务名即启动全部四个含截图）。
- **验证状态**：
  - 已完成：四脚本 `bash -n` 通过；`docker-compose.yml` 经 `python yaml` 解析，截图服务已在默认服务集、无 `profiles`/`platform`；无旧 IP/内联 `ssh -i` 残留。
  - 未完成：本仓所在开发机无 `docker`、也连不通该内网，无法跑 `docker compose config`/真实构建。需在 `10.131.75.39` 上实跑 `up -d --build` 验证四服务健康（尤其截图服务在该机架构下的 Chromium 启动）。
- **后续/风险**：
  - 该 Mac 需运行 Docker Desktop / OrbStack；首次前 `mkdir -p` 数据目录。
  - 若为 Apple Silicon，截图服务原生 arm64 构建；如某处仍需 amd64 镜像，可 `SCREENSHOT_SERVICE_PLATFORM=linux/amd64` 单独指定（compose 已不再强制）。
  - `.env.docker` 的 `JWT_SECRET`/`INTERNAL_API_TOKEN`/`ADMIN_SECRET` 仍是占位值、密码 `123456` 明文入库，仅适用于内网低敏环境；提升安全性时再统一处理。
  - 项目文档中仍有以旧 IP `10.130.33.131` 作示例的段落（`03_Docker部署方案.md`、`01_部署与CORS配置.md`、`02_Codex查询CLI与导出包.md`、`OPS/CLI/README.md`），属示例值，按需再清扫。
- **相关文件**：`docker-compose.yml`、`scripts/deploy.sh`、`scripts/deploy-fast.sh`、`scripts/deploy-author-with-data.sh`、`scripts/sync-production-data-to-local.sh`、`.env.docker`、`docs/用户指南/正式环境部署指南.md`。
