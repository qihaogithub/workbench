# CLI 自动维护 - 2026-06-30 页面版本能力补齐

> 状态：已完成
> 创建日期：2026-06-30

## 背景

- 本任务由 `opencode-workbench-cli` 自动维护线程执行。
- 本轮目标是复查 CLI 与 Web 创作端能力对齐状态，并优先处理可以安全自动实现的低风险或规则明确缺口。
- 2026-06-28 的长期跟踪仍把页面版本历史、会话管理、工作区管理、知识文档 CRUD 和截图任务命令记为缺口，需要重新按当前代码与文档现状核对。

## 目标

- 复查 `project-cli` 命令注册、全命令测试、`project-core` 共享能力和 Web API 对齐状态。
- 优先补齐页面版本历史的 CLI / `project-core` 能力。
- 对其余缺口给出新的自动化等级判断和停止原因。
- 同步更新 CLI 长期文档、能力清单和长期跟踪记录。

## 范围

- `packages/project-cli/src/index.ts`
- `packages/project-cli/src/cli-all-commands.test.ts`
- `packages/project-core/src/service.ts`
- `packages/project-core/src/types.ts`
- `docs/项目文档/创作端/10-CLI/`
- `docs/plans/进行中/CLI与创作端能力对齐长期跟踪.md`

不在本次范围内：

- 管理后台能力
- 权限、鉴权、发布语义变更
- 依赖真实线上密钥或真实远端服务的实现

## 方案

1. 读取 CLI 自动维护文档、项目管理版本文档和相关 Web API 路由。
2. 复查当前注册命令与能力清单、全命令测试是否仍一致。
3. 将页面版本历史拆成三项能力处理：
   - `page version-list`：只读，按 L3 处理。
   - `page version-get`：只读，按 L3 处理。
   - `page version-create`：写操作，但规则明确，按 L4 处理。
4. 其余缺口按“是否已有共享领域能力、是否涉及外部服务、是否改变业务语义”重新分级；不满足自动实现条件时仅更新报告和文档。
5. 按改动范围运行 `corepack pnpm check:project-core` 与 `corepack pnpm check:project-cli`。

## 任务清单

- [x] 读取根 AGENTS.md、CLI 自动维护文档和长期跟踪文档
- [x] 检查 git 状态与相关目录现状
- [x] 复查命令注册、测试覆盖和主要 Web/API 缺口
- [x] 补齐页面版本历史的 `project-core` 能力
- [x] 补齐页面版本历史的 CLI 命令与全命令测试
- [x] 更新 CLI 能力清单、运行文档和长期跟踪记录
- [x] 运行验证并整理本次结构化报告

## 进度记录

- 2026-06-30：确认本轮在 `/Users/qh2/Documents/PGM/1·Work/opencode-workbench` 执行，当前分支 `main` 工作区干净。
- 2026-06-30：复查 CLI 自动维护文档后，确认应优先处理“已有共享语义、无需新增业务规则”的缺口。
- 2026-06-30：扫描 `project-cli`、`project-core` 与 author-site API，确认命令注册和全命令测试主干仍对齐；当前最适合自动补齐的是页面版本历史能力。
- 2026-06-30：确认 `project duplicate` 参数顺序回归已在当前代码中修复，旧长期结论需要更新。
- 2026-06-30：为 `project-core` 增加页面版本列表、读取和创建能力，并修正项目版本号与页面版本号共享递增序列，避免后续项目版本与页面版本撞号。
- 2026-06-30：为 CLI 增加 `page version-list`、`page version-get`、`page version-create`，补齐全命令测试和 `project-core` 单测。
- 2026-06-30：运行 `corepack pnpm check:project-core` 与 `corepack pnpm check:project-cli`，结果均通过。

## 验证方式

- `corepack pnpm check:project-core`
- `corepack pnpm check:project-cli`

如根级检查失败，记录失败摘要并说明是否属于环境阻塞或真实回归。

## 风险与待确认事项

- 会话管理、工作区管理、知识文档 CRUD、截图任务命令跨越 author-site 本地文件逻辑、agent-service 或 screenshot-service，若缺少共享领域能力，本轮不绕过 `project-core` 直接补 CLI。
- 页面版本创建属于写操作，即使实现完成也按 L4 对待，不自动合入。
