# 极简用户系统方案（内部工具版）

## 一、需求概述

| 需求 | 选择 |
|------|------|
| 认证方式 | 用户名 + 密码 |
| 存储方案 | SQLite (better-sqlite3) |
| 扩展需求 | 多设备同步 |
| 使用场景 | **局域网内部工具** |

### 内部工具优化原则

1. **去除冗余**：不需要 sessions 表、设备管理、远程吊销
2. **纯 JWT 方案**：无状态设计，天然支持多设备
3. **简化部署**：首次访问自动初始化
4. **最小配置**：只需 1 个环境变量（JWT_SECRET）

### 为什么选择纯 JWT？

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| **纯 JWT** ✅ | 无状态，性能优秀，代码简单 | 无法主动吊销 | **内部工具** |
| JWT + sessions 表 | 可吊销会话 | 需要额外存储和查询 | 公网应用 |

**内部工具选择纯 JWT 的理由**：
- 用户少（通常 < 20 人），不需要精确控制会话
- 内网环境，安全风险低
- 登出只需清除 Cookie，7 天后自动过期
- 代码量减少 40%（~300 行 vs ~500 行）

---

## 二、技术栈

| 组件 | 选择 | 版本 | 理由 |
|------|------|------|------|
| **数据库** | `better-sqlite3` | ^11.0.0 | 同步 API，无需额外进程 |
| **密码加密** | `bcrypt` | ^5.1.1 | 行业标准，抗暴力破解 |
| **会话管理** | `jose` | ^6.0.0 | JWT 签发/验证，无状态 |
| **类型定义** | `@types/better-sqlite3` | ^7.4.0 | TypeScript 支持 |
| **类型定义** | `@types/bcrypt` | ^5.0.2 | TypeScript 支持 |

### 安装命令

```bash
cd packages/web
pnpm add better-sqlite3 bcrypt jose
pnpm add -D @types/better-sqlite3 @types/bcrypt
```

---

## 三、数据库设计

### 3.1 数据库文件位置

```
packages/web/data/users.db
```

### 3.2 表结构（仅一个用户表）

```sql
-- 用户表（内部工具只需要这一个表）
CREATE TABLE IF NOT EXISTS users (
  id TEXT PRIMARY KEY,              -- UUID v4
  username TEXT UNIQUE NOT NULL,    -- 用户名（3-20 字符，字母数字下划线）
  password_hash TEXT NOT NULL,      -- bcrypt 哈希（10 轮）
  created_at INTEGER NOT NULL       -- 创建时间戳（毫秒）
);
```

### 3.3 设计说明

| 字段 | 说明 |
|------|------|
| `id` | UUID v4，主键 |
| `username` | 唯一索引，3-20 字符，正则 `/^[a-zA-Z0-9_]{3,20}$/` |
| `password_hash` | bcrypt 哈希，10 轮加密 |
| `created_at` | Unix 时间戳（毫秒） |

### 3.4 为什么不需要 sessions 表？

内部工具的特点：
- 用户数量少（通常 < 20 人）
- 安全风险低（内网环境）
- 不需要远程吊销会话
- 不需要查看登录设备

**纯 JWT 方案完全满足需求**：
- 登录时创建 JWT → 设置 Cookie
- 登出时清除 Cookie（JWT 7 天后自动过期）
- 多设备同时使用（JWT 无状态，天然支持）

---

## 四、目录结构

```
packages/web/
├── data/
│   └── users.db                    # SQLite 数据库文件
├── src/
│   ├── lib/
│   │   ├── db/
│   │   │   ├── index.ts            # SQLite 连接管理
│   │   │   └── schema.ts           # 数据库表定义和初始化
│   │   ├── auth/
│   │   │   ├── password.ts         # 密码加密/验证
│   │   │   └── jwt.ts              # JWT 签发/验证/Cookie 管理
│   │   └── user.ts                 # 用户 CRUD 操作
│   │
│   ├── app/
│   │   ├── middleware.ts            # Next.js 中间件（路由守卫）
│   │   │
│   │   ├── api/
│   │   │   ├── auth/
│   │   │   │   ├── register/
│   │   │   │   │   └── route.ts    # POST /api/auth/register
│   │   │   │   ├── login/
│   │   │   │   │   └── route.ts    # POST /api/auth/login
│   │   │   │   └── me/
│   │   │   │       └── route.ts    # GET /api/auth/me
│   │   │   │
│   │   │   └── sessions/
│   │   │       └── route.ts        # 修改：从 Cookie 读取 userId
│   │   │
│   │   ├── (auth)/                 # 认证相关路由组
│   │   │   ├── layout.tsx          # 共享布局（居中卡片）
│   │   │   ├── login/
│   │   │   │   └── page.tsx        # /login 登录页
│   │   │   └── register/
│   │   │       └── page.tsx        # /register 注册页
│   │   │
│   │   ├── (protected)/            # 受保护的路由组
│   │   │   └── ...                 # 现有路由移入此组
│   │   │
│   │   └── page.tsx                # 首页（首次访问引导）
│   │
│   └── components/
│       └── auth/
│           ├── login-form.tsx      # 登录表单组件
│           └── user-menu.tsx       # 用户菜单（右上角）
│
├── next.config.js                  # Next.js 配置
└── .env.local                      # 环境变量（只需 JWT_SECRET）
```

---

## 五、核心实现

### 5.1 数据库连接 (`lib/db/index.ts`)

```typescript
import Database from 'better-sqlite3';
import path from 'path';

const DB_PATH = path.join(process.cwd(), 'data', 'users.db');

let db: Database.Database | null = null;

export function getDb(): Database.Database {
  if (!db) {
    db = new Database(DB_PATH);
    db.pragma('journal_mode = WAL');  // 提升并发性能
    db.pragma('foreign_keys = ON');
  }
  return db;
}

export function closeDb(): void {
  if (db) {
    db.close();
    db = null;
  }
}
```

### 5.2 数据库初始化 (`lib/db/schema.ts`)

```typescript
import { getDb } from './index';

export function initializeDatabase(): void {
  const db = getDb();

  db.exec(`
    CREATE TABLE IF NOT EXISTS users (
      id TEXT PRIMARY KEY,
      username TEXT UNIQUE NOT NULL,
      password_hash TEXT NOT NULL,
      created_at INTEGER NOT NULL
    )
  `);

  console.log('[Database] Users table initialized');
}

/**
 * 获取用户总数（用于首次访问检测）
 */
export function getUserCount(): number {
  const db = getDb();
  const row = db.prepare('SELECT COUNT(*) as count FROM users').get() as { count: number };
  return row.count;
}
```

### 5.3 密码处理 (`lib/auth/password.ts`)

```typescript
import bcrypt from 'bcrypt';

const SALT_ROUNDS = 10;

export async function hashPassword(password: string): Promise<string> {
  return bcrypt.hash(password, SALT_ROUNDS);
}

export async function verifyPassword(
  password: string,
  hash: string
): Promise<boolean> {
  return bcrypt.compare(password, hash);
}

export function validateUsername(username: string): { valid: boolean; error?: string } {
  if (!username || username.length < 3) {
    return { valid: false, error: '用户名至少 3 个字符' };
  }
  if (username.length > 20) {
    return { valid: false, error: '用户名最多 20 个字符' };
  }
  if (!/^[a-zA-Z0-9_]+$/.test(username)) {
    return { valid: false, error: '用户名只能包含字母、数字和下划线' };
  }
  return { valid: true };
}

export function validatePassword(password: string): { valid: boolean; error?: string } {
  if (!password || password.length < 6) {
    return { valid: false, error: '密码至少 6 个字符' };
  }
  return { valid: true };
}
```

### 5.4 JWT 管理 (`lib/auth/jwt.ts`)

```typescript
import { SignJWT, jwtVerify } from 'jose';
import { cookies } from 'next/headers';

const SECRET = new TextEncoder().encode(
  process.env.JWT_SECRET || 'change-me-in-production'
);

export interface UserPayload {
  userId: string;
  username: string;
}

/**
 * 创建 JWT token（7 天过期）
 */
export async function createToken(payload: UserPayload): Promise<string> {
  return new SignJWT(payload)
    .setProtectedHeader({ alg: 'HS256' })
    .setIssuedAt()
    .setExpirationTime('7d')
    .sign(SECRET);
}

/**
 * 验证 JWT token
 */
export async function verifyToken(token: string): Promise<UserPayload | null> {
  try {
    const { payload } = await jwtVerify(token, SECRET);
    return payload as UserPayload;
  } catch {
    return null;
  }
}

/**
 * 设置认证 Cookie（httpOnly，7 天）
 */
export function setAuthCookie(token: string): void {
  cookies().set('auth_token', token, {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'lax',
    maxAge: 7 * 24 * 60 * 60,
    path: '/',
  });
}

/**
 * 获取认证 Cookie
 */
export function getAuthCookie(): string | undefined {
  return cookies().get('auth_token')?.value;
}

/**
 * 清除认证 Cookie（登出）
 */
export function clearAuthCookie(): void {
  cookies().delete('auth_token');
}
```

### 5.5 用户 CRUD (`lib/user.ts`)

```typescript
import { getDb } from '@/lib/db';
import { hashPassword } from '@/lib/auth/password';
import crypto from 'crypto';

export interface User {
  id: string;
  username: string;
  createdAt: number;
}

export interface CreateUserInput {
  username: string;
  password: string;
}

/**
 * 创建用户
 */
export async function createUser(input: CreateUserInput): Promise<User> {
  const db = getDb();
  const id = crypto.randomUUID();
  const passwordHash = await hashPassword(input.password);
  const now = Date.now();

  db.prepare(`
    INSERT INTO users (id, username, password_hash, created_at)
    VALUES (?, ?, ?, ?)
  `).run(id, input.username, passwordHash, now);

  return { id, username: input.username, createdAt: now };
}

/**
 * 根据用户名查找用户
 */
export function findUserByUsername(username: string): User | null {
  const db = getDb();
  return (db.prepare(
    'SELECT id, username, created_at as createdAt FROM users WHERE username = ?'
  ).get(username) as User) || null;
}

/**
 * 根据 ID 查找用户
 */
export function findUserById(id: string): User | null {
  const db = getDb();
  return (db.prepare(
    'SELECT id, username, created_at as createdAt FROM users WHERE id = ?'
  ).get(id) as User) || null;
}

/**
 * 验证用户密码
 */
export async function verifyUserPassword(username: string, password: string): Promise<User | null> {
  const db = getDb();
  const row = db.prepare(
    'SELECT id, username, password_hash FROM users WHERE username = ?'
  ).get(username) as { id: string; username: string; password_hash: string } | undefined;

  if (!row) return null;

  const valid = await (await import('bcrypt')).compare(password, row.password_hash);
  if (!valid) return null;

  return { id: row.id, username: row.username, createdAt: 0 };
}
```

---

## 六、API 路由

### 6.1 注册接口 (`api/auth/register/route.ts`)

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { createUser, findUserByUsername } from '@/lib/user';
import { validateUsername, validatePassword } from '@/lib/auth/password';
import { createToken, setAuthCookie } from '@/lib/auth/jwt';
import { createApiError, createApiSuccess } from '@/lib/fs-utils';

export async function POST(request: NextRequest) {
  try {
    const { username, password } = await request.json();

    const usernameValidation = validateUsername(username);
    if (!usernameValidation.valid) {
      return NextResponse.json(
        createApiError('VALIDATION_ERROR', usernameValidation.error!),
        { status: 400 }
      );
    }

    const passwordValidation = validatePassword(password);
    if (!passwordValidation.valid) {
      return NextResponse.json(
        createApiError('VALIDATION_ERROR', passwordValidation.error!),
        { status: 400 }
      );
    }

    if (findUserByUsername(username)) {
      return NextResponse.json(
        createApiError('CONFLICT', '用户名已存在'),
        { status: 409 }
      );
    }

    const user = await createUser({ username, password });
    const token = await createToken({ userId: user.id, username: user.username });
    setAuthCookie(token);

    return NextResponse.json(createApiSuccess({
      user: { id: user.id, username: user.username },
    }));
  } catch (error) {
    console.error('[Register] Error:', error);
    return NextResponse.json(
      createApiError('INTERNAL_ERROR', '注册失败'),
      { status: 500 }
    );
  }
}
```

### 6.2 登录接口 (`api/auth/login/route.ts`)

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { verifyUserPassword } from '@/lib/user';
import { createToken, setAuthCookie } from '@/lib/auth/jwt';
import { createApiError, createApiSuccess } from '@/lib/fs-utils';

export async function POST(request: NextRequest) {
  try {
    const { username, password } = await request.json();

    if (!username || !password) {
      return NextResponse.json(
        createApiError('VALIDATION_ERROR', '用户名和密码不能为空'),
        { status: 400 }
      );
    }

    const user = await verifyUserPassword(username, password);
    if (!user) {
      return NextResponse.json(
        createApiError('UNAUTHORIZED', '用户名或密码错误'),
        { status: 401 }
      );
    }

    const token = await createToken({ userId: user.id, username: user.username });
    setAuthCookie(token);

    return NextResponse.json(createApiSuccess({
      user: { id: user.id, username: user.username },
    }));
  } catch (error) {
    console.error('[Login] Error:', error);
    return NextResponse.json(
      createApiError('INTERNAL_ERROR', '登录失败'),
      { status: 500 }
    );
  }
}
```

### 6.3 获取当前用户 (`api/auth/me/route.ts`)

```typescript
import { NextResponse } from 'next/server';
import { getAuthCookie, verifyToken } from '@/lib/auth/jwt';
import { findUserById } from '@/lib/user';
import { createApiError, createApiSuccess } from '@/lib/fs-utils';

export async function GET() {
  const token = getAuthCookie();
  if (!token) {
    return NextResponse.json(
      createApiError('UNAUTHORIZED', '未登录'),
      { status: 401 }
    );
  }

  const payload = await verifyToken(token);
  if (!payload) {
    return NextResponse.json(
      createApiError('UNAUTHORIZED', '登录已过期'),
      { status: 401 }
    );
  }

  const user = findUserById(payload.userId);
  if (!user) {
    return NextResponse.json(
      createApiError('NOT_FOUND', '用户不存在'),
      { status: 404 }
    );
  }

  return NextResponse.json(createApiSuccess({
    id: user.id,
    username: user.username,
  }));
}
```

---

## 七、中间件 (`middleware.ts`)

```typescript
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';
import { verifyToken } from '@/lib/auth/jwt';

const PROTECTED_ROUTES = ['/dashboard', '/demo', '/projects', '/api/sessions'];
const AUTH_ROUTES = ['/login', '/register'];

export async function middleware(request: NextRequest) {
  const token = request.cookies.get('auth_token')?.value;
  const user = token ? await verifyToken(token) : null;
  const pathname = request.nextUrl.pathname;

  // 已登录访问登录页 → 重定向到首页
  if (user && AUTH_ROUTES.some(route => pathname.startsWith(route))) {
    return NextResponse.redirect(new URL('/', request.url));
  }

  // 未登录访问受保护路由 → 重定向到登录页
  if (!user && PROTECTED_ROUTES.some(route => pathname.startsWith(route))) {
    const loginUrl = new URL('/login', request.url);
    loginUrl.searchParams.set('redirect', pathname);
    return NextResponse.redirect(loginUrl);
  }

  return NextResponse.next();
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico).*)'],
};
```

---

## 八、Session 管理改造

### 8.1 修改 `session-manager.ts`

```typescript
// 添加 userId 参数
export function findActiveSession(userId: string, projectId: string): string | null {
  const projectSessionDir = getProjectSessionDir(userId, projectId);
  // ... 其余逻辑不变
}

export async function createEditSession(
  userId: string,
  projectId: string
): Promise<CreateSessionResult> {
  const sessionDir = getProjectSessionDir(userId, projectId);
  // ... 其余逻辑不变
}

// 修改目录结构函数
function getProjectSessionDir(userId: string, projectId: string): string {
  return path.join(getSessionsDir(), userId, projectId);
}
```

### 8.2 修改 API 路由读取 userId

```typescript
// api/sessions/route.ts
import { getAuthCookie, verifyToken } from '@/lib/auth/jwt';

export async function POST(request: NextRequest) {
  const token = getAuthCookie();
  const payload = await verifyToken(token!);
  const userId = payload!.userId;
  const { demoId } = await request.json();

  const activeSession = findActiveSession(userId, demoId);
  // ...
}
```

---

## 九、页面实现

### 9.1 登录页 (`app/(auth)/login/page.tsx`)

```tsx
"use client";

import { useState } from "react";
import { useRouter, useSearchParams } from "next/navigation";
import { LoginForm } from "@/components/auth/login-form";
import { useToast } from "@/components/ui/toast-provider";

export default function LoginPage() {
  const router = useRouter();
  const searchParams = useSearchParams();
  const { toast } = useToast();
  const [loading, setLoading] = useState(false);
  const redirect = searchParams.get("redirect") || "/";

  const handleLogin = async (username: string, password: string) => {
    setLoading(true);
    try {
      const res = await fetch("/api/auth/login", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ username, password }),
      });

      const data = await res.json();
      if (!data.success) throw new Error(data.error?.message || "登录失败");

      toast({ title: "登录成功", description: `欢迎回来，${data.data.user.username}` });
      router.push(redirect);
      router.refresh();
    } catch (error) {
      toast({
        title: "登录失败",
        description: error instanceof Error ? error.message : "未知错误",
        variant: "destructive",
      });
    } finally {
      setLoading(false);
    }
  };

  return <LoginForm onSubmit={handleLogin} loading={loading} />;
}
```

### 9.2 用户菜单组件 (`components/auth/user-menu.tsx`)

```tsx
"use client";

import { useRouter } from "next/navigation";
import { useToast } from "@/components/ui/toast-provider";

export function UserMenu({ username }: { username: string }) {
  const router = useRouter();
  const { toast } = useToast();

  const handleLogout = async () => {
    // 清除 Cookie（纯 JWT 方案无需调用后端）
    document.cookie = 'auth_token=; path=/; expires=Thu, 01 Jan 1970 00:00:00 GMT';
    toast({ title: "已登出" });
    router.push("/login");
  };

  return (
    <div className="flex items-center gap-2">
      <span className="text-sm text-muted-foreground">{username}</span>
      <button onClick={handleLogout} className="text-sm text-red-500 hover:underline">
        登出
      </button>
    </div>
  );
}
```

---

## 十、环境变量

创建 `.env.local`：

```env
# JWT 密钥（生产环境务必更换为强随机字符串）
JWT_SECRET=your-super-secret-key-change-this-in-production
```

**生成安全的 JWT_SECRET**：

```bash
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

---

## 十一、数据库初始化

### 11.1 创建初始化脚本

创建 `packages/web/scripts/init-db.ts`：

```typescript
import { initializeDatabase } from '@/lib/db/schema';

console.log('[Init] Initializing database...');
initializeDatabase();
console.log('[Init] Database initialized successfully');
```

### 11.2 添加到 package.json

```json
{
  "scripts": {
    "db:init": "ts-node -r tsconfig-paths/register scripts/init-db.ts"
  }
}
```

### 11.3 运行初始化

```bash
cd packages/web
pnpm db:init
```

---

## 十二、实现步骤

| 步骤 | 任务 | 预估时间 |
|------|------|---------|
| **1** | 安装依赖 | 5 分钟 |
| **2** | 创建数据库模块 | 10 分钟 |
| **3** | 创建认证模块 | 15 分钟 |
| **4** | 创建用户 CRUD | 10 分钟 |
| **5** | 创建 API 路由 | 15 分钟 |
| **6** | 创建中间件 | 10 分钟 |
| **7** | 创建登录/注册页 | 20 分钟 |
| **8** | 创建用户菜单 | 10 分钟 |
| **9** | 改造 session-manager | 15 分钟 |
| **10** | 改造现有 API 路由 | 20 分钟 |
| **11** | 测试多设备同步 | 10 分钟 |
| **总计** | | **~2 小时** |

---

## 十三、多设备同步测试

### 测试步骤

1. **设备 A 登录**：打开浏览器窗口 A，登录账号
2. **设备 B 登录**：打开浏览器窗口 B（或隐身模式），使用相同账号登录
3. **验证多设备同时在线**：
   - 在设备 A 创建/编辑 Demo
   - 在设备 B 刷新页面，验证 session 独立
4. **验证登出不影响其他设备**：
   - 在设备 A 登出
   - 在设备 B 刷新页面，验证仍在线

### 预期结果

| 操作 | 设备 A | 设备 B |
|------|--------|--------|
| 登录 | ✅ 在线 | - |
| 另一设备登录 | ✅ 在线 | ✅ 在线 |
| 设备 A 编辑 Demo | Session 创建 | 无影响 |
| 设备 A 登出 | ❌ 离线 | ✅ 仍在线 |

---

## 十四、安全注意事项

### 14.1 密码安全

- ✅ 使用 bcrypt 哈希，10 轮加密
- ✅ 密码长度限制 6-128 字符
- ❌ 禁止明文存储密码

### 14.2 JWT 安全

- ✅ httpOnly cookie（防止 XSS 窃取）
- ✅ secure 标志（生产环境 HTTPS）
- ✅ sameSite: 'lax'（防止 CSRF）
- ✅ 7 天过期时间

### 14.3 数据库安全

- ✅ 启用 WAL 模式提升并发性能
- ❌ 禁止将数据库文件提交到 Git

### 14.4 生产环境建议

1. **更换 JWT_SECRET**：使用强随机字符串
2. **启用 HTTPS**：生产环境必须使用 HTTPS
3. **速率限制**：登录接口添加限流（防止暴力破解）

---

## 十五、相关文件清单

### 新增文件

| 文件 | 作用 |
|------|------|
| `packages/web/src/lib/db/index.ts` | SQLite 连接管理 |
| `packages/web/src/lib/db/schema.ts` | 数据库表定义 |
| `packages/web/src/lib/auth/password.ts` | 密码加密/验证 |
| `packages/web/src/lib/auth/jwt.ts` | JWT 签发/验证 |
| `packages/web/src/lib/user.ts` | 用户 CRUD |
| `packages/web/src/middleware.ts` | Next.js 中间件 |
| `packages/web/src/app/api/auth/register/route.ts` | 注册接口 |
| `packages/web/src/app/api/auth/login/route.ts` | 登录接口 |
| `packages/web/src/app/api/auth/me/route.ts` | 获取当前用户 |
| `packages/web/src/app/(auth)/layout.tsx` | 认证页布局 |
| `packages/web/src/app/(auth)/login/page.tsx` | 登录页 |
| `packages/web/src/app/(auth)/register/page.tsx` | 注册页 |
| `packages/web/src/components/auth/login-form.tsx` | 登录表单 |
| `packages/web/src/components/auth/user-menu.tsx` | 用户菜单 |
| `packages/web/scripts/init-db.ts` | 数据库初始化脚本 |

### 修改文件

| 文件 | 修改内容 |
|------|---------|
| `packages/web/package.json` | 添加依赖和脚本 |
| `packages/web/src/lib/session-manager.ts` | 添加 userId 参数 |
| `packages/web/src/app/api/sessions/route.ts` | 从 Cookie 读取 userId |
| `packages/web/.env.local` | 添加 JWT_SECRET |
| `packages/web/.gitignore` | 排除 `data/users.db` |

---

**文档版本**：v2.0（内部工具优化版）  
**创建时间**：2026-04-11  
**作者**：Qwen Code AI Agent
