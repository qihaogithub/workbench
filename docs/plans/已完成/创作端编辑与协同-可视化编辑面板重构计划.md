# 创作端编辑与协同 - 可视化编辑面板重构实现计划

> 关联设计文档：`docs/superpowers/specs/2026-07-24-visual-edit-panel-restructure-design.md`
> 创建日期：2026-07-24
> 状态：已完成

最新问题记录：

AI工具栏的选择按钮交互问题（2026-07-24 已修复）
→ 修复方式：
  - 移除了 chat-input.tsx 中的"选择"按钮和 ai-chat.tsx/page.tsx 中的 pickerActive 状态
  - 在 VisualPropertyPanel 头部显示当前选中图层名称 + "添加到对话"按钮
  - 点击按钮以标签形式 `[@选中元素: div.className]` 插入聊天输入框
  - 用户可在标签前后输入文本，体验类似 @ 提及
  - 修改文件：useVisualEditState.ts（新增 buildVisualSelectionTag 函数）、chat-input.tsx、ai-chat.tsx、VisualPropertyPanel.tsx、page.tsx

右键图层栏缺失（2026-07-24 已修复）
→ 修复方式：
  - PreviewPanel 组件渲染时补充了缺失的 visualLayerTreeNodes prop，使右键菜单能正确使用完整的页面节点树
  - 修改文件：page.tsx

## 背景

项目编辑页单页面模式下，可视化编辑面板交互需要重构：右栏改为 tab 切换（编辑/配置），图层栏改为右键菜单，"选择"按钮移至 AI 工具栏，编辑模式始终开启。

## 任务清单

- [ ] T1: page.tsx 状态变更 — 移除旧 overlay 状态，新增 tab/picker/insert 状态
- [ ] T2: page.tsx 右栏 tab 布局 — 替换 overlay 为 Tabs + 条件渲染
- [ ] T3: VisualPropertyPanel 空状态 — 新增未选中元素时的引导提示
- [ ] T4: page.tsx 移除左侧图层 overlay（普通页）— 仅保留草图页 overlay
- [ ] T5: PreviewPanel 右键菜单增强 — 数据源改为完整节点树
- [ ] T6: page.tsx 预览区头部清理 — 移除"选择"按钮和 VisualDraftActionBar
- [ ] T7: VisualPropertyPanel 底部操作栏 — 新增 VisualDraftActionBar 固定底栏
- [ ] T8: chat-input.tsx AI 工具栏"选择"按钮 — 新增按钮和 props 透传
- [ ] T9: 元素引用插入机制 — 提取 buildVisualSelectionPrompt，实现 chatInputInsert
- [ ] T10: page.tsx 预览区点击处理 — aiPickerActive 时额外插入
- [ ] T11: 边缘情况处理 — 页面/模式切换时重置状态
- [ ] T12: 验证 — 类型检查 + 单元测试 + E2E

## 详细步骤

### T1: page.tsx 状态变更

**文件**: `packages/author-site/src/app/demo/[id]/edit/page.tsx`

1. **移除状态**（L1631-1634）:
   - `visualPropertyDrawerOpen` / `setVisualPropertyDrawerOpen`
   - `visualPropertyDrawerMounted` / `setVisualPropertyDrawerMounted`

2. **移除状态**（L2548-2550）:
   - `visualLayerTreeOpen` / `setVisualLayerTreeOpen`
   - `visualLayerDrawerMounted` / `setVisualLayerDrawerMounted`

3. **新增状态**（在 L1630 `tabValue` 后）:
   ```ts
   const [rightPanelTab, setRightPanelTab] = useState<"edit" | "config">("edit");
   const [aiPickerActive, setAiPickerActive] = useState(false);
   const [chatInputInsert, setChatInputInsert] = useState<string | null>(null);
   ```

4. **修改派生值**（L2559）:
   - `propertyPanelActive` → 移除，替换为内联 `previewMode === "single" && !singlePreviewViewingDocument`
   - 或定义 `const visualEditActive = previewMode === "single" && !singlePreviewViewingDocument;`

5. **修改派生值**（L5331-5351）:
   - `visualLayerDrawerActive` → 移除
   - `layerDrawerMounted` → 简化为 `sketchLayerDrawerActive`
   - `layerDrawerActive` → 简化为 `sketchLayerDrawerActive`

6. **移除 useEffect**（L5353-5377）: 两个 mount/unmount 动画 effect 不再需要

7. **移除 handler**（L5379-5412）:
   - `handleOpenVisualEditMode`
   - `handleCloseVisualEditMode`
   - `handleSubmitVisualDraftAction` 中的 `setVisualPropertyDrawerOpen(false)` / `setVisualLayerTreeOpen(false)` 行

8. **修改 effect**（L5419-5441）: 移除 `visualPropertyDrawerOpen` 依赖和 `setVisualPropertyDrawerOpen(false)` / `setVisualLayerTreeOpen(false)` 调用，改为重置 `rightPanelTab` 和 `aiPickerActive`:
   ```ts
   if (!targetChanged) return;
   setRightPanelTab("edit");
   setAiPickerActive(false);
   setVisualPanelHoverNodeId(null);
   ```

### T2: page.tsx 右栏 tab 布局

**文件**: `packages/author-site/src/app/demo/[id]/edit/page.tsx` (L7507-7579)

将 `previewMode === "single"` 分支（L7509-7579）中的内容替换：

1. **移除** `PageConfigPanel` 直接渲染（L7511-7541）和 `VisualPropertyPanel` overlay（L7542-7570）

2. **新增** shadcn Tabs 包裹:
   ```tsx
   <Tabs value={rightPanelTab} onValueChange={(v) => setRightPanelTab(v as "edit" | "config")}>
     <TabsList className="grid w-full grid-cols-2">
       <TabsTrigger value="edit">编辑</TabsTrigger>
       <TabsTrigger value="config">配置</TabsTrigger>
     </TabsList>
     <TabsContent value="edit" className="flex-1 flex flex-col mt-0 min-h-0">
       <VisualPropertyPanel
         selectedNode={selectedVisualNode}
         sessionId={sessionId}
         projectId={demoId}
         pageId={activeDemoId}
         runtimeType={activeDemoPage?.runtimeType}
         propertyChanges={visualPropertyChanges}
         configMarks={visualConfigMarks}
         aiInstruction={visualAiInstruction}
         usedConfigKeys={visualConfigUsedKeys}
         onPropertyChange={handleVisualPropertyChange}
         onRestoreProperty={handleRestoreVisualProperty}
         onClearChanges={handleClearSelectedVisualProperties}
         onMarkConfig={handleMarkVisualConfig}
         onUpdateConfigMark={handleUpdateVisualConfigMark}
         onRemoveConfigMark={handleRemoveVisualConfigMark}
         onAiInstructionChange={setVisualAiInstruction}
         pendingChangeCount={visualPropertyChanges.length}
         onSaveChanges={handleSaveVisualProperties}
         onSendToAI={handleSubmitVisualDraftAction}
         onDiscardChanges={handleClearVisualProperties}
       />
     </TabsContent>
     <TabsContent value="config" className="flex-1 flex flex-col mt-0 min-h-0">
       <PageConfigPanel
         {/* 保持现有所有 props 不变 */}
         hideDetailHeader
       />
     </TabsContent>
   </Tabs>
   ```

3. **保留** `sketchLayerDrawerActive` 分支（L7571-7578）不变 — 草图页 inspector overlay

4. **注意**: 需要确认 `handleSaveVisualProperties` 是否存在，或使用现有的保存逻辑。检查 `visualDraftAction` 的 save 行为。

### T3: VisualPropertyPanel 空状态

**文件**: `packages/author-site/src/app/demo/[id]/edit/components/VisualPropertyPanel.tsx`

1. 在组件渲染逻辑中，当 `selectedNode` 为 `null` 时，显示空状态:
   ```tsx
   if (!selectedNode) {
     return (
       <div className="flex flex-1 flex-col items-center justify-center gap-3 p-6 text-center">
         <MousePointer2 className="h-8 w-8 text-muted-foreground/50" />
         <p className="text-sm text-muted-foreground">
           点击预览区元素查看属性
         </p>
       </div>
     );
   }
   ```

2. 需要导入 `MousePointer2` from `lucide-react`

### T4: page.tsx 移除左侧图层 overlay（普通页）

**文件**: `packages/author-site/src/app/demo/[id]/edit/page.tsx` (L6882-6923)

1. 修改 overlay 条件：从 `layerDrawerMounted` 改为 `sketchLayerDrawerActive`
2. 移除 `LayerTreeMenu` 渲染分支（L6902-6919），仅保留 `SketchEditorEngineLayerPanel`
3. 简化后的 overlay:
   ```tsx
   {sketchLayerDrawerActive && (
     <div className="absolute inset-0 z-20 flex flex-col border-r bg-card shadow-2xl">
       <div className="flex h-12 shrink-0 items-center gap-2 border-b px-3">
         <div className="flex min-w-0 items-center gap-2">
           <Layers className="h-4 w-4 text-muted-foreground" />
           <span className="truncate text-sm font-medium">图层</span>
         </div>
       </div>
       <div className="min-h-0 flex-1 overflow-hidden p-2">
         <SketchEditorEngineLayerPanel
           host={sketchEditorHost}
           scene={activeSketchScene}
         />
       </div>
     </div>
   )}
   ```

### T5: PreviewPanel 右键菜单增强

**文件**: `packages/demo-ui/src/PreviewPanel.tsx`

1. **新增 prop**: `visualLayerTreeNodes?: VisualNodeTreeItem[]` — 完整页面节点树

2. **修改右键菜单数据源**（~L1180-1215 VISUAL_SELECT handler）:
   - 当 `openLayerPicker === true` 时，优先使用 `visualLayerTreeNodes` prop
   - 如果 prop 为空，回退到 iframe 返回的 `nodeTree` / `buildVisualTreeFromStack`
   ```ts
   const menuTree = visualLayerTreeNodes && visualLayerTreeNodes.length > 0
     ? visualLayerTreeNodes
     : (nodeTree ? [nodeTree] : buildVisualTreeFromStack(nodeStack));
   setVisualContextMenu({ ...position, tree: menuTree });
   ```

3. **菜单样式**（~L1546-1573）: 给 `LayerTreeMenu` 容器或 `scrollClassName` 增加 `max-h-[320px] overflow-y-auto`

4. **page.tsx 传参**: 在 `PreviewPanel` 渲染处（L7347 和 L7429 附近）传入 `visualLayerTreeNodes={visualLayerTreeNodes}`

### T6: page.tsx 预览区头部清理

**文件**: `packages/author-site/src/app/demo/[id]/edit/page.tsx` (L7112-7172)

1. **移除** `VisualDraftActionBar` 条件渲染（L7113-7121）
2. **移除** "选择" `Button`（L7122-7172）— 仅移除非草图页的逻辑，草图页的"选择"按钮（`setSketchEditing` toggle）需要保留

   修改后的头部（L7112 之后）:
   ```tsx
   <div className="flex-1" />
   {activeDemoPage?.runtimeType === "sketch-scene" && (
     <Button
       type="button"
       variant="outline"
       size="icon"
       className={`h-7 w-7 ${
         sketchEditing
           ? "border-emerald-500/80 bg-emerald-500/15 text-emerald-300 hover:bg-emerald-500/20 hover:text-emerald-200"
           : ""
       }`}
       title={sketchEditing ? "退出手绘编辑" : "手绘编辑"}
       aria-label="手绘编辑"
       aria-pressed={sketchEditing}
       onClick={() => setSketchEditing((current) => !current)}
     >
       <MousePointer2 className="h-3.5 w-3.5" />
     </Button>
   )}
   ```

3. **修改 `visualEditMode` 传参**（L7347, L7429）:
   - 从 `visualEditMode={propertyPanelActive}` 改为 `visualEditMode={previewMode === "single" && !singlePreviewViewingDocument}`

### T7: VisualPropertyPanel 底部操作栏

**文件**: `packages/author-site/src/app/demo/[id]/edit/components/VisualPropertyPanel.tsx`

1. **新增 props**:
   ```ts
   pendingChangeCount?: number;
   onSaveChanges?: () => void;
   onSendToAI?: () => void;
   onDiscardChanges?: () => void;
   ```

2. **在属性编辑内容下方**渲染固定底栏:
   ```tsx
   {pendingChangeCount && pendingChangeCount > 0 && (
     <div className="shrink-0 border-t">
       <VisualDraftActionBar
         action={{ kind: "send", count: pendingChangeCount }}
         onPrimary={onSendToAI}
         onCancel={onDiscardChanges}
       />
     </div>
   )}
   ```

3. 需要确认 `VisualDraftActionBar` 的 `action` prop 格式和 `disabled` 逻辑，可能需要调整 props

### T8: chat-input.tsx AI 工具栏"选择"按钮

**文件**: `packages/ai-chat-shared/src/chat/chat-input.tsx`

1. **新增 props**（ChatInputProps ~L206）:
   ```ts
   pickerActive?: boolean;
   onTogglePicker?: () => void;
   ```

2. **在 PromptInputTools 中**（~L350）新增按钮:
   ```tsx
   {onTogglePicker && (
     <Button
       variant={pickerActive ? "default" : "ghost"}
       size="icon"
       onClick={onTogglePicker}
       title="选择页面元素插入对话"
     >
       <MousePointer2 className="h-4 w-4" />
     </Button>
   )}
   ```

3. 导入 `MousePointer2` from `lucide-react`

**文件**: `packages/ai-chat-shared/src/ai-chat.tsx`

1. **新增 props**（AIChatProps ~L100）: `pickerActive`, `onTogglePicker`, `chatInputInsert`, `onChatInputInsertHandled`
2. **透传**给 `ChatInput`
3. **新增 useEffect**: 当 `chatInputInsert` 有值时，追加到输入框并调用 `onChatInputInsertHandled`

**文件**: `packages/author-site/src/app/demo/[id]/edit/page.tsx` (L6239 AIChat 渲染)

1. 传入新 props:
   ```tsx
   pickerActive={aiPickerActive}
   onTogglePicker={() => setAiPickerActive((v) => !v)}
   chatInputInsert={chatInputInsert}
   onChatInputInsertHandled={() => setChatInputInsert(null)}
   ```

### T9: 元素引用插入机制

**文件**: `packages/author-site/src/app/demo/[id]/edit/hooks/useVisualEditState.ts`

1. **提取** `buildVisualSelectionPrompt` 函数（从 `handleSendSelectionToAI` L1490-1507 中提取格式化逻辑）:
   ```ts
   function buildVisualSelectionPrompt(
     node: VisualNodeInfo,
     projectId: string,
   ): string {
     return `请只针对当前可视化选区提出修改建议，不要静默扩大范围。

   【当前选区】
   - 元素：<${node.tagName}>
   - DOM 路径：${node.domPath}
   - className：${node.className || "无"}
   - 文本：${node.textContent || "无"}
   - 页面文件：demos/${projectId}/index.tsx

   请给出可审阅的局部修改建议；如果必须修改选区外代码，请明确说明影响范围。`;
   }
   ```

2. **修改** `handleSendSelectionToAI` 使用 `buildVisualSelectionPrompt`

3. **导出** `buildVisualSelectionPrompt` 供 page.tsx 使用

**文件**: `packages/ai-chat-shared/src/ai-chat.tsx`

1. **新增 `chatInputInsert` 处理**: 使用 `useEffect` 监听 `chatInputInsert`，将文本追加到 `ChatInput` 的输入值末尾（换行分隔），然后调用 `onChatInputInsertHandled`
2. 需要访问 `ChatInput` 的内部状态 — 可能需要通过 ref 或提升输入状态到 `AIChat` 层

### T10: page.tsx 预览区点击处理

**文件**: `packages/author-site/src/app/demo/[id]/edit/page.tsx`

1. **修改** `onVisualSelect` 回调（传给 `PreviewPanel` 的回调）:
   - 始终执行元素选中（现有 `handleVisualSelect` 逻辑）
   - 当 `aiPickerActive` 为 `true` 且选中节点非空时，额外设置 `chatInputInsert`:
   ```ts
   const handleVisualSelectWithPicker = useCallback((node: VisualNodeInfo | null, stack?: VisualNodeInfo[]) => {
     handleVisualSelect(node, stack);
     if (aiPickerActive && node && activeDemoId) {
       setChatInputInsert(buildVisualSelectionPrompt(node, activeDemoId));
     }
   }, [aiPickerActive, handleVisualSelect, activeDemoId]);
   ```

2. 将此回调传给 `PreviewPanel` 的 `onVisualSelect` prop

### T11: 边缘情况处理

**文件**: `packages/author-site/src/app/demo/[id]/edit/page.tsx`

1. **切换单页 → 画布模式**（L7097-7104 "画布"按钮 onClick）:
   - 新增 `setRightPanelTab("edit")` 和 `setAiPickerActive(false)`

2. **切换页面**: 已在 T1 step 8 的 effect 中处理

3. **`aiPickerActive` 时切换页面**: 已在 T1 step 8 的 effect 中处理（重置 `aiPickerActive`）

4. **确认** `confirmDiscardVisualPropertyWork` 逻辑在切换到画布模式时仍然生效（L7098）

### T12: 验证

```bash
# 类型检查
pnpm check:author

# 单元测试（如有相关测试）
pnpm --filter @workbench/author-site test -- --testPathPattern="visual-edit"

# E2E
pnpm test:e2e -- edit-page-regression.spec.ts
```

手动验证：
- [ ] 单页面模式右栏默认显示编辑 tab
- [ ] tab 切换编辑 ↔ 配置正常
- [ ] 编辑 tab 空状态显示引导提示
- [ ] 右键预览区弹出完整图层菜单
- [ ] 右键菜单选中节点 → 编辑栏同步
- [ ] AI 工具栏"选择"按钮切换激活态
- [ ] 激活时点击元素 → 编辑栏选中 + AI 输入框插入
- [ ] 编辑栏底部操作栏显示和操作（保存/发送给AI/取消）
- [ ] 草图页编辑不受影响
- [ ] 画布模式不受影响
- [ ] 左侧栏 tab（AI/页面/文件/版本）正常工作

## 风险与待确认

1. **`VisualDraftActionBar` props 格式**: 需要确认 `action` prop 的确切类型和 `disabled` 逻辑，可能需要调整 T7 的实现
2. **`chatInputInsert` 插入机制**: `ChatInput` 的输入状态可能在内部组件中，需要确认是否能从 `AIChat` 层追加文本。可能需要通过 ref 或 callback 方式
3. **`handleSaveVisualProperties`**: 需要确认保存逻辑的 handler 名称和是否存在
4. **`PreviewPanel` `visualLayerTreeNodes` prop**: 需要确认 `PreviewPanelProps` 类型定义位置（`packages/demo-ui/src/types.ts`）并正确新增 prop
5. **page.tsx 体积**: 文件已 7948 行，本次改动会移除部分代码但也会新增，净变化应较小

## 进度记录

- 2026-07-24: 设计 spec 已确认并提交，实现计划已编写
- 2026-07-24: 全部 12 项任务完成，类型检查通过，已提交
- 2026-07-24: 修复 AI 工具栏选择按钮交互问题（移除选择按钮，新增"添加到对话"标签插入）和右键图层栏缺失（补充 visualLayerTreeNodes prop）。类型检查 + lint 通过。
