# CLI 与创作端能力对齐检查 - 2026-06-28

## 背景

- 本任务由 `cli` 自动化每日执行。
- 目标是确认 `packages/project-cli` 是否覆盖创作端 Web 除管理后台外的可触达能力，并同步检查 `project-core` / `project-scaffold` / author-site API 之间的共享能力边界和验证状态。

## 目标

- 复核 [`docs/项目文档/创作端/10-CLI/CLI能力_需求文档.md`](../../项目文档/创作端/10-CLI/CLI能力_需求文档.md)。
- 识别创作端 Web 当前暴露的项目、模板、页面/文件夹、配置、资产、发布、AI、会话、工作区、知识库和截图任务能力。
- 对照 CLI 的命令入口、共享服务层能力、测试覆盖与验证脚本结果。

## 范围

- 文档基线：
- [`docs/项目文档/INDEX.md`](../../项目文档/INDEX.md)
- [`docs/项目文档/创作端/03-项目管理/`](../../项目文档/创作端/03-项目管理/)
- [`docs/项目文档/创作端/04-配置与预览/`](../../项目文档/创作端/04-配置与预览/)
- [`docs/项目文档/创作端/05-AI对话/`](../../项目文档/创作端/05-AI对话/)
- [`docs/项目文档/创作端/09-知识库/`](../../项目文档/创作端/09-知识库/)
- 代码范围：
- [`packages/project-cli/src/index.ts`](../../../packages/project-cli/src/index.ts)
- [`packages/project-core/src/service.ts`](../../../packages/project-core/src/service.ts)
- [`packages/project-scaffold/src/index.ts`](../../../packages/project-scaffold/src/index.ts)
- [`packages/author-site/src/app/api/`](../../../packages/author-site/src/app/api/)

## 方案

- 先读 CLI 基线与创作端相关模块文档，确认当前 Web 语义。
- 用 CodeGraph 建立 CLI / project-core / author-site API 的结构上下文。
- 再针对缺口点读取具体路由或命令源码，确认是否有共享服务支撑、CLI 入口和测试覆盖。
- 最后运行最小验证；当 `pnpm check:*` 被环境阻塞时，退回到现有 `node_modules/.bin` 的 `tsc`、`tsx`、`vitest` 直跑。

## 任务清单

- [x] 读取自动化记忆与长期跟踪文档
- [x] 读取 CLI 基线和相关 Web 模块文档
- [x] 对照 CLI / project-core / project-scaffold / author-site API 能力
- [x] 识别当前 CLI 缺口与验证风险
- [x] 运行最小但足够的验证
- [x] 更新长期跟踪文档

## 进度记录

- 2026-06-28：复核 CLI 基线后，确认 Web 侧页面版本、会话、工作区、知识库和截图任务能力仍存在，而 CLI 仍缺对应命令。
- 2026-06-28：发现一个新回归。`project-cli` 的 `project duplicate` 在 [`packages/project-cli/src/index.ts`](../../../packages/project-cli/src/index.ts) 第 599 行把 `actor` 误传给 `duplicateProject` 的 `category` 位置，导致类型检查失败，也会在全命令测试里运行时崩溃。
- 2026-06-28：`project-core` 首次发布版本号单测失败，但结合 [`docs/项目文档/创作端/03-项目管理/会话管理_需求文档.md`](../../项目文档/创作端/03-项目管理/会话管理_需求文档.md) 与 [`packages/project-core/src/service.ts`](../../../packages/project-core/src/service.ts) 的实现看，当前语义是“发布前自动生成发布快照”，因此更像测试预期过期，而不是产品行为回退。
- 2026-06-28：`project-scaffold` 测试在 `pnpm install --lockfile-only` 处失败，属于 `pnpm@11` 与仓库 `pnpm@8.15.0` 锁文件不兼容导致的环境阻塞。

## 结论

- 本次检查结论：`验证失败`
- 能力面对比：
- CLI 已覆盖项目、模板、本地项目包、编辑事务、页面/文件夹、配置、资产、预览基础、发布、AI 摘要、审计和管理员锁定的大部分主干能力。
- Web 对 CLI 仍有 5 个已知缺口：页面版本历史/快照查询、会话管理、工作区管理、知识文档 CRUD、截图任务命令。
- 本轮未发现新的 Web 能力面缺口，但新增 1 个 CLI 已有命令回归：`project duplicate`。

## 发现

### P0：现有 CLI 命令回归

- [`packages/project-cli/src/index.ts`](../../../packages/project-cli/src/index.ts) 第 599 行
- 问题：`service.duplicateProject(projectId, name, actor)` 与 [`packages/project-core/src/service.ts`](../../../packages/project-core/src/service.ts) 第 564-581 行签名不匹配，`actor` 被当成 `category` 传入。
- 影响：`project duplicate` 类型检查失败，且 `cli-all-commands.test.ts` 运行时在 `normalizeProjectCategory(category)` 处崩溃。
- 需要补什么：
- 修正调用为显式传 `undefined` 类别或补 `--category`
- 补 `project duplicate` 正常路径回归测试

### P1：页面版本历史/快照命令缺失

- Web 证据：
- [`packages/author-site/src/app/api/projects/[projectId]/demos/[demoId]/versions/route.ts`](../../../packages/author-site/src/app/api/projects/[projectId]/demos/[demoId]/versions/route.ts)
- [`packages/author-site/src/app/api/projects/[projectId]/demos/[demoId]/versions/[versionId]/route.ts`](../../../packages/author-site/src/app/api/projects/[projectId]/demos/[demoId]/versions/[versionId]/route.ts)
- CLI 现状：
- [`packages/project-cli/src/index.ts`](../../../packages/project-cli/src/index.ts) 只有 `page restore-version`
- [`packages/project-core/src/service.ts`](../../../packages/project-core/src/service.ts) 只有 `restorePageVersion`
- 缺什么：
- `page version-list`
- `page version-get`
- `page version-create`
- `project-core` 对应共享读取/创建能力

### P1：会话管理命令缺失

- Web 证据：
- [`packages/author-site/src/app/api/sessions/route.ts`](../../../packages/author-site/src/app/api/sessions/route.ts)
- [`packages/author-site/src/app/api/sessions/project/[projectId]/route.ts`](../../../packages/author-site/src/app/api/sessions/project/[projectId]/route.ts)
- [`packages/author-site/src/app/api/sessions/[sessionId]/route.ts`](../../../packages/author-site/src/app/api/sessions/[sessionId]/route.ts)
- CLI 现状：
- [`packages/project-cli/src/index.ts`](../../../packages/project-cli/src/index.ts) 只有 `ai session-list/get/run-logs/workspace-context/send-message`
- 缺什么：
- 会话创建或复用
- 项目会话列表
- 会话详情
- 会话删除

### P1：工作区管理命令缺失

- Web 证据：
- [`packages/author-site/src/app/api/workspaces/route.ts`](../../../packages/author-site/src/app/api/workspaces/route.ts)
- [`packages/author-site/src/app/api/workspaces/[workspaceId]/route.ts`](../../../packages/author-site/src/app/api/workspaces/[workspaceId]/route.ts)
- CLI 现状：
- `project-core` 只在编辑事务和 AI 会话辅助方法里间接处理工作区，没有独立公共命令面。
- 缺什么：
- `workspace create`
- `workspace list`
- `workspace get`
- `workspace delete`

### P1：知识文档 CRUD 命令缺失

- Web 证据：
- [`packages/author-site/src/app/api/knowledge/route.ts`](../../../packages/author-site/src/app/api/knowledge/route.ts)
- [`packages/author-site/src/app/api/knowledge/content/route.ts`](../../../packages/author-site/src/app/api/knowledge/content/route.ts)
- [`packages/author-site/src/app/api/knowledge/[docId]/route.ts`](../../../packages/author-site/src/app/api/knowledge/[docId]/route.ts)
- CLI 现状：
- [`packages/project-scaffold/src/index.ts`](../../../packages/project-scaffold/src/index.ts) 只支持把知识文件随本地项目包拉取/提交，没有在线工作区的 CRUD 命令。
- 缺什么：
- `knowledge list/create/get/update/delete`
- 对应 JSON 输出、错误码和测试

### P1：截图任务命令缺失

- Web 证据：
- [`packages/author-site/src/app/api/screenshots/generate/route.ts`](../../../packages/author-site/src/app/api/screenshots/generate/route.ts)
- [`packages/author-site/src/app/api/screenshots/generate-batch/route.ts`](../../../packages/author-site/src/app/api/screenshots/generate-batch/route.ts)
- [`packages/author-site/src/app/api/screenshots/status/[projectId]/[batchId]/route.ts`](../../../packages/author-site/src/app/api/screenshots/status/[projectId]/[batchId]/route.ts)
- [`packages/author-site/src/app/api/screenshots/cancel/[projectId]/[batchId]/route.ts`](../../../packages/author-site/src/app/api/screenshots/cancel/[projectId]/[batchId]/route.ts)
- CLI 现状：
- [`packages/project-cli/src/index.ts`](../../../packages/project-cli/src/index.ts) 只有 `preview screenshot`、`preview healthcheck`，只够看服务可用性，不够驱动截图任务。
- 缺什么：
- `screenshot generate`
- `screenshot generate-batch`
- `screenshot status`
- `screenshot cancel`
- `screenshot file`

### P2：测试与环境问题

- [`packages/project-core/src/__tests__/service.test.ts`](../../../packages/project-core/src/__tests__/service.test.ts) 第 442 行
- 问题：首次发布断言仍写死 `v1`，与当前“发布前自动生成发布快照”的实现不一致。
- 建议：按现行语义更新测试，而不是回退发布逻辑。

- [`packages/project-scaffold/src/scaffold.test.ts`](../../../packages/project-scaffold/src/scaffold.test.ts) 第 79-82 行
- 问题：测试强依赖 `pnpm install --lockfile-only` 在当前环境返回 0；`pnpm@11` + 仓库 `pnpm@8.15.0` 锁文件会失败。
- 建议：固定测试所用 pnpm 版本，或对非兼容环境显式跳过该断言。

## 验证方式

- 失败：`pnpm check:project-cli`
- 失败：`pnpm check:project-core`
- 失败：`pnpm check:project-scaffold`
- 阻塞原因相同：当前终端 `pnpm@11.7.0` 与仓库锁文件不兼容，且无 TTY 时拒绝清理 `node_modules`

- 通过：在 `packages/project-cli` 目录执行 `../../node_modules/.bin/tsc.CMD --noEmit`
  修正：失败，见上文 P0
- 通过：在 `packages/project-cli` 目录执行 `node --import tsx src/cli.test.ts`
- 失败：在 `packages/project-cli` 目录执行 `node --import tsx src/cli-all-commands.test.ts`
- 通过：在 `packages/project-core` 目录执行 `../../node_modules/.bin/tsc.CMD --noEmit`
- 失败：在 `packages/project-core` 目录执行 `../../node_modules/.bin/vitest.CMD run`
- 通过：在 `packages/project-scaffold` 目录执行 `../../node_modules/.bin/tsc.CMD --noEmit`
- 失败：在 `packages/project-scaffold` 目录执行 `node --import tsx src/scaffold.test.ts`

## 风险与待确认事项

- 如果后续仍用当前终端自带的 `pnpm@11`，根级 `check:*` 会持续受锁文件兼容性影响，无法直接作为稳定回归入口。
- `project duplicate` 修复后，需要重新确认 CLI 主干能力是否仍只有 5 个结构性缺口。
- 本轮未补跑 `check:author`，因为未发现新的 author-site / shared 契约变更；如果后续把会话、工作区、知识库或截图能力下沉到共享层，需要追加 author-site 对齐验证。
