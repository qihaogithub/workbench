# CLI 与创作端能力对齐检查 - 2026-06-27

> 本文档记录 2026-06-27 当前审计证据。长期状态、缺口关闭进度和续跑主线见 [CLI与创作端能力对齐长期跟踪.md](../../进行中/CLI与创作端能力对齐长期跟踪.md)。

## 背景

- 本任务是 `cli` 自动化的当日复查运行。
- 目标是检查 `packages/project-cli` 是否已经覆盖创作端网页版除“管理后台”外的核心能力，并识别命令、接口、测试和文档缺口。

## 目标

- 基于 `docs/项目文档/创作端/10-CLI/CLI能力_需求文档.md` 建立 CLI 能力基线。
- 对照创作端 Web 已有能力，确认 CLI 是否具备等价入口、JSON 输出、错误码、保护性确认和测试覆盖。
- 给出本轮检查结论和可执行后续任务清单。

## 范围

- 文档基线：
  - `docs/项目文档/INDEX.md`
  - `docs/项目文档/创作端/03-项目管理/`
  - `docs/项目文档/创作端/04-配置与预览/`
  - `docs/项目文档/创作端/05-AI对话/`
  - `docs/项目文档/创作端/09-知识库/`
  - `docs/项目文档/创作端/10-CLI/`
- 代码范围：
  - `packages/project-cli/`
  - `packages/project-core/`
  - `packages/project-scaffold/`
  - `packages/author-site/` 中与创作端 Web 能力对应的 API / 服务
- 验证范围：
  - `pnpm check:project-cli`
  - `pnpm check:project-core`
  - `pnpm check:project-scaffold`

## 方案

1. 读取 CLI 需求文档和创作端模块文档，整理 Web 能力基线。
2. 用 CodeGraph 梳理 `project-cli`、`project-core`、`project-scaffold` 的命令与服务边界。
3. 映射 author-site 的相关 API / 服务能力，识别 CLI 对齐缺口。
4. 运行最小验证命令，整理结论、风险和后续任务。

## 任务清单

- [x] 读取自动化上下文、仓库指南和文档维护规范
- [x] 读取 CLI 基线文档和项目文档索引
- [x] 建立 Web 能力与 CLI 能力映射
- [x] 识别命令、接口、测试、文档缺口并分级
- [x] 运行最小验证命令
- [x] 输出审计结论与后续任务

## 进度记录

- 2026-06-27：已读取 `doc-maintainer` 技能、CLI 基线文档和创作端相关模块文档。
- 2026-06-27：已确认 `.codegraph/` 可用，并使用 CodeGraph 对比 `project-cli`、`project-core`、`project-scaffold` 与 author-site 对应能力。
- 2026-06-27：已确认 CLI 现有命令覆盖项目、模板、本地项目包、编辑事务、页面/文件夹、配置、资产、预览基础、发布、AI 摘要、审计和锁定；高风险删除操作统一走 preview + confirm token 两阶段。
- 2026-06-27：复查既有缺口后，未发现新的 CLI 对齐缺口；仍存在页面版本历史查询、会话管理、工作区管理、知识文档 CRUD、截图任务 5 类缺口。
- 2026-06-27：`pnpm check:project-cli`、`pnpm check:project-core`、`pnpm check:project-scaffold` 已恢复可运行并全部通过。

## 验证方式

- 以根目录 `check:*` 脚本为主，覆盖 `project-cli`、`project-core`、`project-scaffold`。
- 本轮未发现新的 author-site / shared 契约变更，因此未额外运行 `check:author`。
- 不运行真实 LLM、Docker 或浏览器 E2E。

### 本轮实际执行

- `pnpm check:project-cli`：通过。执行 `pnpm --filter @opencode-workbench/project-cli typecheck` 与 `node --import tsx src/cli.test.ts && node --import tsx src/cli-all-commands.test.ts`。
- `pnpm check:project-core`：通过。执行 `pnpm --filter @opencode-workbench/project-core typecheck` 与 `vitest run`，`11` 个测试全部通过。
- `pnpm check:project-scaffold`：通过。执行 `pnpm --filter @opencode-workbench/project-scaffold typecheck` 与 `node --import tsx src/scaffold.test.ts`。

## 当前结论

- 结论等级：部分缺口
- 未发现新的 CLI 对齐缺口。
- CLI 主干能力对齐度较高，但仍缺少 5 组 Web 已上线且面向用户/代理可触达的命令入口。

## 发现的主要缺口

### 1. 页面版本历史只支持恢复，不支持查询历史列表和版本内容

- Web 已提供页面版本列表、版本内容读取和版本恢复：
  - `packages/author-site/src/lib/project-api.ts:94`
  - `packages/author-site/src/lib/project-api.ts:125`
  - `packages/author-site/src/lib/project-api.ts:139`
  - `packages/author-site/src/app/api/projects/[projectId]/demos/[demoId]/versions/route.ts:21`
  - `packages/author-site/src/app/api/projects/[projectId]/demos/[demoId]/versions/[versionId]/route.ts:21`
- CLI 只有页面版本恢复入口：
  - `packages/project-cli/src/index.ts:865`
  - `packages/project-core/src/service.ts:1233`
- `packages/project-cli/src/index.ts` 中未注册 `page version-list`、`page version-get`。

### 2. 会话管理仍缺少独立命令

- Web 已提供项目维度会话列表与单会话详情/删除：
  - `packages/author-site/src/app/api/sessions/project/[projectId]/route.ts:15`
  - `packages/author-site/src/app/api/sessions/[sessionId]/route.ts:15`
  - `packages/author-site/src/app/api/sessions/[sessionId]/route.ts:72`
- CLI 仅提供 AI 侧摘要读取，不提供项目会话管理：
  - `packages/project-cli/src/index.ts:1064`
  - `packages/project-cli/src/index.ts:1069`
  - `packages/project-core/src/service.ts:1866`
- `packages/project-cli/src/index.ts` 中未注册 `session *` 命令。

### 3. 工作区管理仍缺少独立命令

- Web 已提供工作区创建、列表、详情和删除：
  - `packages/author-site/src/app/api/workspaces/route.ts:13`
  - `packages/author-site/src/app/api/workspaces/route.ts:58`
  - `packages/author-site/src/app/api/workspaces/[workspaceId]/route.ts:12`
  - `packages/author-site/src/app/api/workspaces/[workspaceId]/route.ts:57`
- CLI 只有 AI 会话关联工作区文件列表：
  - `packages/project-cli/src/index.ts:1079`
  - `packages/project-core/src/service.ts:1895`
- `packages/project-cli/src/index.ts` 中未注册 `workspace *` 命令。

### 4. 知识文档 CRUD 仍未开放到 CLI

- Web 已提供知识文档列表、新增、读取正文、更新、删除：
  - `packages/author-site/src/app/api/knowledge/route.ts:87`
  - `packages/author-site/src/app/api/knowledge/route.ts:113`
  - `packages/author-site/src/app/api/knowledge/content/route.ts:10`
  - `packages/author-site/src/app/api/knowledge/[docId]/route.ts:43`
  - `packages/author-site/src/app/api/knowledge/[docId]/route.ts:122`
- CLI 命令表中没有任何 `knowledge *` 入口。

### 5. 截图任务链路仍未开放到 CLI

- Web 已提供单次生成、批量生成、状态查询、取消和文件访问：
  - `packages/author-site/src/app/api/screenshots/generate/route.ts:11`
  - `packages/author-site/src/app/api/screenshots/generate-batch/route.ts:11`
  - `packages/author-site/src/app/api/screenshots/status/[projectId]/[batchId]/route.ts:10`
  - `packages/author-site/src/app/api/screenshots/cancel/[projectId]/[batchId]/route.ts:10`
  - `packages/author-site/src/app/api/screenshots/file/[projectId]/[pageId]/route.ts:91`
- CLI 目前只有截图服务可用性检查：
  - `packages/project-cli/src/index.ts:1009`
  - `packages/project-core/src/service.ts:1673`
  - `packages/project-core/src/service.ts:1695`
- `packages/project-cli/src/index.ts` 中未注册截图任务生成、状态、取消或文件访问命令。

## 已确认对齐项

- 项目、模板、本地项目包和模板初始化：
  - `packages/project-cli/src/index.ts:557`
  - `packages/project-cli/src/index.ts:627`
  - `packages/project-cli/src/index.ts:705`
- 编辑事务、页面、文件夹、配置、资产：
  - `packages/project-cli/src/index.ts:731`
  - `packages/project-cli/src/index.ts:766`
  - `packages/project-cli/src/index.ts:875`
  - `packages/project-cli/src/index.ts:912`
  - `packages/project-cli/src/index.ts:955`
- 发布、AI 摘要、审计、管理员锁定：
  - `packages/project-cli/src/index.ts:1030`
  - `packages/project-cli/src/index.ts:1064`
  - `packages/project-cli/src/index.ts:1100`
  - `packages/project-cli/src/index.ts:1110`
- 高风险删除操作已统一采用预览与确认 token：
  - `packages/project-core/src/service.ts:569`
  - `packages/project-core/src/service.ts:585`
  - `packages/project-core/src/service.ts:767`
  - `packages/project-core/src/service.ts:779`
  - `packages/project-core/src/service.ts:1565`
  - `packages/project-core/src/service.ts:1584`

## 风险与待确认事项

- 当前缺口大多不只是 CLI 命令缺失，还需要把 author-site 路由层能力下沉到 `project-core`，避免 CLI 直接复制 Web 逻辑。
- 会话、工作区、知识库和截图任务都涉及文件系统路径、权限与保护性删除，需要延续现有 JSON 错误码和 preview/confirm 模式。
- 本轮未运行 `check:author`、E2E、真实 LLM、Docker；若后续实现上述缺口，需补相应验证。

## 后续任务清单

1. 为页面版本历史补 `project-core` 读取能力与 `project-cli` 的 `page version-list`、`page version-get`，并在 `cli-all-commands.test.ts` 增加覆盖。
2. 为会话管理补 `session list/get/delete` 命令，并复用 author-site 现有会话元数据语义与稳定错误码。
3. 为工作区管理补 `workspace create/list/get/delete` 命令，并补充受保护删除的确认机制。
4. 为知识文档补 `knowledge list/create/get/update/delete` 命令，支持 Markdown 内容输入与 JSON 输出。
5. 为截图链路补 `preview screenshot-generate`、`preview screenshot-generate-batch`、`preview screenshot-status`、`preview screenshot-cancel`、`preview screenshot-file`，并明确本地文件输出或 URL 返回格式。
