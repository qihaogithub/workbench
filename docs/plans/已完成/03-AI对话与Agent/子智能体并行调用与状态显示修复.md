# 子智能体并行调用与状态显示修复

## 背景

创作端 AI 对话已支持通过 `delegateTask` 委派短生命周期子 Agent。当前用户期望一次任务中可以同时委派多个子智能体，并反馈界面出现“子 Agent 已完成”但整轮 AI 仍在处理、页面结果尚未完成的误导。

## 目标

- 支持主 Agent 在同一轮工具调用中并行启动多个 `delegateTask`。
- 子 Agent 卡片状态能表达“委派任务已返回，等待主 Agent 汇总/验收”，避免误解为整轮 AI 已结束。
- 保持子 Agent 不可递归创建子 Agent 的安全边界。
- 补充最小回归测试，覆盖并行配置与状态显示。

## 范围

- 后端：`packages/agent-service/src/backends/pi-tools/subagent-tool.ts`、相关单元测试。
- 前端：`packages/author-site/src/components/ai-elements/assistant-message.tsx`、相关展示测试。
- 文档：同步 AI 对话和独立 Agent 服务层的子 Agent 行为说明。

不处理本次无关的工作区脏数据、截图缓存、历史计划文档归档。

## 方案

1. 将 `delegateTask` 的工具执行模式从串行改为并行，使 Pi Agent core 可以在同一批工具调用中并发执行多个委派任务。
2. 保留 `runSubagent` 每次委派创建独立 harness、session、env 的模型，并继续用 `activeSubagents` 统一取消和销毁。
3. 在流式中仍把单个 `delegateTask` 的 completed 解释为“委派已返回”；当整条 assistant 消息仍在 streaming 时，显示“待主 Agent 汇总”，最终消息完成后再显示“已完成”。
4. 更新测试和文档，明确“子 Agent 完成”与“整轮 AI 完成”的区别。

## 任务清单

- [x] 建立任务记录，确认问题现象和初步根因。
- [x] 修改后端委派工具执行模式并补测试。
- [x] 修改前端子 Agent 状态展示并补测试。
- [x] 更新相关技术文档。
- [x] 运行匹配验证命令。

## 进度记录

- 2026-06-25 11:55：用户反馈需要多个子智能体并行调用，且状态显示“已完成”但实际整轮任务仍未完成。
- 2026-06-25 12:00：代码证据确认 `delegateTask` 当前声明 `executionMode: 'sequential'`，会使包含该工具的批次按顺序执行；前端 `SubagentTaskBlock` 在 `part.status === "completed"` 时直接显示“已完成”，未结合整轮 `isStreaming` 状态。
- 2026-06-25 12:10：已将 `delegateTask` 调整为并行执行模式，并让每个并行子 Agent 本地记录自己的文件变更后再汇总到父会话，避免并行时结果互相串入。
- 2026-06-25 12:15：前端流式状态改为区分“待主 Agent 汇总”和最终“已完成”，并增加 `part.details` fallback，避免原始详情只显示 `{}`。
- 2026-06-25 12:20：目标单测和类型检查通过。
- 2026-06-25 12:25：用户截图复现“子 Agent 进行中显示已完成”。补充定位：后端初始工具事件传入 `toolCallStatus: "in_progress"`，前端初始入库时未归一化为 `running`，展示层也未识别 `in_progress/pending`，因此落到默认“已完成”。已修复初始事件归一化和展示层兜底。
- 2026-06-25 12:35：用户反馈子 Agent 详情页内容对普通用户不友好。已将默认详情改为用户摘要视图：状态、耗时、页面/文件数量、任务目标、执行结果和变更文件；完整任务、补充上下文、原始详情移动到“技术详情”折叠区。
- 2026-06-25 12:45：用户希望详情页也像 AI 对话页。已将详情弹窗调整为只读对话记录：主 Agent 消息展示委派目标，子 Agent 消息展示状态与结果，更新内容消息展示文件变更，技术详情继续折叠。
- 2026-06-25 12:55：根据截图继续优化默认内容：主 Agent 气泡从完整技术 prompt 改为“请创建/检查/处理 N 个页面”；子 Agent 气泡从原始返回文本改为“已更新 N 个页面、M 个文件”等产品化总结，原始文本仅在技术详情中保留。
- 2026-06-25 13:00：修正摘要边界：支持“3 个新页面”等自然表达，未显式给出数量的整理/修改任务不从文件路径硬推任务数量；组件测试和类型检查通过。
- 2026-06-25 13:10：根据最新截图继续收口只读对话弹窗：对话流限制阅读宽度，主/子 Agent 气泡宽度区分，文件列表增加高度上限，避免弹窗看起来像文件清单页。
- 2026-06-25 13:15：根据反馈调整主 Agent 气泡文案，从过短摘要改为自然交办语气：说明交给子 Agent 的页面任务、期望交付结果和文件变更反馈，同时仍不直接展示完整内部 prompt。
- 2026-06-25 13:20：用户确认主 Agent 气泡应基于真实消息而非模板。已移除关键词模板，改为展示 `delegateTask.parameters.task` 的机械清理版：仅压缩空白、截断超长文本，不改写任务语义。
- 2026-06-25 13:30：根据最终反馈进一步简化：主 Agent 显示真实 `task`，子 Agent 显示真实 `content/error`，两者均使用 Markdown 渲染；长消息不做内容截断，只做视觉折叠并提供展开/收起。
- 2026-06-25 13:40：根据反馈移除弹窗内“更新内容”和“技术详情”区块；文件数量保留在子 Agent 指标中，弹窗主体只保留主/子 Agent 对话消息。
- 2026-06-25 13:50：根据标注截图继续收口 UI：删除弹窗说明文案，主 Agent 气泡改为暗色友好的 muted 背景，子 Agent 消息去掉消息框，状态/耗时/页面/文件弱化为弹窗底部状态栏。
- 2026-06-25 14:00：根据反馈将状态栏移出消息滚动区，弹窗改为标题、可滚动消息区、吸底状态栏三段结构，长消息滚动时状态栏保持固定在弹窗底部。
- 2026-06-25 14:10：根据反馈取消子 Agent 消息折叠，仅主 Agent 长委派消息可折叠；弹窗消息滚动区复用项目 `scrollbar-thin` 样式弱化滚动条。

## 实施摘要

- 后端 `delegateTask` 改为 `executionMode: 'parallel'`，允许 Pi Agent core 在同一批工具调用中并发执行多个委派任务。
- 子 Agent 文件变更由单次委派本地收集，再合并进父会话，避免并行子 Agent 使用共享文件数组切片导致文件变更归属错误。
- 前端在消息仍流式处理中，将已返回的子 Agent 显示为“待主 Agent 汇总”；消息结束归档后显示“已完成”。
- 前端将 `in_progress`、`pending`、`running`、`awaiting-approval` 等非终态统一视为运行中，只有明确终态才显示完成或失败。
- 子 Agent 详情弹窗优先读取 `part.result.details`，并补充 `part.details` fallback，避免详情 payload 被空结果覆盖。
- 子 Agent 详情弹窗默认以只读对话记录展示，复用 AI 消息区的 Markdown 渲染与气泡式阅读体验；技术字段默认收起，避免长 prompt 和 JSON 占满首屏。
- 主 Agent 气泡展示真实委派任务，不使用写死模板、不做语义改写。
- 子 Agent 返回气泡展示真实返回消息或错误消息，不再替换成产品化摘要。
- 主/子 Agent 消息均使用 Markdown 渲染；长消息只在视觉上折叠，展开后可查看完整内容。
- 弹窗内不再重复展示文件清单和原始 payload；文件数量保留为子 Agent 消息下方的指标。
- 弹窗不再展示说明文案；子 Agent 消息不使用卡片框，状态信息以底部状态栏展示。
- 弹窗底部状态栏不参与消息区滚动，始终吸附在弹窗底部。
- 子 Agent 返回消息始终完整展示，不出现折叠按钮；弹窗消息区使用细滚动条。
- System Prompt 和技术文档已同步并行委派规则与状态语义。

## 验证方式

- `pnpm --filter @opencode-workbench/agent-service test -- tests/unit/pi-agent.test.ts`
- `pnpm --filter @opencode-workbench/author-site test -- --testPathPatterns=assistant-message-subagent.test.tsx`
- `pnpm --filter @opencode-workbench/agent-service typecheck`
- `pnpm --filter @opencode-workbench/author-site typecheck`

验证结果：以上命令均已通过。`--testPathPattern` 在当前 Jest 版本中已替换为 `--testPathPatterns`，首次按旧参数运行失败后已用新参数重跑通过。

补充验证：修复 `in_progress` 误判后，`pnpm --filter @opencode-workbench/author-site test -- --testPathPatterns=assistant-message-subagent.test.tsx` 与 `pnpm --filter @opencode-workbench/author-site typecheck` 再次通过。

补充验证：优化只读对话默认摘要后，`pnpm --filter @opencode-workbench/author-site test -- --testPathPatterns=assistant-message-subagent.test.tsx` 与 `pnpm --filter @opencode-workbench/author-site typecheck` 再次通过。

补充验证：收口只读对话弹窗布局后，`pnpm --filter @opencode-workbench/author-site test -- --testPathPatterns=assistant-message-subagent.test.tsx` 与 `pnpm --filter @opencode-workbench/author-site typecheck` 再次通过。

补充验证：优化主 Agent 气泡文案后，`pnpm --filter @opencode-workbench/author-site test -- --testPathPatterns=assistant-message-subagent.test.tsx` 与 `pnpm --filter @opencode-workbench/author-site typecheck` 再次通过。

补充验证：移除主 Agent 气泡模板、改为真实 task 清理版后，`pnpm --filter @opencode-workbench/author-site test -- --testPathPatterns=assistant-message-subagent.test.tsx` 与 `pnpm --filter @opencode-workbench/author-site typecheck` 再次通过。

补充验证：改为主/子 Agent 均显示真实 Markdown 消息并新增长消息折叠测试后，`pnpm --filter @opencode-workbench/author-site test -- --testPathPatterns=assistant-message-subagent.test.tsx` 与 `pnpm --filter @opencode-workbench/author-site typecheck` 再次通过。

补充验证：移除弹窗“更新内容”和“技术详情”区块后，`pnpm --filter @opencode-workbench/author-site test -- --testPathPatterns=assistant-message-subagent.test.tsx` 与 `pnpm --filter @opencode-workbench/author-site typecheck` 再次通过。

补充验证：调整弹窗说明、消息背景和底部状态栏后，`pnpm --filter @opencode-workbench/author-site test -- --testPathPatterns=assistant-message-subagent.test.tsx` 与 `pnpm --filter @opencode-workbench/author-site typecheck` 再次通过。

补充验证：状态栏移出滚动区并吸附底部后，`pnpm --filter @opencode-workbench/author-site test -- --testPathPatterns=assistant-message-subagent.test.tsx` 与 `pnpm --filter @opencode-workbench/author-site typecheck` 再次通过。

补充验证：取消子 Agent 消息折叠并优化消息区滚动条后，`pnpm --filter @opencode-workbench/author-site test -- --testPathPatterns=assistant-message-subagent.test.tsx` 与 `pnpm --filter @opencode-workbench/author-site typecheck` 再次通过。

## 风险与待确认事项

- 多个子 Agent 并行写同一文件时仍可能产生最后写入覆盖；需要主 Agent 在任务拆分时保持文件边界清晰。
- 并行委派会增加同一时间模型 API 并发和资源占用；本次先启用能力，不新增并发数配置。
