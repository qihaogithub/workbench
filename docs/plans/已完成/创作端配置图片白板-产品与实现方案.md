# 创作端配置图片白板：产品与实现方案

> 状态：一期及嵌套对象数组图片支持已完成，完整图片回填方案已通过功能定向质量门禁；交付范围仍需在独立提交/工作树中收敛，完整图片矩阵与灰度性能基线待补
> 创建日期：2026-08-28
> 适用范围：创作端配置面板、图片资产、草图绘图 SDK、协同草稿

## 一、背景

当前配置系统中的图片配置主要通过上传或选择已有图片完成。流程图、线框图、标注图和简单插画等素材需要用户先在外部工具绘制，再回到项目上传；后续修改时也无法恢复原始绘图内容。

项目已有 `@workbench/sketch-core` / `@workbench/sketch-react`，具备场景协议、节点编辑、图片处理、撤销重做和 PNG/SVG 导出能力。本方案拟将其封装为配置图片字段可打开的独立白板，不新增页面运行时，也不改变当前页面画布。

本方案不把白板 scene 当作普通图片资产的附属元数据，也不复用页面草图 scene 的存储与协同链路。白板是创作端私有编辑能力；它最终只向页面配置回填一个已本地化的 PNG 引用。

## 二、目标与非目标

### 目标

- 图片配置字段可直接打开类似 Excalidraw 的绘图白板。
- 绘图结果默认导出 PNG，并回填当前图片配置。
- 回填后可再次打开并编辑原始 scene。
- 覆盖标准单图字段、`imageList` 当前项和对象数组内的图片字段。
- 白板的 scene、视口、草稿和保存链路与页面画布隔离。
- 回填成功时，配置值、白板版本、绑定关系和 PNG 引用属于同一 committed revision。

### 非目标

- 不引入 Excalidraw 作为核心编辑器。
- 不把白板设计成新的页面 runtime。
- 不自动改写代码中的任意图片 URL 或普通字符串。
- 第一阶段不把 SVG 作为配置回填格式。
- 不支持 `oneOf`/变体、视频 `poster`、共享/页面跨作用域复用和 Excalidraw 文件兼容；这些能力在稳定目标绑定协议后另行评估。
- 不把白板 scene 放进 HTML sandbox canonical 文件、execution ticket 或页面 runtime。

## 三、方案决策

### 产品形态

白板以配置字段内的弹窗工作台提供。关闭/取消不改变配置；“回填图片”才提交图片值。

- 对已有白板来源图片，入口为“继续编辑绘图”，恢复其已提交的 scene 与编辑视口。
- 对普通上传图、外链图或历史图片，入口为“用白板重新创作”：以原图作为可删除的背景图创建新白板；在用户回填前不修改原字段。
- 图片列表入口位于每个图片项，明确显示“替换当前项”；列表工具栏可提供“追加白板图片”。删除、排序期间不保留悬空目标。
- 视频能力二期仅对白板开放 `poster` 子字段，绝不把入口表述为编辑视频内容。

### 技术路线

复用 `SketchSceneDocument` 和 sketch-react 受控编辑器，新增 `WhiteboardDialog`、`WhiteboardHost`、一等 `WhiteboardDocument` 资源和 `WhiteboardBinding`。采用 Schema 驱动、fail-closed 的图片字段识别器。

Whiteboard Studio 与 `WhiteboardDialog` 进入同一个 `SketchEditorSurface` 工作区，并通过唯一的 `whiteboard` profile 共享选择、抓手、矩形、圆形、画笔分组/橡皮、文本和图片入口。profile 只限制创建入口与工具激活，不过滤已有 scene 节点；`SketchPageEditor` 和 `html-css-v2` bridge 保持各自边界。

`@workbench/demo-ui` 只负责展示入口并向宿主传出稳定的 `ImageConfigTarget`；它不得直接持有项目、工作区、权限、Authority revision 或协同 IO。author-site 通过 `WhiteboardLauncher` capability 承接这些能力，避免共享表单组件耦合创作端存储。

### 数据与提交合同

配置值继续保持现有图片字符串或数组结构；PNG 作为项目图片资产登记。图片资产按内容可去重、可复用，因此不承担字段所有权。

| 对象 | 职责 | 关键字段 |
| :-- | :-- | :-- |
| `WhiteboardDocument` | 可编辑、版本化的白板内容 | `id`、`version`、已校验的 `scene`、`editorView` |
| `WhiteboardBinding` | 稳定地把一个配置目标关联到白板产物 | `id`、owner（page/project）、稳定 target pointer/数组项 ID、`whiteboardId`、`sceneRevision`、`outputAssetHash` |
| 图片资产 | PNG 内容与来源审计 | content hash、尺寸、MIME、来源类别；不记录字段归属 |

`SketchSceneDocument` 不保存缩放/平移状态，因此 `WhiteboardDocument` 使用版本化 envelope：`scene` 继续按 Sketch 协议校验，`editorView` 单独校验且不混入 scene。

白板 scene 不能按普通 `assets/` 文本文件落地。实现前必须定义一等 `whiteboard-document` 资源类型：受管路径、大小上限、JSON 校验、Authority/Yjs adapter、读取 API、版本与脱敏诊断。它不复用页面 `sketch.scene.json` 的资源语义。

“回填图片”必须走专用 `WhiteboardCommit` command，而非普通表单 `onChange`：

```text
私有草稿 → 导出 PNG → 临时产物/校验 → WhiteboardCommit
  → scene + binding + config-values + 项目资产引用一次提交
  → Authority receipt / revision 投影 → 预览刷新
```

提交失败时，原配置和已发布预览不变，私有草稿与临时产物保留可重试；临时产物在超时后由受控清理任务回收。

## 四、实施任务

- [x] 定义 `WhiteboardDocument`、`WhiteboardBinding`、`ImageConfigTarget` 和 `WhiteboardCommit` 的跨包类型、校验、权限和错误合同。
- [x] 设计并注册一等 `whiteboard-document` 资源类型：受管路径、Authority adapter、版本与读取；不在 `assets/` 写 scene JSON。
- [x] 实现一期 fail-closed 目标解析器：只接受标准 `format: image` 与 `imageList` 的明确当前项。
- [x] 在 demo-ui 提供无 IO 的入口 capability；由 author-site 实现 `WhiteboardLauncher`、`WhiteboardDialog` 与宿主接入。
- [x] 实现私有草稿、快捷键作用域、取消/关闭保护与冲突提示界面。
- [x] 实现完整 V3 scene 的服务端 PNG 渲染、受管静态 PNG 资产归一化、大小校验、Authority staging 与 `WhiteboardCommit` 原子提交；客户端不再提交可信的最终 `pngBase64`。
- [x] 将当前图片背景、全局图床、项目/会话 workspace、上传/粘贴/拖拽/替换、data URL 和外链图片统一纳入资产管线；动图按回填时静态帧处理，资源失败 fail-closed 并返回中文错误。
- [x] 将人工回填与 `html-css-v2` bridge 解耦；完整 scene 负责编辑/恢复/渲染，bridge 只负责代码导入导出和 AI 草稿。
- [x] 实现 V3 文档完整字段保留、服务端 fail-closed 渲染清单、白板图片草稿租约、durable reference 与资产 GC。
- [x] 实现二次编辑、页面复制时 binding/document 派生、延迟 GC 与权限边界。
- [x] 补齐一期单元测试、组件测试和服务路径回归；以 feature flag 灰度开放。性能基线须在目标设备灰度前实测记录。
- [x] 扩展受限嵌套对象数组图片字段：目标以规范路径定位，提交前补齐 Schema 默认值，并保持单图默认值恢复语义。
- [ ] 在一期稳定后评估 `oneOf`、视频 `poster` 与多作用域支持。

## 五、数据流程草图

```text
图片字段 → ImageConfigTarget → WhiteboardLauncher / Dialog
  → WhiteboardDocument 私有草稿（scene + editorView）
  → 图片归一化为受管静态 PNG + V3 scene 校验
  → 服务端按完整 scene 渲染 PNG，生成渲染清单
  → WhiteboardCommit（PNG + scene + binding + 资产引用 + config-values）
  → Authority receipt / revision 投影 → 现有预览刷新
```

草稿保存与配置回填分离。回填失败、权限拒绝或冲突时保留草稿，不覆盖原配置。

### 草稿与回填状态机

`未修改 → 本地已修改 → 草稿已同步 → 导出/上传中 → 回填提交中 → 已回填`

任一阶段可进入 `提交失败` 或 `冲突待处理`。Esc、关闭按钮、遮罩关闭和路由离开在存在未同步修改时都要求确认；关闭后焦点返回原字段入口。冲突不得静默覆盖，用户可选择刷新远端版本、另存为副本或放弃本地稿。

## 六、验证方式

- 识别器：标准单图与 `imageList` 当前项的路径和目标定位；不支持字段不展示入口且不改变原值。
- 编辑器：绘制、文本、撤销重做、导出、二次编辑、焦点陷阱/归还与快捷键作用域。
- 持久化：刷新恢复、权限拒绝、Authority receipt、页面复制、延迟 GC 和未引用保护。
- 回填：预览刷新；尺寸超限、上传失败、权限拒绝或冲突均不改配置；成功结果属于同一 committed revision。
- 协同：两人同时回填同一目标不能静默覆盖，冲突拥有刷新、另存副本、放弃本地稿的恢复路径。
- 输入安全：一期仅允许 data URL 或已本地化项目资产进入 scene；远程图片必须先经受控本地化，导出前检查 canvas taint。
- 隔离性：白板操作不改变页面画布 scene、视口、HTML sandbox canonical 文件或协同资源。
- 性能：在约定浏览器与测试设备上，记录首开耗时、200 节点拖拽输入响应、指定画布尺寸 PNG 导出耗时及失败降级阈值。

## 七、风险与待确认事项

- `WhiteboardDocument` 的资源目录、最大 JSON 大小、Yjs adapter、Authority operation 和读取 API 需要先完成跨包契约设计。
- 现有图片资产登记与临时产物“提升为正式引用”的边界需要在实现前确认，避免回填失败留下孤儿资产。
- PNG 导出是否需要 worker，取决于指定画布尺寸与已本地化图片的实测耗时；应先记录基线再决定。
- 白板复制、配置字段删除、运行值覆盖、历史/发布版本对 binding 可达性和延迟 GC 的保护规则需要统一。
- Excalidraw 文件导入/导出、嵌套/变体字段和视频 `poster` 不属于第一阶段；如后续需要，应以稳定 binding pointer 协议为前提单独设计适配层。

## 八、图片能力优化实施记录

2026-09-01 至 2026-09-02 按图片白板优化方案完成编辑器能力落地及反馈修正：

- [x] 普通图片导入/替换读取解码后的原始像素尺寸，按最长边 600px 等比计算默认 frame；手动尺寸继续由用户输入决定，`source-background` 保留整板边界。
- [x] 图片主工具栏和命令面板改为直接打开本地选择器，选中图片默认插入当前视口中心并自动选中；图片按钮不再进入持续绘图态，空白拖拽不再创建图片占位节点；Studio、`WhiteboardDialog` 和创作端页面编辑宿主统一接入，文件拖入/粘贴/替换保持不变。
- [x] 图片工具栏接入“更换图片 / 裁剪图片 / 描边 / 图层 / 更多”，裁剪菜单支持矩形、圆形和重置。
- [x] 通过 `imageCrop` 保存归一化源区域、首次裁剪 frame 与适配方式；裁剪状态提供 50% 黑遮罩、裁剪柄、失焦/Esc 提交退出和单次连续拖动撤销，编辑过程中保留完整原图并让被裁区域以半透明方式可见。
- [x] 裁剪交互改为固定裁剪框下平移图片内容：裁剪框内部和当前图片内容区域均可拖动，`sourceRect.x/y` 支持有限越界坐标，图片可部分移出裁剪框并保留顶部等局部空白；编辑态使用共享内容 frame 不套 clip，提交态继续裁剪并保持透明 SVG/白底 PNG 语义。
- [x] 图片接入 0–20px、1px 步进描边滑杆，属性面板和悬浮描边面板均保留精确线宽输入；文案统一为“描边宽度”，面板使用白板统一的浅色视觉；文本与分组不提供描边。
- [x] `sketch-core` renderer、`whiteboard-core` `html-css-v2` bridge/`whiteboard-action-v2` action、agent bridge 约束和 WhiteboardDialog JSON 资产本地化链路保留原图尺寸与裁剪数据。

此前定向回归曾通过：`sketch-core` 71 项、`whiteboard-core` 31 项、`sketch-react` 182 项、`demo-ui` 213 项、author-site WhiteboardDialog 11 项与 WhiteboardCommit 回写 7 项，agent-service 与 author-site typecheck 通过；Studio E2E 11 项通过。该记录保留为前序基线，当前门禁见下方最新记录。

## 九、进度记录

- 2026-09-02：执行范围收口修复：创作端 `WhiteboardDialog` 的主工具栏恢复为独立 Whiteboard 基线的选择、抓手、矩形、圆形、文本、图片六项；移除新建创作端白板中的共享工厂教学便签，但保留已确认的“手绘页面”标题和当前图片背景。完整 V3 scene 仍允许已有菱形、线条、箭头、画笔、便签等节点保存、恢复和服务端回填，未通过入口收口误删。新增创作端单测与宿主 E2E 反向断言；Studio 宿主文件无改动。Node 24 下创作端白板定向测试 22/22、Studio E2E 11/11、宿主 E2E 1/1、构建和类型检查通过；Holmes 功能门禁通过，Ford 交付范围门禁暂不通过：当前工作树含此前遗留的非白板改动/生成物，完整图片来源与动图当前帧仍以单测为主，不能据此执行整体发布。

- 2026-09-02：按“白板全量图片回填完整方案”完成收口：新增 V3 完整 scene 原生校验与规范化，代码 bridge 继续独立使用受限校验；新增图片资产 API，将当前背景、workspace/会话资源、图床、data URL、外链及常见图片格式统一解码为受管静态 PNG，并用草稿租约与白板引用保护生命周期；回填请求不再信任客户端 PNG，由服务端使用 `sketch-core` 渲染完整 scene、生成可见图片渲染清单并原子写入 PNG、document、binding 和配置。新增服务端渲染、格式归一化、资源缺失 fail-closed、V3 工作空间/GC 和 API 回归；Node 24 下 whiteboard-core 34/34、sketch-core 73/73、project-core 166/166、Agent 白板工具 5/5、创作端白板相关 5 suites/26 tests 通过，author-site/agent-service/whiteboard-core/sketch-core/project-core 类型检查、author-site 构建和 Holmes/Ford 质量门禁通过。创作端白板宿主 E2E 1/1 通过；完整上传/粘贴/拖拽/动图当前帧矩阵和性能基线仍待在目标设备补跑。

- 2026-09-02：完成“统一门” profile 收口：`@workbench/sketch-react` 的 `SKETCH_EDITOR_PROFILES.whiteboard` 成为 Studio 与 `WhiteboardDialog` 的唯一工具入口来源，两个宿主共同开放选择、抓手、矩形、圆形、画笔分组/橡皮、文本和图片；profile 优先于临时工具参数，已有隐藏入口节点继续按完整 V3 scene 保留。`SketchPageEditor`、bridge 白名单、配置提交协议和图片回填语义未改变。

- 2026-08-28：完成现状调研和技术路线决策，确认优先复用现有 sketch SDK，不直接引入 Excalidraw。
- 2026-08-28：完成方案审阅并重构持久化与提交设计：白板采用 `WhiteboardDocument + WhiteboardBinding + WhiteboardCommit`，不将 scene 作为普通 `assets/` 文本资源，不把字段归属写入可去重图片资产元数据；一期收窄为标准单图与图片列表当前项。尚未开始代码实现。
- 2026-08-28：完成一期入口、私有 Dialog、白板资源协议、Authority 原子回填、页面复制派生、删除解除 binding 与七天延迟 GC。白板 document 已纳入现有协同资源权限适配；Dialog 草稿仍保持私有，未开放为公共多人草稿。完成受管资源、表单入口、Dialog、复制/删除和 GC 的定向回归与跨包 typecheck。功能默认关闭，待灰度环境记录首开、200 节点交互和 PNG 导出性能基线后启用。
- 2026-09-01：根据创作端与独立白板真机走查完成修复闭环：补齐缺失 `config.values.json` 时以 Schema 默认值建立并发基线；隔离白板命令面板/快捷键帮助的内部 Esc，避免误关闭外层 Dialog；将页面级 `config.values.json` 登记为 Authority 受管资源，并修正 `whiteboards/bindings.json` 被通用 document 校验误判的问题；补齐 `assets/whiteboards/<sha>.png` 在 widget、iframe 与预览预热链路中的 session workspace URL 解析。浏览器实测已完成“代码导入矩形 → 回填提交 → 关闭 → 重新打开 → 代码读取”的闭环，配置值、binding、document revision 和 PNG 均已落盘，预览图片自然宽度为 1440，控制台无 error/warn。原图背景及“手绘页面”标题按产品确认保留，不计为缺陷。定向单测、类型检查和创作端真机回归作为本条目收尾门禁；诊断 CLI 的 SQLite fallback/Node ABI 环境限制仍按验证备注保留。
- 2026-09-02：根据浏览器批注修正裁剪预览数据流：裁剪拖动时渲染完整原图并单独叠加半透明遮罩，退出后仍按 `imageCrop` 产出最终裁剪；描边宽度模块改为浅色面板并统一“描边宽度”文案及无障碍标签。新增回归断言覆盖裁剪态无 `clip-path`、原始 frame 可见和浅色控件样式；定向测试继续通过。
- 2026-09-02：根据创作端浏览器批注完成三项修正：共享原型预览绑定支持 `assets/whiteboards/<sha>.png` 并转换为当前 session workspace URL，修复白板回填图片在原型中丢失；填充色块与描边指示器统一为 `16×16px`，对齐独立 Whiteboard Studio；bridge 诊断统一在产生阶段转为中文，创作端代码导入只展示中文文案、不透传英文诊断码。新增资源解析、工具条尺寸和中文错误回归用例；`whiteboard-core` 31/31、`sketch-react` 182/182、`demo-ui` 213/213、WhiteboardDialog 11/11，浏览器页面回归无控制台 error/warn。
- 2026-09-02：根据裁剪反馈完成平移与空白保留：裁剪框保持固定，拖动裁剪框内或当前图片内容区域会更新 `sourceRect.x/y`；核心校验、bridge 序列化/反序列化和 agent action schema 放宽为有限越界坐标并要求仍有源图交集；编辑态遮罩只覆盖图片内容在裁剪区外的区域，顶部空白保持可见，退出后恢复提交态 clip。新增负坐标往返、内容 frame、裁剪框外拖动、平移后缩放与单次撤销回归用例。Node 24.20.0 下 `sketch-core` 72/72、`whiteboard-core` 34/34、`sketch-react` 184/184、agent-service 530/530、Studio 类型检查和 Studio E2E 11/11 通过；author-site 类型检查与 WhiteboardDialog 定向测试 11/11 通过。创作端白板 E2E 使用现有本地用户 `zxy` 通过 1/1；默认 `qihao` 因本地用户数据缺失未采用。React 预览入口不再过滤未解析绑定图片；场景无法渲染时直接显示中文错误并停止渲染，避免静默丢失其他内容。
- 2026-09-02：补齐统一资产管线的项目图片 API URL 解析，并修正浏览器动图当前帧捕获的同步完成边界；外链地址拒绝携带登录凭据并覆盖 IPv4-mapped IPv6 内网地址。历史 V1/V2 白板首次恢复时补入当前图片背景，V3 场景继续尊重用户已删除背景的结果；提交失败时统一返回中文事务错误。资产 API 回归 2/2、`author-site` lint 通过（既有 warning，无 error）。
- 2026-09-02：按反馈调整图片入口：`sketch-react` 主工具栏与命令面板直接触发 Canvas 自有文件选择器，导入后按当前视口中心创建并选中新图片；移除图片工具的空白拖拽占位路径，空白拖拽回到矩形框选。`SketchEditorSurface`、`SketchPageEditor` 与创作端 `SketchEditorEngineStage` 均完成接线；新增共享 Surface 单测、命令面板上传单测和 Studio 上传 E2E。当前环境 Node 22 可通过 `sketch-react` 186/186、Studio E2E 12/12、WhiteboardDialog 15/15、两包 typecheck；创作端白板宿主 E2E 仍因 better-sqlite3 的 Node 24 编译产物与当前 Node 22 ABI 不匹配而无法启动，待目标 Node 24 环境补跑。
