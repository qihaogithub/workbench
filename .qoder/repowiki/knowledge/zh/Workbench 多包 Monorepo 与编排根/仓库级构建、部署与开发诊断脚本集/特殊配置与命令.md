`scripts/deploy.sh` 要求宿主机已配置 `~/.ssh/figma-mirror-deploy-key` 免密登录 `root@10.130.33.131`，且需先复制 `.env.docker` 生成 `.deploy.env`；`local-production-preview.mjs` 会尝试用 `lsof -tiTCP:3200` 释放端口，macOS 上需安装 lsof。