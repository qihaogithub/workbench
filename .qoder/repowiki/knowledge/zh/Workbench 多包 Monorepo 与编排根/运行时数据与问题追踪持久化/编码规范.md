- session 相关目录统一采用 `session-{毫秒时间戳}-{短随机串}` 命名，便于排序与去重。
- JSONL 日志文件以 `msg-{毫秒时间戳}.jsonl` 形式按时间顺序追加，单条 JSON 一行。
- 项目级资源按 `proj_{时间戳}_{短后缀}` 命名，并在 screenshots/、snapshots/、published/ 等多处共享同一 ID 前缀进行关联。
- issues 清单使用 JSONL 行式索引，每条包含 id/title/status/priority/type/files 字段，对应 inbox 下的 Markdown 说明文件。