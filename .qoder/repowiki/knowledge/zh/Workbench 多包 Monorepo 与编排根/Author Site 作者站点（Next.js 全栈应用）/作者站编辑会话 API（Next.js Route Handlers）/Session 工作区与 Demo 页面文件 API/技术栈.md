Next.js Route Handlers + Node `fs`/`path` 同步 I/O；JWT 鉴权（`getAuthCookie` + `verifyToken`）；通过 `@workbench/shared` 的 `applySketchScenePatchOperations` 对 Sketch 场景执行增量 patch 回放；`@workbench/project-core` 的 `RuntimeValidationResult` 做原型/手绘页运行时校验。