- API 路由按资源域在 `src/app/api/<domain>/[...path]/route.ts` 下组织，每个 handler 文件独立实现一个 REST 端点
- 服务端鉴权统一通过 JWT Cookie + Session 中间件校验后再执行业务逻辑
- 前端共享逻辑集中在 `src/lib` 下按功能拆分小模块，并通过 hooks 暴露给上层页面组件使用
- UI 组件优先复用 `src/components/ui` 中的原子组件，业务组件放在对应功能目录下