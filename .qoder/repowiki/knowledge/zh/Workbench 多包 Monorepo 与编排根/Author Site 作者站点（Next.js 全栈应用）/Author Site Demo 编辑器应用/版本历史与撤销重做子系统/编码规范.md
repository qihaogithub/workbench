- 对外暴露的副作用操作统一封装为 `handle*` 命名函数，并在 try/catch 中通过 `toast({ variant: 'destructive' })` 反馈错误，成功路径则刷新相关 history 状态。
- 异步幂等性通过 `ref` 守卫：使用 `autoCheckpointInFlightRef` 防止并发检查点，用 `lastAutoCheckpointSignatureRef` 对比签名避免重复提交。
- 需要跨渲染周期读取最新 props 时采用 `ref` 持有快照（如 `latestSnapshotRef`、`activeDemoIdRef`），配合 `useEffect` 同步更新，避免闭包过期。
- 键盘事件拦截遵循 `shouldIgnoreGlobalUndoRedoEvent` 模式：先判断目标是否为可编辑元素，再校验 Ctrl/Cmd+Z/Y 组合，最后 `event.preventDefault()` 接管行为。