- 编辑页内所有副作用型状态均使用 `xxxRef.current = xxx` 同步 ref + `useState` 双写模式，避免闭包过期导致 stale state。
- 对外部服务的调用统一包裹在自定义 hook 中（`useXxx`），并通过 `ref` 暴露实例方法供命令系统或生命周期回调访问。
- 跨模块共享的类型定义一律从 `@workbench/shared` 与 `@workbench/demo-ui` 引入，不在本目录重复声明。