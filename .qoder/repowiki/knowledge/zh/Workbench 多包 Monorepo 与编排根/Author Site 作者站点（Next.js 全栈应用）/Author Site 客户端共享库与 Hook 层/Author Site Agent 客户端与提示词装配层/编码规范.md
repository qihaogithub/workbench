- 对外部 HTTP 调用一律 try/catch 并返回 `{ ok: boolean; message: string; data?: unknown }` 结果对象，禁止向上抛出异常，由调用方决定 UI 反馈。
- 所有对 agent-service 的内部请求通过 `getServerAgentServiceUrl()` 拼接 baseUrl，并在 headers 中携带 `X-Internal-Token`，token 缺失时直接返回失败结果。
- System Prompt 采用「静态模板 + 运行时正则替换」模式：在 `system-prompt.md` 中预留占位段，由 `buildStaticSystemPrompt` 根据工具能力集分支注入具体规则，避免硬编码多份模板。
- 上下文前缀按 L1/L2/L3/L4 分层拆分独立函数（`buildActiveViewContextPrefix` / `buildStaticSystemPrompt` / `buildDynamicContextPrefix` / `buildMemoryPrefix` / `buildKnowledgeIndexPrefix`），由上层按需拼接。
- 工作区文件读取全部包裹 try/catch 并返回 null 或空集合作为降级值，保证提示词构建不因个别文件缺失而中断。