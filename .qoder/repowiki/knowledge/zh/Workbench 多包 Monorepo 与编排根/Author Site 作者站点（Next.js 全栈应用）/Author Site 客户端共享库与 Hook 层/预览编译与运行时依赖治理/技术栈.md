Node 端使用 Node `crypto.createHash('md5')` 与 `fs` 做编译缓存与模块落盘；浏览器端通过 `fetch('/api/compile')` 远程编译；依赖解析统一走 ESM.SH CDN（由 `getCdnBaseUrl()` 配置），并通过 `@workbench/preview-contract/runtime` 提供的 AST 级 `extractImports`/`rewriteImportsWithResolver` 完成 import 重写。