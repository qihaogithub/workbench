Node.js fs/child_process 执行 shell 脚本；通过 `execFile('bash', ['scripts/sync-to-cloudflare.sh'])` 集成 Cloudflare Pages 部署；预览运行时可选择 CDN 或本地 `public/preview-runtime` 拷贝，由 `PREVIEW_RUNTIME_MANIFEST_VERSION` 控制版本。