- 每个子命令对应 `src/commands/<name>.ts` 中的一个或多个命名导出函数，index.ts 中只负责注册与参数转发，不包含业务逻辑。
- 所有命令函数统一接收 `(url, options, jsonMode?)` 三参形式，其中 `url` 来自全局 `-u/--url` 选项，`jsonMode` 来自全局 `--json` 开关。
- 可写操作默认 dry-run，需显式传入 `--apply` 才真正执行变更（如 workspace-authority-* 系列命令）。