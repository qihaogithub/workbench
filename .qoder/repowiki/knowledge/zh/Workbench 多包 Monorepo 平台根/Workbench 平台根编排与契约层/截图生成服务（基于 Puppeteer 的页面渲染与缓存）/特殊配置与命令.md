`pnpm dev` 通过 tsx watch 启动开发服务器；`pnpm build:docker` 使用 esbuild 以 Node20 目标打包 cjs 产物并 externalize fastify/puppeteer-core 等运行时依赖；Docker 环境需通过 `PUPPETEER_EXECUTABLE_PATH` 或系统路径提供 Chromium，且默认启用 `--no-sandbox` 参数。