Next.js Route Handlers (App Router) + Node `fs`/`crypto` 同步读写；通过 `@workbench/shared` 的 `WorkspaceTree`/`DemoPageMeta` 类型描述工作区树，通过 `@workbench/shared/contracts` 的 `WorkspaceMutationOperation` 抽象原子文件操作，并由 `@/lib/workspace-authority-client.commitWorkspaceMutation` 作为 live workspace 的持久化入口。