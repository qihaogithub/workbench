Next.js App Router Route Handler（`NextRequest`/`NextResponse`）；Agent 客户端通过 `@/lib/agent-client` 访问 http://localhost:3201；知识持久化使用本地文件系统 + JSON manifest，live workspace 场景通过 `WorkspaceAuthorityClientError` 与 `WorkspaceMutationOperation` 提交原子变更。