数据库初始化：`cd packages/author-site && npx ts-node scripts/init-db.ts`；会话目录迁移需设置环境变量：`MIGRATION_DEFAULT_USER_ID="your-user-id" npx ts-node scripts/migrate-sessions.ts`，也可通过 `SESSIONS_DIR` / `DATA_DIR` 覆盖目标路径。