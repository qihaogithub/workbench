- CLI 脚本统一通过 `process.env.*` 读取配置（如 `MIGRATION_DEFAULT_USER_ID`、`SESSIONS_DIR`、`DATA_DIR`），未设置时打印用法后 `process.exit(1)` 退出。
- 一次性脚本采用 try/catch 包裹主逻辑并以 `[Init]` / `[Migration]` 前缀输出结构化日志，失败时输出错误堆栈再 exit。
- 持久化数据目录使用 `manifest.json` 作为索引，记录唯一 id、mimeType、sha256、创建时间等元信息，与实际文件并存于同一子目录。