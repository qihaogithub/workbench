开发端口固定为 3300（`next dev -p 3300`）；构建前先执行工作区脚本 `corepack pnpm --workspace-root build:preview-runtime` 生成 `public/preview-runtime/manifest.json`，再由 `next build` 导出静态站点；本地需设置 `NEXT_PUBLIC_DATA_BASE=http://localhost:3200` 指向数据源，`NEXT_PUBLIC_PREVIEW_RUNTIME_SOURCE=local|cdn` 控制运行时来源，`PREVIEW_SHELL_MODE=inline|fixed` 控制外壳模式。