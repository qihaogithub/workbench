Next.js App Router Route Handlers + Node.js `fs` 同步 I/O；JWT 鉴权通过 `@/lib/auth/jwt`；工作空间变更通过 `@/lib/workspace-authority-client` 以 `WorkspaceMutationOperation` 形式提交；Schema 冲突检测使用 `@/lib/schema-validator.validateNoSchemaConflictFromStrings`。