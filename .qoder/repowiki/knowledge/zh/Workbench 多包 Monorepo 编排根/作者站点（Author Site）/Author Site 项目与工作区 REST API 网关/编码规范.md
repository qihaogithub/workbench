- Route Handler 统一使用 `verifyToken` 解析 JWT Cookie 获取 userId，再结合 `getEditSession` 校验 sessionId 与当前 projectId/workspaceId 的绑定关系
- 业务异常通过 `createApiError` 包装为 `{ code, message }` 标准结构返回，避免直接抛出未捕获错误
- 写路径遵循「Live Workspace → commitWorkspaceMutation → Workspace Authority」模式，非 Live 分支才直写磁盘