Next.js App Router + React Server Components 客户端模式（"use client"），UI 基于 shadcn/ui（Button/Input/Dialog/Tabs 等）与 lucide-react 图标；预览渲染委托 `@workbench/demo-ui` 的 `PreviewCanvas/SketchPagePreview/PrototypePagePreview`，Sketch 场景由 `@workbench/sketch-core` 解析。