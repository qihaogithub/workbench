Cloudflare 同步需存在 `scripts/sync-to-cloudflare.sh` 且设置 `CLOUDFLARE_SYNC_ENABLED=true`；可通过 `VIEWER_CLOUDFLARE_URL` / `VIEWER_LAN_URL` 控制 iframe 嵌入地址；`DATA_DIR` 环境变量决定 `published`、`screenshots`、`projects/*/images.json` 等数据落盘位置。