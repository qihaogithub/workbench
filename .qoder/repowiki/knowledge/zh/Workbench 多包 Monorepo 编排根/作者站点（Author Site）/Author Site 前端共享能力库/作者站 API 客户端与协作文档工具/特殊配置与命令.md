运行时需要设置 `DATA_DIR`、`PROJECTS_DIR`、`TEMPLATES_DIR`、`SESSIONS_DIR`、`WORKSPACES_DIR`、`SNAPTS_DIR` 控制磁盘布局；WebSocket 地址由 `NEXT_PUBLIC_COLLAB_WS_URL` 覆盖，否则自动把 `NEXT_PUBLIC_AGENT_SERVICE_URL` 的 http(s) 前缀替换为 ws(s)。