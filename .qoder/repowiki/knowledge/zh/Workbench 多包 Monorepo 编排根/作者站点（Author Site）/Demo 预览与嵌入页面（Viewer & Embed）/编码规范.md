- 所有 viewer 页面顶部声明 ViewerIncomingMessage / ViewerOutgoingMessage 联合类型，并通过 postOutgoing 封装 window.parent.postMessage 向外广播，入站消息统一用 switch(msg.type) 分发。
- URL 查询参数中 config=false、toolbar=false、modeSwitch=false 等布尔开关采用非 false 即 true 的默认开启策略，pageList=true 则显式要求等于字符串 true 才启用。
- 配置默认值合并遵循 mergeConfigToProps(projectSchema, pageSchema) 再与 URL configData 覆盖的三段式顺序，并用 try/catch 包裹以防 schema 解析失败。
- 主题切换通过 applyTheme 直接操作 document.documentElement.classList 添加或移除 dark，而非走 Tailwind 类名绑定。
- 配置面板按 scope 拆分为 project/page 两个 ConfigScopeWrapper，仅在同时存在时才显示分隔线，且根据 hideDetailHeader 控制标题可见性。