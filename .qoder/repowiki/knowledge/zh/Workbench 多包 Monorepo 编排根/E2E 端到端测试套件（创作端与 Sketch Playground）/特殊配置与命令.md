创作端回归需先运行目标服务并设置 `E2E_BASE_URL`（默认 http://localhost:3200），可选 `DATA_DIR` 指向含 `users.db` 的数据目录、`JWT_SECRET` 指定签名密钥；`E2E_AUTH_COOKIE_ONLY=false` 可强制走 `/api/auth/login` 表单登录路径。Sketch playground 工程无需外部服务，会自动拉起本地 dev server。