Bash + Node.js ESM (`.mjs`) 双栈；部署链路使用 SSH/rsync + Docker Compose（默认服务 `agent-service author-site viewer-site`，可选 `screenshot-service`）；诊断脚本依赖 Playwright Chromium、`@playwright/test` 与 Project CLI (`packages/project-cli/src/index.ts`)。